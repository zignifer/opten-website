# Copy to .env.local and fill in real values. .env.local is gitignored.
#
# Paddle (production: set in Vercel project settings, not here)
VITE_PADDLE_ENV=sandbox
VITE_PADDLE_CLIENT_TOKEN=

# Supabase — opten-seo project (separate from production "opten")
# These stay server-side / script-only — no VITE_ prefix so they are not
# bundled into the client. Real values live only in .env.local on dev
# machines and (if needed) in Vercel env vars scoped to the right env.
SUPABASE_SEO_URL=https://hxqhlsjqrxalyteyqhfd.supabase.co
SUPABASE_SEO_PUBLISHABLE_KEY=
SUPABASE_SEO_SECRET_KEY=
