---
tags:
  - gsd
  - ingest
  - conflict-report
kind: "ingest-report"
status: resolved
---
## Conflict Detection Report

Generated by `gsd-doc-synthesizer` from 7 classified docs on 2026-05-14.
Precedence applied: ADR > SPEC > PRD > DOC.

### BLOCKERS (0)

No blockers. Synthesis is safe to route to the roadmapper.

- No LOCKED-vs-LOCKED ADR contradictions (the ingest contains exactly one LOCKED ADR: `docs/INTEGRATION-CONTRACT.md`).
- No UNKNOWN-low-confidence classifications.
- No cross-ref cycles that prevent per-doc content extraction (see INFO #1 below for benign sibling refs).
- No existing `.planning/` context to conflict against (mode = `new`).

### WARNINGS (0)

No competing acceptance variants. The two SPEC documents in this ingest (`2026-05-14-geo-phase-a-design.md` and `2026-05-14-geo-phase-a.md`) describe the **same** GEO Phase A milestone at design-spec and implementation-plan granularity. They agree on every concrete value — file paths, exact contents of `robots.txt` / `sitemap.xml` / `llms.txt`, JSON-LD shape, OG meta-tag block, header values, preconnect placement. Treated as complementary, not competing — collapsed into one requirement (`REQ-geo-phase-a`) with 8 sub-tasks in `intel/requirements.md`. Both source paths preserved on the parent and on each sub-task.

### INFO (4)

[INFO] Benign sibling cross-references between GEO Phase A docs
  Found: .planning/phases/01-static-geo-foundations/1-SPEC.md cross-refs .planning/phases/01-static-geo-foundations/1-PLAN.md, and vice-versa. .planning/research/GEO-AUDIT.md cross-refs both of them and is cross-ref'd back.
  Note: A strict DFS three-color marker would flag these as cycles, but they are bidirectional sibling links between (audit ↔ design-spec ↔ implementation-plan), not content-derivation cycles. Each document is self-contained — content extraction is per-doc, not transitive, so no synthesis loop occurs.
  → No action. Recorded for transparency.

[INFO] Auto-resolved: SPEC > DOC on robots.txt disallow set for /pay
  Found: .planning/phases/01-static-geo-foundations/1-SPEC.md §Scope-decisions chooses to keep /pay open (Disallow only /account, /success, /dashboard/) with rationale "it is a marketing/pricing surface."
  Found: docs/SEO-AUDIT.md §"Critical gaps" #18 recommends "transactional pages (/account, /dashboard/*, /pay, /success) probably should NOT be indexed (or at least not crawled)" — i.e. include /pay in the disallow set.
  → SPEC wins by precedence (SPEC=1 > DOC=3). /pay remains crawlable. Recorded in intel/constraints.md as CON-robots-disallow-set with rationale.

[INFO] Auto-resolved: SPEC > DOC on sitemap.xml route list
  Found: .planning/phases/01-static-geo-foundations/1-SPEC.md §Components-#2 lists exactly six URLs in sitemap.xml (/, /pay, /welcome, /privacy, /terms, /refund) — deliberately omits /account, /success, /dashboard/*.
  Found: .planning/research/GEO-AUDIT.md C-2 recommends "list canonical URLs of the 8 routes" and "list 8 canonical routes" in the 30-day plan.
  → SPEC wins by precedence (SPEC=1 > DOC=3). The omitted three routes are correctly excluded — they are also Disallow'd in robots.txt, so listing them in sitemap.xml would be contradictory. Recorded in intel/constraints.md as CON-sitemap-six-routes.

[INFO] Two SPECs collapsed into one requirement
  Found: .planning/phases/01-static-geo-foundations/1-SPEC.md (design spec) and .planning/phases/01-static-geo-foundations/1-PLAN.md (implementation plan) are two artifacts for the same milestone.
  Note: They are NOT competing variants — the plan explicitly cites the design as its source. They agree on every concrete value. Collapsed into REQ-geo-phase-a with 8 atomic sub-tasks. Both source paths preserved on every entry.
  → No action. Recorded for transparency.
