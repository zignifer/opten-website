---
phase: 04.2-seo-geo-polish-post-synthesis
plan: 01
type: summary
wave: 1
status: complete
date: 2026-05-17
requirements: [P0-1, P0-5, P1-3, P1-7]
risk: LOW
commit: de0ce11
---

# Phase 4.2 / Plan 01 — Summary

Closed 4 low-risk findings from the 2026-05-17 dual Codex review synthesis (S-1, S-5, A-4, A-6) in one parallel sweep. No extension-coupled surfaces touched.

## Tasks completed

### Task 1 — Model-count sweep (`43+` → `60+`) — P0-1 / S-1

Replaced every `43+` with `60+` across all source + build-pipeline drift points:

- `scripts/seo-routes.ts` — 5 hits (lines 223, 360, 363, 643, 646: productBlock description + RU `/` description/ogDescription + EN `/en/` description/ogDescription)
- `src/i18n/ru.json:26` — `hero.badge` string
- `src/i18n/en.json:26` — `hero.badge` string
- `index.html` — 3 hits (lines 8, 17, 28: meta description + og:description + twitter:description)
- `scripts/llms.mjs:28` — `SUMMARY` constant feeding `dist/llms.txt` (NOT in plan's enumerated 10-hit list, but the plan's `grep -rcE "43\+" dist/` verify gate would otherwise have failed because llms.mjs emits to `dist/llms.txt`; deviation Rule 1/3 — see "Deviations" below)

Total: 11 source-line edits across 6 files. After edits, `grep -rcE "43\+" dist/ scripts/seo-routes.ts src/i18n/ru.json src/i18n/en.json index.html scripts/llms.mjs` → 0 hits.

### Task 2 — Remove `aggregateRating` from SoftwareApplication + Product schemas — P0-5 / S-5

Deleted the hardcoded `aggregateRating: { ratingValue: "5", reviewCount: "2", … }` block from both schema factories in `scripts/seo-routes.ts`:

- `SOFTWARE_APP_BLOCK` (formerly lines 125–131)
- `productBlock(...)` product object (formerly lines 230–236)

Each deletion site now carries a one-line rationale comment so future maintainers do not "fix" the absence by re-adding the placeholder values:

```
// Phase 4.2 P0-5 / CONTEXT D-4: aggregateRating removed — hardcoded 5.0/2 did not match live Chrome Web Store. Restore when ≥10 honest reviews accumulate (Phase 5).
```

Also trimmed two pre-existing comments that still referenced aggregateRating as "the strongest trust signal" / "the strongest social-proof signal" — those comments would have encouraged a future re-add. (Deviation Rule 1, see below.)

`offers` arrays preserved untouched in both blocks. `PERSON_FOUNDER_BLOCK.image` left alone (deferred per CONTEXT P1-6).

### Task 3 — Documentation sweep + hreflang policy paragraph — P1-7 / A-4 + P1-3 / A-6

Three small text edits aligning project docs with current reality (16 routes / 8 EN siblings after Phase 4.1):

- `CLAUDE.md` — Architecture diagram block updated:
  - `14 client routes: 9 RU + 6 EN siblings` → `16 client routes: 9 RU (… /about, /dashboard/download-skill, /guides/:slug) + 8 EN siblings (… /en/about, /en/guides/:slug)`
  - `12 dist/**/index.html files (6 RU + 6 EN, …)` → `16 dist/**/index.html files (8 RU + 8 EN, …)`
- `docs/ARCHITECTURE.md`:
  - `12 prerendered HTML files: 6 RU canonical (…) and 6 EN siblings (…)` → `16 prerendered HTML files: 8 RU canonical (/, /pay, /welcome, /privacy, /terms, /refund, /about, /guides/gpt-image-2) and 8 EN siblings (...)`
  - NEW paragraph documenting the deliberate hreflang locale policy (language-only `ru`/`en` in hreflang vs region-specific `ru-RU`/`en-US` in schema `inLanguage`)
- `src/i18n/paths.ts:2` header comment — `EN_SIBLINGS MUST match the 6 EN entries` → `8 EN entries`

No code paths in `applyHreflang` / `sitemap.mjs` were modified — hreflang change is DOCUMENTATION-ONLY as scoped.

## Files touched (8 total)

| File | Kind | Lines changed |
|---|---|---|
| `scripts/seo-routes.ts` | source | 5×text + 2×schema deletions + 2×comment trims = ~9 logical edits |
| `src/i18n/ru.json` | i18n | 1 |
| `src/i18n/en.json` | i18n | 1 |
| `index.html` | template | 3 |
| `scripts/llms.mjs` | build pipeline | 1 (deviation — see below) |
| `CLAUDE.md` | docs | route-count block (~10 lines reformatted) |
| `docs/ARCHITECTURE.md` | docs | 1×count line + 1×new paragraph |
| `src/i18n/paths.ts` | i18n | 1 (header comment only) |

## Verify outputs (all green)

```
--- 43+ in source+dist ---
ZERO HITS

--- aggregateRating in dist (must be 0) ---
ZERO HITS

--- 60+ in dist/index.html ---
dist/index.html:6
dist/en/index.html:6

--- Prerendered files count ---
16
```

`npm run build` final pass:
- 16 routes prerendered (8 RU + 8 EN)
- sitemap.xml → 16 routes, per-route lastmod via git mtime
- llms.txt → 16 routes, 1.6 KB
- llms-full.txt → 42.1 KB
- FAQ-parity gate: 4 routes checked, 20 questions, all visible↔schema parity holds

Docs sweep verification:
- `grep -cE "16 client routes|16 prerendered|8 EN entries|8 EN siblings" CLAUDE.md docs/ARCHITECTURE.md src/i18n/paths.ts` → `CLAUDE.md:2, docs/ARCHITECTURE.md:1, src/i18n/paths.ts:1` (total 4, matches plan expectation ≥4)
- `grep -c "language-only" docs/ARCHITECTURE.md` → 1 (new hreflang policy paragraph present)

## Deviations from plan

### Rule 1/3 — Added `scripts/llms.mjs:28` to the `43+` sweep

**Found during:** Task 1 verification grep
**Issue:** The plan enumerates 10 known `43+` hits (5 in `scripts/seo-routes.ts`, 2 in i18n, 3 in `index.html`) but `scripts/llms.mjs:28` (the `SUMMARY` constant that ships into `dist/llms.txt`) also contains `43+ image-generation models`. The plan's own verify gate is `grep -rcE "43\+" dist/ ...` → 0, which would have failed (dist/llms.txt still carries the old value).
**Fix:** Updated `43+` → `60+` in `scripts/llms.mjs:28`. Same canonical-value substitution as the other 10 hits.
**Files modified:** `scripts/llms.mjs`
**Rationale:** Plan instructs ("If new hits appear (e.g. in a file the synthesis missed), update those too — the canonical-number rule is exhaustive, not partial"). This is the canonical handling.

### Rule 1 — Trimmed two pre-existing stale comments in `scripts/seo-routes.ts`

**Found during:** Task 2
**Issue:** After deleting `aggregateRating` blocks, two surrounding comments still described `aggregateRating` as "the strongest trust signal" (line 114, SoftwareApp factory) and "the single strongest social-proof signal for AI citation of pricing pages" (line 218, productBlock factory). These contradict the deletion rationale comments inserted at the actual deletion sites and would encourage a future maintainer to re-add the hardcoded placeholder values.
**Fix:** Trimmed the references to `aggregateRating` from those two non-deletion-site comments while preserving the still-accurate parts (Google requires `image`, softwareVersion sync source).
**Files modified:** `scripts/seo-routes.ts` (comments only)
**Rationale:** Pre-existing comments that misrepresent reality become traps; minimal cleanup directly tied to the Task 2 deletion.

### Out-of-scope `43+` hits intentionally NOT touched

`src/imports/{LandingPage,Frame51,Frame2}/*.tsx` (Figma-Make auto-generated SVG components) contain `43+ моделями` strings but those component files are NOT imported into the live React tree (only their `.svg` and `.webp` sibling assets are imported by `PayPage.tsx` / `AccountPage.tsx`). Therefore they do NOT appear in `dist/` HTML and do NOT trigger the plan's verify gate. Per CLAUDE.md convention these `imports/` files are "auto-generated; brittle" — leaving them out of scope matches both the SCOPE BOUNDARY rule (only auto-fix issues directly caused by current task changes) and Figma-Make regeneration safety (changes here are overwritten on next sync).

## Locked invariants preserved

- ✅ `EXTENSION_IDS` — not touched
- ✅ `SUPABASE_URL`, `SUPABASE_ANON_KEY` — not touched
- ✅ Locked routes (`/welcome`, `/pay`, `/success`) — paths unchanged
- ✅ `PERSON_FOUNDER_BLOCK.image` — still commented out (deferred per CONTEXT P1-6)
- ✅ No CSP header added (deferred per CONTEXT D-3)
- ✅ Definitional paragraph in `src/app/App.tsx` — not touched (D-1 footer placement stays)
- ✅ `applyHreflang` in `scripts/prerender.mjs` and `xhtml:link` in `scripts/sitemap.mjs` — unchanged (hreflang change is docs-only)
- ✅ `offers` arrays on both SoftwareApplication + Product schemas — preserved intact

## Anomalies

None. Build clean, FAQ-parity green, all 16 routes prerender, all greps return expected counts.

## Commit

Single atomic commit per plan template. SHA: `de0ce11`.

Commit subject: `chore(04.2-01): canonical model count 60+ + drop aggregateRating + docs sweep + hreflang policy (P0-1, P0-5, P1-3, P1-7)`

## Self-Check: PASSED

- `scripts/seo-routes.ts` exists and contains updated content (verified by Read after Edit)
- `scripts/llms.mjs:28` updated (verified by Read after Edit)
- `src/i18n/{ru,en}.json:26` updated (verified by Read after Edit)
- `index.html` lines 8/17/28 updated (verified by Read after Edit)
- `CLAUDE.md` route-count block updated (verified by Read after Edit)
- `docs/ARCHITECTURE.md` 16-prerendered line + hreflang policy paragraph added (verified by grep)
- `src/i18n/paths.ts:2` updated (verified by Read after Edit)
- Build exit 0, 16 routes prerendered (verified by `npm run build` final pass)
- FAQ-parity gate green (verified by build output)
- Plan's `grep -rcE "43\+" ...` = 0 (verified)
- Plan's `grep -c "aggregateRating" dist/**` = 0 (verified)
- Plan's `grep -cE "16 client routes|16 prerendered|8 EN entries|8 EN siblings" ...` ≥ 4 (got 4, verified)
- Plan's `grep -c "language-only" docs/ARCHITECTURE.md` ≥ 1 (got 1, verified)
