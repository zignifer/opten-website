---
phase: 04.2-seo-geo-polish-post-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seo-routes.ts
  - src/i18n/ru.json
  - src/i18n/en.json
  - index.html
  - CLAUDE.md
  - docs/ARCHITECTURE.md
  - src/i18n/paths.ts
autonomous: true
requirements: [P0-1, P0-5, P1-3, P1-7]
risk: LOW
approval_gate: false
must_haves:
  truths:
    - "Every visible/schema mention of model count says `60+` (or bare `60`) — `43+` is fully purged from source + dist."
    - "`SoftwareApplication` schema no longer carries an `aggregateRating` block."
    - "`Product` schema (productBlock builder) no longer carries an `aggregateRating` block."
    - "Documentation reflects current 16 routes / 8 EN siblings + a written hreflang locale policy."
  artifacts:
    - path: "scripts/seo-routes.ts"
      provides: "Per-route metadata + schema blocks with the canonical `60+` model count and no aggregateRating fields"
    - path: "src/i18n/ru.json"
      provides: "hero.badge string updated to 60+"
    - path: "src/i18n/en.json"
      provides: "hero.badge string updated to 60+"
    - path: "index.html"
      provides: "Default <meta description> + og:description + twitter:description say 60+"
    - path: "CLAUDE.md"
      provides: "Route counts state 16 client routes / 8 EN siblings"
    - path: "docs/ARCHITECTURE.md"
      provides: "Bilingual URLs section says 16 prerendered with 8 EN siblings + hreflang policy paragraph"
    - path: "src/i18n/paths.ts"
      provides: "Header comment says 8 EN entries"
  key_links:
    - from: "scripts/seo-routes.ts"
      to: "scripts/prerender.mjs"
      via: "RouteMeta.description / RouteMeta.ogDescription / RouteMeta.schema — values flow into prerendered <head> and JSON-LD"
      pattern: "60\\+|60(?!\\d)"
---

# Phase 4.2 / Plan 01 — Safe text + schema sweeps (model count, aggregateRating, docs)

<objective>
Close 4 low-risk text/schema findings from the synthesis in one parallel
sweep. None of these touch extension-coupled surfaces, routing, or
build-pipeline shape — they are pure value replacements and a documentation
catch-up.

Purpose: drop the AI-confidence-eroding contradiction between "43+" body
text and "60+" schema (S-1 in synthesis), prevent Google Rich Results
warnings on the `aggregateRating` block that does not match the live CWS
listing (S-5), and align project docs with reality post-Phase-4.1 (A-4).

Output: 6 modified files, 1 atomic commit.
</objective>

<context>
@.planning/phases/04.2-seo-geo-polish-post-synthesis/04.2-CONTEXT.md
@.planning/research/SEO-REVIEW-SYNTHESIS-2026-05-17.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model-count sweep — `43+` → `60+` everywhere</name>
  <files>scripts/seo-routes.ts, src/i18n/ru.json, src/i18n/en.json, index.html</files>
  <action>
    Replace every `43+` with `60+` in the 7 known drift points (5 in
    `scripts/seo-routes.ts`, 1 each in `src/i18n/{ru,en}.json`) plus
    `index.html` (3 hits: `<meta name="description">`, `<meta property="og:description">`,
    `<meta name="twitter:description">` — currently say "43+ моделями"). Per
    CONTEXT D-2 the canonical value is `60+` (matches 60 skill files in
    `promptscore-proxy/skills/` + Organization + SoftwareApplication +
    About copy).

    Exact known hits (verify with `grep -rn "43+" scripts/seo-routes.ts src/content src/i18n index.html` before editing):
    - `scripts/seo-routes.ts:223` — productBlock description: `"… for 43+ image generation models."` → `"… for 60+ image generation models."`
    - `scripts/seo-routes.ts:360` — RU `/` description: `"… с 43+ моделями …"` → `"… с 60+ моделями …"`
    - `scripts/seo-routes.ts:363` — RU `/` ogDescription: `"… для 43+ моделей …"` → `"… для 60+ моделей …"`
    - `scripts/seo-routes.ts:643` — EN `/en/` description: `"… with 43+ models …"` → `"… with 60+ models …"`
    - `scripts/seo-routes.ts:646` — EN `/en/` ogDescription: `"… for 43+ image generation models …"` → `"… for 60+ image generation models …"`
    - `src/i18n/ru.json:26` — `"hero.badge": "Работает с 43+ моделями генерации"` → `"Работает с 60+ моделями генерации"`
    - `src/i18n/en.json:26` — `"hero.badge": "Works with 43+ generation models"` → `"Works with 60+ generation models"`
    - `index.html:8` — `<meta name="description" content="… с 43+ моделями …">` → `60+`
    - `index.html:17` — `<meta property="og:description" content="… для 43+ моделей …">` → `60+`
    - `index.html:28` — `<meta name="twitter:description" content="… для 43+ моделей …">` → `60+`

    After editing, re-run the grep — it MUST return zero hits. If new hits
    appear (e.g. in a file the synthesis missed), update those too — the
    canonical-number rule is exhaustive, not partial.

    Per D-2, do NOT use bare `60` where the existing copy uses the `N+`
    pattern — keep the `+` so the phrasing reads identically before/after.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -rcE "43\+" dist/ scripts/seo-routes.ts src/i18n/ru.json src/i18n/en.json index.html</automated>
    Expected: build exits 0; grep total = 0 across all listed paths.
    Also spot-check: `grep -c "60+" dist/index.html` ≥ 3 (description + og + twitter).
  </verify>
  <done>Zero `43+` hits in source or dist; `60+` appears in every previously-drifted location; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove aggregateRating from SoftwareApplication + Product schemas</name>
  <files>scripts/seo-routes.ts</files>
  <action>
    Per CONTEXT D-4: delete the `aggregateRating` block from BOTH schema
    factories because hardcoded 5.0 / 2 reviews does not match the live
    Chrome Web Store listing and would trigger structured-data warnings in
    GSC. Restore when CWS accumulates 10+ honest reviews (tracked
    out-of-band in Phase 5 brand-authority work).

    Two deletions:
    1. `scripts/seo-routes.ts:125-131` — remove the `aggregateRating: { … }`
       property from `SOFTWARE_APP_BLOCK` (entire block, including the
       trailing comma). Leave `offers: [...]` and all other fields intact.
    2. `scripts/seo-routes.ts:230-236` — remove the `aggregateRating: { … }`
       property from the `product` object inside `productBlock(...)`. Leave
       `name`, `description`, `image`, `brand`, `isRelatedTo` intact.

    Add a one-line comment at each deletion site explaining the rationale
    so future maintainers do not "fix" the absence by re-adding hardcoded
    placeholder values. Suggested comment (both sites):
    `// Phase 4.2 P0-5 / CONTEXT D-4: aggregateRating removed — hardcoded 5.0/2 did not match live Chrome Web Store. Restore when ≥10 honest reviews accumulate (Phase 5).`

    Do NOT touch the `offers` array on either block. Do NOT touch
    `PERSON_FOUNDER_BLOCK.image` (commented-out, deferred per CONTEXT P1-6).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -c "aggregateRating" dist/index.html dist/en/index.html dist/pay/index.html dist/en/pay/index.html</automated>
    Expected: build exits 0; grep returns 0 on EVERY listed file (4×0).
    Also: `grep -c "AggregateRating" scripts/seo-routes.ts` should return 0.
  </verify>
  <done>Build clean. No `aggregateRating` or `AggregateRating` token in source or in any prerendered HTML. SoftwareApplication + Product schemas still validate as JSON (`offers` array preserved).</done>
</task>

<task type="auto">
  <name>Task 3: Documentation sweep + hreflang policy paragraph (P1-7 + P1-3)</name>
  <files>CLAUDE.md, docs/ARCHITECTURE.md, src/i18n/paths.ts</files>
  <action>
    Three small text edits aligning project docs with current reality
    (16 routes / 8 EN siblings after Phase 4.1) plus one paragraph capturing
    the deliberate hreflang locale policy (P1-3).

    1. **`CLAUDE.md`** — find the route-count line (around line 65 in the
       Architecture section, currently reads `"14 client routes: 9 RU … + 6 EN siblings"`).
       Update to `"16 client routes: 9 RU (/, /pay, /success, /account, /welcome, /privacy, /terms, /refund, /about, /dashboard/download-skill, /guides/:slug) + 8 EN siblings (/en/, /en/pay, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/about, /en/guides/:slug) — Phase 4.1 B-03 added /en/about; Phase 4 D-06 added bilingual /guides/:slug"`.
       Then also update the comment `12 dist/**/index.html files (6 RU + 6 EN, …)` → `16 dist/**/index.html files (8 RU + 8 EN, …)`. Use the actual file as source of truth — match exact phrasing pattern so existing context (`with hreflang triplets + per-page <html lang>`) stays intact.

    2. **`docs/ARCHITECTURE.md`** — line ~102 currently says `"12 prerendered HTML files: 6 RU canonical (…) and 6 EN siblings (…)"`. Update to `"16 prerendered HTML files: 8 RU canonical (/, /pay, /welcome, /privacy, /terms, /refund, /about, /guides/gpt-image-2) and 8 EN siblings (/en/, /en/pay, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/about, /en/guides/gpt-image-2)"`.

       Then INSERT a new paragraph after the `<html lang>` baked-per-route bullet, capturing the P1-3 hreflang policy decision (per CONTEXT "Hreflang Policy (documented, no code change)"):
       > **Hreflang locale code policy:** hreflang annotations use language-only codes (`ru`, `en`, `x-default`) in both `scripts/prerender.mjs#applyHreflang` and `scripts/sitemap.mjs` xhtml:link entries, while schema-level `inLanguage` stays region-specific (`ru-RU`, `en-US`) in `scripts/seo-routes.ts`. Both are valid per Google's documentation; the mix is intentional — language-only hreflang targets the broadest audience (RU readers globally, not just RU-from-Russia), while schema-level region tags give AI systems a precise origin signal for the content. Do not "unify" these — the asymmetry is the policy.

    3. **`src/i18n/paths.ts:1-3`** — the header comment currently says
       `"single source of truth for EN-sibling rewrites. SYNC: EN_SIBLINGS MUST match the 6 EN entries in scripts/seo-routes.ts."` Update `6 EN entries` → `8 EN entries` (matches the actual Set size — 8 elements: `/`, `/welcome`, `/pay`, `/privacy`, `/terms`, `/refund`, `/about`, `/guides/gpt-image-2`).

    Do not touch any code in `src/i18n/paths.ts` — comment-only edit. Do
    not touch any code paths in `applyHreflang` or `sitemap.mjs` — this is
    a DOCUMENTATION-ONLY confirmation of existing behaviour.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -cE "16 client routes|16 prerendered|8 EN entries|8 EN siblings" CLAUDE.md docs/ARCHITECTURE.md src/i18n/paths.ts</automated>
    Expected: build exits 0; grep total ≥ 4 (at least one match per phrase across the three files).
    Also: `grep -c "language-only" docs/ARCHITECTURE.md` ≥ 1 (the new hreflang policy paragraph).
  </verify>
  <done>CLAUDE.md route counts say 16 / 8 EN; docs/ARCHITECTURE.md says 16 prerendered + has the hreflang policy paragraph; src/i18n/paths.ts header says 8 EN entries. No code paths changed.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `npm run build` → exit 0; FAQ parity gate green ("4 routes checked, 20 questions, all visible↔schema parity holds"); sitemap emits 16 routes.
2. `grep -rcE "43\+" dist/ src/i18n index.html scripts/seo-routes.ts` → total 0.
3. `grep -c "aggregateRating" dist/**/index.html` → 0 on every file.
4. Spot-check `dist/index.html` for the canonical `60+` value (≥ 3 occurrences across description / og / twitter).
5. Manual diff: `CLAUDE.md` and `docs/ARCHITECTURE.md` say 16 routes / 8 EN siblings; new hreflang policy paragraph is present in ARCHITECTURE.md.
</verification>

<success_criteria>
- Closes synthesis items S-1 (P0-1 model count), S-5 (P0-5 aggregateRating), A-4 (P1-7 docs sweep), A-6 (P1-3 hreflang policy doc).
- Zero `43+` in repo or build output.
- Zero `aggregateRating` in source schema blocks or any prerendered HTML.
- All three doc files reflect 16 routes / 8 EN siblings.
- Hreflang policy paragraph committed to `docs/ARCHITECTURE.md`.
- No regression to extension-coupled invariants (EXTENSION_IDS, SUPABASE_*, locked routes, Paddle CDN injection).
</success_criteria>

<atomic_commit>
Single commit (one logical change per Phase 4.1 + morning-batch convention):

```
chore(04.2): canonical model count 60+ + drop aggregateRating + docs sweep + hreflang policy (P0-1, P0-5, P1-3, P1-7)

Closes 4 low-risk findings from the 2026-05-17 dual Codex review synthesis:

- P0-1 (S-1): replaces every "43+" with "60+" across scripts/seo-routes.ts
  (5 hits), src/i18n/{ru,en}.json (1 hit each), and index.html default
  meta block (3 hits). Matches 60 skill files in promptscore-proxy/skills/.
- P0-5 (S-5): removes the aggregateRating block from SoftwareApplication
  and Product schemas — hardcoded 5.0/2 did not match the live Chrome Web
  Store listing and would trigger structured-data warnings in GSC. Restore
  when ≥10 honest reviews accumulate (Phase 5 brand-authority work).
- P1-7 (A-4): updates CLAUDE.md + docs/ARCHITECTURE.md + src/i18n/paths.ts
  header comment from "14 client routes / 6 EN siblings" / "12 prerendered"
  to the actual post-Phase-4.1 reality (16 client routes / 8 EN siblings /
  16 prerendered).
- P1-3 (A-6): documents the deliberate hreflang policy — language-only
  ("ru"/"en") in hreflang vs region-specific ("ru-RU"/"en-US") in schema
  inLanguage. No code change; the asymmetry IS the policy.

Out of scope: P1-2 CSP header (separate ticket per CONTEXT D-3), P1-6
founder image asset (user sub-task per CONTEXT deferred block).
```
</atomic_commit>

<output>
After completion, create `.planning/phases/04.2-seo-geo-polish-post-synthesis/04.2-01-SUMMARY.md`.
</output>
