---
phase: 04.2-seo-geo-polish-post-synthesis
plan: 06
status: complete
commit: 1f5099b
approval_gate: passed
---

# Phase 4.2 / Plan 06 — Visible `<h1>` on /pay + /welcome (W6 SUMMARY)

**Date:** 2026-05-17
**Wave:** 6 of 7 (extension-coupled, approval-gated)
**Commit:** `1f5099b` — `feat(04.2): visible <h1> on /pay + /welcome RU + EN (P0-3) [APPROVED]`
**Closes:** synthesis A-1 / P0-3
**Status:** complete + user-approved

## What shipped

| Route | Heading | Position |
|-------|---------|----------|
| /pay | `Тарифы Opten` | Above currency-toggle pill, inside pricing-section flex column |
| /en/pay | `Opten pricing` | Same component, EN locale via `t("pay.h1")` |
| /welcome | `Добро пожаловать в Opten` | Above carousel, inside main flex column |
| /en/welcome | `Welcome to Opten` | Same component, EN locale via `t("welcome.h1")` |

## Files touched (4, +10 lines total)

- `src/app/pages/PayPage.tsx` — +3 lines (h1 markup at line 421, before role="tablist" pill)
- `src/app/pages/WelcomePage.tsx` — +3 lines (h1 markup at line 54, before {/* Auto-slider */})
- `src/i18n/ru.json` — +2 keys (`pay.h1`, `welcome.h1`)
- `src/i18n/en.json` — +2 keys (`pay.h1`, `welcome.h1`)

## Verification (post-build, all green)

- `npm run build` → exit 0, 16 routes prerendered, FAQ-parity gate green
- `grep -c "<h1" dist/{pay,en/pay,welcome,en/welcome}/index.html` → 1 per route (exactly one H1)
- `grep -c "cdn.paddle.com" dist/{pay,en/pay}/index.html` → 2 each (Paddle CDN sync script intact)
- `grep -c '199|299' dist/pay/index.html` → 4 (pricing cards SSR'd)
- `grep -c "chromewebstore.google.com" dist/pay/index.html` → 2 (SSR Chrome Store CTA branch intact)
- `grep -c "EXTENSION_IDS" src/app/pages/PayPage.tsx` → 4 (unchanged)
- `grep -c 'welcome.slide' src/app/pages/WelcomePage.tsx` → 4 (Wave 4 per-slide alts intact)

## Extension-coupled invariants preserved

- Locked routes `/pay`, `/en/pay`, `/welcome`, `/en/welcome` paths unchanged
- Paddle synchronous CDN injection via `scripts/prerender.mjs#applyPaddleScript` untouched
- Pricing-card runtime-conditional CTA pattern (SSR Chrome Store anchor → hydration Pay button swap when extension detected) preserved
- `EXTENSION_IDS`, `SUPABASE_URL`, `SUPABASE_ANON_KEY` constants untouched (INTEGRATION-CONTRACT)
- Welcome carousel mechanics (rotating idx state, setInterval, cross-fade), step list, blinking arrow, inline `<style>` all unchanged

## Approval audit trail

- Plan file marked `autonomous: false` + `approval_gate: true` + `risk: HIGH`
- Orchestrator (Claude) applied diff, ran build, presented diff with file:line evidence to user via AskUserQuestion
- User selected "Approve — коммитим"
- Commit message tagged `[APPROVED]` and includes `User-approved before commit per feedback_autonomy_and_caution.md`

## Deviations

None. Plan executed exactly as written; copy + styling guidance applied verbatim.

## Next

W7 — `X-Robots-Tag` headers in vercel.json for `/account*`, `/success*`, `/dashboard/*`, `/api/*`. Also approval-gated (vercel.json change). After W7 ships → code review → verify-work → phase-close.
