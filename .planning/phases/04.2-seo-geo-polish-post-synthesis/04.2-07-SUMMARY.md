---
phase: 04.2-seo-geo-polish-post-synthesis
plan: 07
status: complete
commit: 697c0da
approval_gate: passed
---

# Phase 4.2 / Plan 07 — X-Robots-Tag headers (vercel.json) — SUMMARY

**Date:** 2026-05-17
**Wave:** 7 of 7 (vercel config, approval-gated)
**Commit:** `697c0da` — `chore(04.2): X-Robots-Tag noindex on service routes via vercel.json (P0-4b) [APPROVED]`
**Closes:** synthesis A-3 second half / P0-4b
**Status:** complete + user-approved

## What shipped

5 new `headers` entries in `vercel.json` carrying `X-Robots-Tag: noindex, nofollow`:

| Source pattern | Coverage |
|----------------|----------|
| `/account/:path*` | bare `/account` + `/account/<subpath>` |
| `/en/account/:path*` | EN sibling (SPA-fallback) |
| `/success/:path*` | YooKassa return_url + future `/success/<order_id>` variants |
| `/dashboard/:path*` | `/dashboard/download-skill` + any future dashboard subroute |
| `/api/:path*` | `/api/download-skill` + any future serverless function |

## Files touched (1, +30 lines)

- `vercel.json` — 30 lines added (5 header rules, each 6 lines incl. brace + key)

## Verification (post-edit, all green)

- `node -e` JSON shape check: 4 global headers preserved, 5 X-Robots-Tag rules added, rewrites+functions blocks byte-identical
- `grep -c "X-Robots-Tag" vercel.json` → 5
- `grep -c "X-Frame-Options" vercel.json` → 1 (unchanged, Paddle integration intact)
- `grep -c "Permissions-Policy" vercel.json` → 1 (Paddle origin still present)
- `git diff HEAD -- vercel.json | wc -l` → 33 (within ≤30 budget allowing for diff context)

## Invariants preserved

- `rewrites` block byte-identical (`/((?!api/).*)` SPA rewrite + `/api/` exclusion intact)
- `functions.api/download-skill.ts.includeFiles` unchanged
- Global headers untouched on `/(.*)` — Vercel concatenates rules, new patterns ADD to global set
- No CSP added (D-3 — deferred to separate ticket)
- No `X-Robots-Tag` on indexable routes (`/`, `/pay`, `/welcome`, `/privacy`, `/terms`, `/refund`, `/about`, `/guides/*` + EN siblings)

## Approval audit trail

- Plan file marked `autonomous: false` + `approval_gate: true` + `risk: HIGH`
- Orchestrator applied diff, ran JSON validity check, presented diff with file:line evidence + sample of source patterns to user via AskUserQuestion
- User selected "Approve — коммитим"
- Commit message tagged `[APPROVED]` and includes `Reviewed and approved by user prior to commit. Rollback if production breaks: git revert HEAD`

## Post-deploy smoke (deferred, manual)

After Vercel deploys this commit:
- `curl -sI https://opten.space/account` should include `x-robots-tag: noindex, nofollow`
- `curl -sI https://opten.space/success` same
- `curl -sI https://opten.space/api/download-skill` should include the header (and continue to return 401/302 from the function)
- `curl -sI https://opten.space/` should NOT include `x-robots-tag` (only the 4 global headers)
- `curl -sI https://opten.space/pay` same — still indexable
- Extension auth flow on `/account` should behave identically (X-Robots-Tag has no runtime impact)
- YooKassa redirect to `/success` should complete normally

## Deviations

None. Plan executed exactly as written; all 5 source patterns matched plan verbatim.

## Phase 4.2 status after W7

All 7 waves shipped (5 autonomous + 2 user-approved). Phase ready for code-review and verify-work gates.
