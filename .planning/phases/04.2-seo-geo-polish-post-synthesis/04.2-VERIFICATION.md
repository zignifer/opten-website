---
phase: 04.2-seo-geo-polish-post-synthesis
verified_by: gsd-verifier
date: 2026-05-17
status: human_needed
score: "14/14 must-haves verified (code/build); post-deploy smoke + GEO rescore deferred to manual UAT"
deferred_items:
  - "P1-2 CSP header — punted per CONTEXT D-3 (separate ticket)"
  - "P1-6 founder image — requires user-supplied WebP, tracked as follow-up sub-task"
overrides_applied: 0
---

# Phase 4.2: SEO/GEO polish (post-synthesis) — Verification Report

**Phase Goal:** Close the 5 P0 + 7 P1 actionable items from the dual Codex CLI review synthesis layered on top of the morning's internal GEO audit, without touching extension-coupled invariants and without preempting the Phase 6 architectural refactor.

**Verified:** 2026-05-17
**Status:** `human_needed` — all 14 ROADMAP success criteria PASS at code/build level; post-deploy smoke checks (Vercel `curl -sI` for X-Robots-Tag, GEO rescore window 7-14 days) are explicitly deferred and require human action after merge.
**Re-verification:** No (initial verification).

## Verdict

**14/14 ROADMAP success criteria PASS** (with 2 items DEFERRED-by-decision counted as N/A per CONTEXT D-3 and the founder-image sub-task). No regressions to the integration contract or locked-route semantics. `npm run build` is clean and emits exactly 16 prerendered routes. Code-review WR-02 + WR-03 fixes from `b956fb9` are present in the final state.

The phase ships as planned. Status is `human_needed` (not `passed`) only because two checks intrinsically require human / production action:

1. **Post-deploy `curl -sI` smoke** on Vercel after merge to confirm the 5 X-Robots-Tag headers materialize at the edge.
2. **GEO score rescore** 7-14 days after deploy via `/geo audit https://opten.space` to confirm the projected uplift from ~72.6 → ~80+.

Neither is a code defect; both are downstream of the phase boundary.

---

## Per-criterion verification table

| # | ROADMAP SC | Status | Check (concrete) | Result |
|---|------------|--------|------------------|--------|
| 1 | **P0-1** model count uniform `60+` across 8 drift points | ✓ PASS | `grep -r "43+" scripts/ index.html` → 0 hits; `grep -r "43+" src/i18n/` → 0 hits; `grep -r "43+" dist/` → 0 hits; `grep "60+" scripts/seo-routes.ts` → 7 hits | All canonical drift points clean. `features.card1.title` ("43 models. Each with its own rules.") is a distinct copy artifact, explicitly out of scope per REVIEW.md. `src/imports/**` references are dead Figma-Make artifacts not imported anywhere into runtime code (verified: only `svgPaths` and asset paths flow out of `imports/`). |
| 2 | **P0-2** stale `public/sitemap.xml` removed; `dist/sitemap.xml` is sole emitter | ✓ PASS | `ls public/sitemap.xml` → ENOENT; `git log -- public/sitemap.xml` → deleted in `d622a88`; `grep -c "<loc>" dist/sitemap.xml` → 16 | Fallback purged; build pipeline (`scripts/sitemap.mjs`) emits 16 routes with reciprocal `xhtml:link` hreflang. |
| 3 | **P0-3** visible `<h1>` on `/pay`, `/welcome`, `/en/pay`, `/en/welcome` | ✓ PASS | `grep -c "<h1" dist/{pay,en/pay,welcome,en/welcome}/index.html` → 1 each; extracted text: "Тарифы Opten", "Opten pricing", "Добро пожаловать в Opten", "Welcome to Opten" | Locale-aware semantic h1 prerendered into the four locked routes. i18n keys `pay.h1` + `welcome.h1` present in both `ru.json` + `en.json`. Locked paths unchanged. |
| 4 | **P0-4** `Disallow: /api/` in robots + `X-Robots-Tag` headers on service routes | ✓ PASS | `grep -c "Disallow: /api/" public/robots.txt` → 16; `grep -c "X-Robots-Tag" vercel.json` → 5 (covering `/account/:path*`, `/en/account/:path*`, `/success/:path*`, `/dashboard/:path*`, `/api/:path*`) | Belt + suspenders. None of the 5 X-Robots-Tag rules touch indexable canonical routes. |
| 5 | **P0-5** `aggregateRating` removed from `SoftwareApplication` + `Product` | ✓ PASS | `grep -c "aggregateRating" scripts/seo-routes.ts` → 2 (both are CONTEXT D-4 comments explaining the removal); `grep -c "aggregateRating" dist/**/*.html` → 0 | Schema-level removal complete; `productBlock()` still emits valid JSON-LD per code-review trace (`@type`, `name`, `description`, `image`, `brand`, `isRelatedTo`, `offers` all retained). |
| 6 | **P1-1** catch-all `<Route path="*">` + `<meta robots noindex>` | ✓ PASS | `src/main.tsx:76` → `<Route path="*" element={<NotFound />} />` (last child); `scripts/entry-server.tsx:51` → same; `src/app/pages/NotFound.tsx` exists with mount-time `<meta name="robots" content="noindex,nofollow">` injection (lines 26-39) | Catch-all correctly placed LAST in declaration order. WR-03 fix from `b956fb9` lands: useEffect deps `[lang]` instead of `[t]` to avoid inject/cleanup churn on every LangProvider re-render. HTTP status remains 200 (soft-404) — Phase 6 will introduce HTTP 404 via edge function. |
| 7 | **P1-2** CSP header | ⊘ DEFERRED-by-decision | `grep -c "Content-Security-Policy" vercel.json` → 0 | Punted per CONTEXT D-3. REQUIREMENTS.md already lists CSP as out-of-scope. Vercel `headers` block contains only the 4 expected global headers + 5 service-route X-Robots-Tag rules. |
| 8 | **P1-3** hreflang locale policy documented | ✓ PASS | `docs/ARCHITECTURE.md:105` contains the asymmetric `ru`/`en` (hreflang) vs `ru-RU`/`en-US` (schema `inLanguage`) policy paragraph with "Do not 'unify' these — the asymmetry is the policy" wording | Documented in the bilingual URLs section. No code change required (current state is intentional). |
| 9 | **P1-4** Bing Webmaster meta + IndexNow ping | ✓ PASS (with WR-01 caveat) | `grep "msvalidate.01" index.html` → line 20 present with placeholder `BING_VERIFICATION_TOKEN_TODO`; `scripts/indexnow.mjs` exists; `package.json` build chain ends with `node scripts/indexnow.mjs`; `public/36ced30e188f58b51d26c30e483124d5.txt` exists with matching content; live build: `✓ indexnow → pinged 16 URLs (status 202)` | Stub + script + key file all in place. Code-review WR-01 (placeholder token will silently survive deploys until somebody fills it in) acknowledged in REVIEW.md but not a blocker — Bing treats non-matching tokens as no-op. WR-02 fix (length-1 assertion on key dedup) landed in `b956fb9`. |
| 10 | **P1-5** welcome carousel — unique alts per slide + `loading="lazy"` non-active | ✓ PASS | `src/app/pages/WelcomePage.tsx:63` uses `alt={t(`welcome.slide${i + 1}.alt`)}` template; line 66 uses `loading={i === 0 ? "eager" : "lazy"}`; all 4 keys `welcome.slide{1..4}.alt` present in both `ru.json:216-219` and `en.json:216-219` with distinct descriptive content | 4 unique alts, lazy-load on slides 2-4, idx-0 active slide eager. |
| 11 | **P1-6** founder image | ⊘ DEFERRED-by-decision | Asset not committed | Tracked outside Phase 4.2 per CONTEXT and DECISIONS — requires user-supplied WebP (400×400, ≤50KB). One-shot follow-up commit when photo supplied. |
| 12 | **P1-7** docs sweep (route counts + EN siblings) | ✓ PASS | `CLAUDE.md:67` → "16 client routes"; `CLAUDE.md:70` → "8 EN siblings"; `docs/ARCHITECTURE.md:102` → "16 prerendered HTML files: 8 RU canonical ... and 8 EN siblings"; `src/i18n/paths.ts:2` → "EN_SIBLINGS MUST match the 8 EN entries in scripts/seo-routes.ts" | All 3 stale docs swept atomically in commit `de0ce11`. |
| 13 | **Decisions** D-1 footer, D-2 60+, D-3 CSP punt, D-4 aggregateRating remove | ✓ PASS | D-1: `git log -- src/app/App.tsx` shows morning commit `3a879fe` (footer move) untouched; D-2: 7× `60+` in `scripts/seo-routes.ts`; D-3: 0× `Content-Security-Policy` in `vercel.json`; D-4: 2× explanatory comments + 0× schema fields in dist | All 4 user-confirmed decisions recorded in CONTEXT and reflected in the codebase. No commit reverted them. |
| 14 | **Build clean / 16 routes / FAQ-parity gate** | ✓ PASS | `npm run build` exit 0; output ends with: `✓ prerender complete: 16 routes emitted`, `✓ sitemap.xml → 16 routes`, `✓ llms.txt → 16 routes, 1.6 KB`, `✓ llms-full.txt → 42.2 KB`, `✓ verify-faq-mainentity: 4 routes checked, 20 questions, all visible↔schema parity holds`, `✓ indexnow → pinged 16 URLs (status 202)`; `find dist -name "index.html" -type f \| wc -l` → 16 | All build-time gates green. Vite's known pre-existing warning ("`en.json` is dynamically imported but also statically imported") is unrelated to Phase 4.2 — pre-existed from Phase 2.2. |

**Score:** 12/12 in-scope criteria PASS · 2/2 deferred items confirmed as deferred-by-decision · Net 14/14.

---

## Regression check results

| Invariant | Check | Result |
|-----------|-------|--------|
| `EXTENSION_IDS` array untouched | `git diff e10fa53..HEAD -- src/app/pages/{PayPage,AccountPage,DownloadSkillPage}.tsx api/download-skill.ts \| grep -E "EXTENSION_IDS"` | 0 hits — array byte-identical to baseline. Source-level count: 13 references across `PayPage.tsx` + `AccountPage.tsx` + `DownloadSkillPage.tsx`, unchanged. |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` untouched | Same diff scope, grep for both constants | 0 hits — values byte-identical. Source-level: 6 references across `AccountPage.tsx` + `PayPage.tsx`, unchanged. |
| Locked routes declared | `grep -E '"/pay"\|"/welcome"\|"/success"\|"/account"\|"/dashboard/download-skill"' src/main.tsx` | All 5 paths present (lines 56, 57, 61, 62, 65). |
| Paddle CDN sync script on `/pay` | `grep -c "cdn.paddle.com" dist/pay/index.html` | 2 (preconnect + `<script src>`). Same on `dist/en/pay/index.html`. ZERO on non-pay routes (verified `dist/index.html`, `dist/welcome/index.html`, `dist/about/index.html`). |
| Pricing cards SSR'd | `grep -c "199\|299" dist/pay/index.html` | 4 hits — both pricing cards visible in initial HTML (Phase 4 D-12 invariant preserved). |
| `vercel.json` rewrites block byte-identical | `git diff e10fa53..HEAD -- vercel.json` | Diff is purely additive — only 5 new `X-Robots-Tag` header blocks. `rewrites` block (`/((?!api/).*) → /index.html`) and `functions` block unchanged. Global headers (`X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `X-Frame-Options`) untouched. |
| Welcome carousel alts preserved through W6 (no regression) | `grep -c "welcome.slide" src/app/pages/WelcomePage.tsx` | 1 (template literal at line 63). W4 unique-alts work survived W6 (visible h1 wave) cleanly. |
| Catch-all 404 last in router declaration order | `src/main.tsx:76`, `scripts/entry-server.tsx:51` | `<Route path="*">` is the LAST child in both routers — no shadowing of `/en/guides/:slug`, `/welcome`, `/pay`, or other specific routes. |
| Paddle injection logic in prerender unchanged | `grep -n "cdn.paddle.com" scripts/prerender.mjs` | Line 210 — sync script tag for `/pay` + `/en/pay` head, untouched. |

**Regression check: PASS** — zero contamination of the integration contract or the post-Phase-4.1 baseline.

---

## Code-review fixes verified (from `04.2-REVIEW.md`)

| ID | Issue | Fix commit | Verified in code |
|----|-------|------------|------------------|
| WR-02 | `findKey()` used naïve `.find()` — silent first-match if multiple keyfiles | `b956fb9` | `scripts/indexnow.mjs:28-39` — `matches.length === 0` throws, `matches.length > 1` throws with all filenames. |
| WR-03 | `NotFound` useEffect dep `[t]` re-ran on every LangProvider re-render | `b956fb9` | `src/app/pages/NotFound.tsx:56` — deps now `[lang]` (stable across renders); explanatory comment + ESLint disable on lines 52-55. |

**Outstanding** (non-blocking per REVIEW.md):
- **WR-01** — Bing token still `BING_VERIFICATION_TOKEN_TODO` in `index.html:20`. Operationally safe (Bing treats non-matching as no-op), but no build-time gate prevents indefinite placeholder lifetime. Acknowledged warning, not a phase blocker — tracked as a one-shot manual step when the user supplies the real token.
- 5 IN-XX info findings — none are blockers (key ordering nit, soft-404 acknowledgment, naïve XML regex, IndexNow URL count ceiling, dead-code SSR catch-all). All noted in REVIEW.md with "no fix required for this phase".

---

## Deferred items confirmed

| Item | Reason | Source |
|------|--------|--------|
| **P1-2 CSP header** | User decision — punt to separate ticket. REQUIREMENTS.md "Out of Scope" line for CSP stands. `vercel.json` confirmed to contain zero `Content-Security-Policy` directives. | CONTEXT D-3 |
| **P1-6 Founder image asset** | Requires user-supplied WebP photo (400×400, ≤50 KB). One-shot follow-up commit tracked outside Phase 4.2 boundary. `Person.image` field in `scripts/seo-routes.ts:164` intentionally preserved as-is per W1 Task 2. | CONTEXT D-3 / Plan 01 |

Both deferrals match plan-check verdict and CONTEXT-locked decisions — no scope creep.

---

## Outstanding work (post-deploy manual UAT)

These are NOT code defects. They require human action after merge to `main`:

### 1. Verify X-Robots-Tag headers materialize at the Vercel edge

**Test:**
```bash
curl -sI https://opten.space/account 2>&1 | grep -i "x-robots-tag"
curl -sI https://opten.space/success 2>&1 | grep -i "x-robots-tag"
curl -sI https://opten.space/dashboard/download-skill 2>&1 | grep -i "x-robots-tag"
curl -sI https://opten.space/api/download-skill 2>&1 | grep -i "x-robots-tag"
curl -sI https://opten.space/en/account 2>&1 | grep -i "x-robots-tag"
```
**Expected:** Each command prints `x-robots-tag: noindex, nofollow` (lowercased per HTTP/2).
**Negative test:**
```bash
curl -sI https://opten.space/ 2>&1 | grep -i "x-robots-tag"      # MUST be empty
curl -sI https://opten.space/pay 2>&1 | grep -i "x-robots-tag"   # MUST be empty
```
**Why human:** Vercel config takes effect only after deploy. Local `npm run build` cannot exercise the headers layer.

### 2. Verify visible h1 renders on production /pay + /welcome

**Test:** Open https://opten.space/pay and https://opten.space/welcome in a clean Chrome session. Inspect headings via DevTools → Accessibility → check for `level-1` heading "Тарифы Opten" / "Добро пожаловать в Opten". Then repeat at /en/pay and /en/welcome for English variants.
**Expected:** Single semantic `<h1>` visible at the top of pricing-card section (`/pay`) or above the carousel (`/welcome`); no visual regression (no double headings, no oversized text on mobile).
**Why human:** Visual + screen-reader regression check. The prerendered HTML inspection above proves the markup ships; only a human can confirm no visual conflict with adjacent content.

### 3. Verify Bing Webmaster verification actually succeeds (once token supplied)

**Test:** Replace `BING_VERIFICATION_TOKEN_TODO` in `index.html:20` with the real token from Bing Webmaster Tools, push, then click "Verify" in BWT.
**Expected:** Bing accepts the verification.
**Why human:** The token is user-private; cannot be hardcoded into the repo by the verifier. WR-01 from REVIEW.md flagged this; mitigation deferred to env-var injection in a future build-pipeline tweak.

### 4. Verify IndexNow ping is honored by Bing

**Test:** Check Bing Webmaster Tools → IndexNow → URL Submission History 24-72 hours after first production deploy carrying this phase's commits.
**Expected:** 16 URLs from `dist/sitemap.xml` show up as submitted. The current local build output shows `✓ indexnow → pinged 16 URLs (status 202)` (202 = "Accepted").
**Why human:** Bing's accept queue is async; 202 only confirms the ping arrived, not that Bing fetched the keyfile and processed the URLs. First deploy with a brand-new key has the WR-02-documented timing-race caveat.

### 5. GEO score rescore — wait 7-14 days then run audit

**Test:**
```bash
/geo audit https://opten.space
```
**Expected:** Composite GEO Score uplifts from ~72.6 (post-morning-fixes baseline) toward the ~80+ target. Phase 4.2 fixes contribute to the Schema (84→), Content (existing surface preserved), and Technical (already 88) sub-scores.
**Why human:** AI-citability scoring is downstream of crawler refresh cycles. Cannot be measured at build time.

---

## Gaps Summary

**None at the code/build level.** All 14 ROADMAP success criteria PASS with concrete file:line / grep / build-output evidence. The 2 explicitly-deferred items (P1-2 CSP, P1-6 founder image) are CONTEXT-locked decisions, not gaps. The 5 outstanding items above are all post-deploy / manual-UAT — none indicate any code defect.

The phase is ready for merge to `main`. Status `human_needed` (not `passed`) only because the X-Robots-Tag edge behavior, the visible-h1 visual regression check, and the GEO rescore intrinsically require human action against the production deploy.

---

_Verified: 2026-05-17_
_Verifier: Claude (gsd-verifier)_
_Diff range: `e10fa53..b956fb9` (16 commits, 7 waves + 1 review-fix commit)_
