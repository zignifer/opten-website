# Phase 4.2 Plan Check

**Date:** 2026-05-17
**Checker:** gsd-plan-checker
**Verdict:** APPROVED ✅
**Plans:** 7 atomic plans + 1 index across 7 waves
**Issues:** 0 blockers · 2 minor warnings (cosmetic) · 1 info (idempotency)

## Coverage — ROADMAP success criteria → wave

| # | Criterion | Wave | Status |
|---|-----------|------|--------|
| 1 | P0-1 canonical `60+` across 8 drift points | W1 | COVERED |
| 2 | P0-2 `public/sitemap.xml` removed | W2 | COVERED |
| 3 | P0-3 visible `<h1>` on /pay + /welcome (RU+EN) | W6 | COVERED (approval-gated) |
| 4 | P0-4 `Disallow: /api/` + `X-Robots-Tag` | W2 + W7 | COVERED (W7 approval-gated) |
| 5 | P0-5 aggregateRating removed | W1 | COVERED |
| 6 | P1-1 catch-all 404 + noindex | W3 | COVERED |
| 7 | P1-2 CSP header | — | OUT-OF-SCOPE per CONTEXT D-3 |
| 8 | P1-3 hreflang policy documented | W1 | COVERED (doc-only per decision) |
| 9 | P1-4 Bing Webmaster + IndexNow | W5 | COVERED |
| 10 | P1-5 welcome carousel alts + lazy | W4 | COVERED |
| 11 | P1-6 founder image | — | DEFERRED (user-supplied WebP) |
| 12 | P1-7 docs sweep | W1 | COVERED |
| 13 | Decisions D-1..D-4 recorded | — | RESOLVED in CONTEXT |
| 14 | Build clean / 16 routes / no regressions | All | COVERED by per-wave verify blocks |

11/11 in-scope items covered. 2 deferred items correctly excluded.

## Dependency check ✅

- W6 depends on W4 (shared file `src/app/pages/WelcomePage.tsx`) — ordering respected.
- W1–W5, W7 file-disjoint, can run in any order; sequential atomic-commit execution mirrors Phase 4.1 + morning batch convention.
- W6 + W7 carry `autonomous: false` + `approval_gate: true`, ordered last.

## Hard-constraint check ✅

- No CSP header anywhere (W7 explicitly disclaims).
- No `Person.image` work (W1 Task 2 explicitly preserves `PERSON_FOUNDER_BLOCK.image`).
- No architectural refactor — catch-all is React-Router-only.
- No EXTENSION_IDS / SUPABASE_* / locked-route renames touched.
- aggregateRating REMOVED (not "updated").
- Model count uniform 60+ everywhere.
- Hreflang doc-only (no `applyHreflang` / `sitemap.mjs` changes).
- Definitional paragraph footer placement preserved.

## Warnings (non-blocking)

- **W1 prose vs grep:** "7 known hits" claim overstates by 2; grep-based verify gate is exhaustive regardless. Cosmetic.
- **W1 grep precision:** `grep -c "60+" dist/index.html` may match unintended substrings; tighter regex would be cleaner. Acceptable — primary gate is `grep "43+" → 0`.

## Info

- **W5 IndexNow key idempotency:** `crypto.randomBytes(16)` regenerates key on every run. Verify gate (`m.length !== 1`) catches duplicates but a "reuse existing" preamble would be cleaner.

## Next steps

1. Execute W1–W5 sequentially as autonomous atomic commits.
2. **STOP for approval before W6** (extension-coupled `<h1>` on PayPage.tsx + WelcomePage.tsx).
3. **STOP for approval before W7** (vercel.json X-Robots-Tag headers).
4. After all 7 waves merge: phase-close verification against the 14 ROADMAP success criteria (sketch in `04.2-PLAN-INDEX.md`).
