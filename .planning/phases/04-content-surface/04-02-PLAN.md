---
phase: 04-content-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-content-surface/04-LCP-AUDIT.md
autonomous: false
requirements:
  - GEO-D-3
must_haves:
  truths:
    - "The actual mobile LCP element on production / and /en/ is identified by URL or text-node ID"
    - "Wave 2's hero preload implementation knows what asset to preload"
  artifacts:
    - path: ".planning/phases/04-content-surface/04-LCP-AUDIT.md"
      provides: "LCP-element audit log (mobile + desktop strategy) captured before D-13 implementation"
      min_lines: 20
  key_links:
    - from: "PageSpeed MCP run output"
      to: "Plan 04-03 applyHeroPreload regex"
      via: "the recorded LCP-element URL / type field"
      pattern: "lcp_element"
---

<objective>
Resolve RESEARCH Open Question Q1 / VALIDATION Wave-0 prerequisite: identify the actual mobile LCP element on production `https://opten.space/` and `https://opten.space/en/` BEFORE Wave-2 implements `applyHeroPreload`. Without this, D-13's `<link rel="preload" as="image">` may target the wrong asset and the LCP gate (V-18 / V-19) cannot be designed to pass.

Purpose: Replace the speculative "the hero is the LCP element" assumption with a measured fact. The LCP could be a text node (e.g. the H1), a webfont-rendered headline, or a different `<img>` than the hero — each case changes the Wave-2 preload target.
Output: A short audit file `.planning/phases/04-content-surface/04-LCP-AUDIT.md` listing LCP element URL (or "text node") + size + render-blocking dependencies for `/` and `/en/`, mobile strategy. This file is consumed by Plan 04-03 as input (referenced from its `<read_first>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-content-surface/04-CONTEXT.md
@.planning/phases/04-content-surface/04-RESEARCH.md
@.planning/phases/04-content-surface/04-VALIDATION.md
@.planning/codebase/TESTING.md

<interfaces>
PageSpeed Insights MCP exposes `mcp__pagespeed__*` tools (see USER_MEMORY `reference_pagespeed_mcp.md` — auto-loaded from `$HOME/.claude/projects/.../memory/MEMORY.md`). The LCP element identity surfaces in the Lighthouse JSON under `lighthouseResult.audits["largest-contentful-paint-element"].details.items[0]` — fields of interest: `node.snippet`, `node.selector`, `node.nodeLabel`.

For asset preload syntax, the target line emitted by `applyHeroPreload` in Wave 2 will look like:

`<link rel="preload" as="image" href="<LCP_ASSET_URL>" fetchpriority="high">`

The `href` value MUST come from this audit's output. If the LCP is a text node (e.g. the hero H1 rendered with PT Root UI), the preload target switches to the WOFF2 font file already self-hosted under `/assets/fonts/...` — and Plan 04-03 task action must be updated accordingly.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run PageSpeed MCP audit on production / and /en/ (mobile)</name>
  <files>.planning/phases/04-content-surface/04-LCP-AUDIT.md</files>
  <read_first>
    - .planning/phases/04-content-surface/04-RESEARCH.md (search for "Open Question Q1" + "LCP element")
    - .planning/phases/04-content-surface/04-VALIDATION.md "Wave 0 Requirements" + rows V-18, V-19
    - .planning/codebase/TESTING.md §"PageSpeed MCP workflow"
  </read_first>
  <what-built>
    The executor (Claude) invokes the PageSpeed Insights MCP tools (`mcp__pagespeed__*`) twice — once for `https://opten.space/` with `strategy=mobile`, once for `https://opten.space/en/` with `strategy=mobile`. For each run, extract from the Lighthouse JSON:
    - `audits["largest-contentful-paint-element"].details.items[0].node.snippet`
    - `audits["largest-contentful-paint-element"].details.items[0].node.selector`
    - Current LCP value in milliseconds
    - The first `<img>` href in the snippet IF it is an image LCP — else the text content
    - Render-blocking resources from `audits["render-blocking-resources"]` (for context on what else competes for LCP budget)

    Then write `.planning/phases/04-content-surface/04-LCP-AUDIT.md` with the structure:

    1. "Run metadata" — strategy, date, PageSpeed run IDs.
    2. "LCP on / (mobile)" — element type (img / text / video), if `img`: the URL; if `text`: the node selector + the webfont URL responsible for it.
    3. "LCP on /en/ (mobile)" — same shape.
    4. "Implication for Plan 04-03" — single recommended line that Plan 04-03 should hard-code into `applyHeroPreload`'s `href` AND a single recommended regex Plan 04-03 should use to detect that asset in the built HTML.
    5. "Render-blocking context" — list of CSS/JS that current page is waiting on (informational only).

    DO NOT modify any code in this task. The output is a planning artifact, not an implementation.

    If the PageSpeed MCP is unavailable or fails, fall back to `npx lighthouse https://opten.space/ --preset=desktop --form-factor=mobile --output=json` (Lighthouse CLI is npm-runnable; not in `package.json` devDependencies — use `npx`). Document the fallback in the audit file.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/04-content-surface/04-LCP-AUDIT.md`.
    2. Confirm the LCP element identity is named for BOTH `/` and `/en/` (e.g., `feature-models-w800.webp` OR `text: "Не сливай кредиты..." rendered with /assets/fonts/PTRootUI-Bold.woff2`).
    3. Confirm a recommended preload-target line for Plan 04-03 exists. Example expected lines:
       - If image LCP: `Plan 04-03 applyHeroPreload should preload: /assets/landing-design/feature-models-w800.webp`.
       - If text/font LCP: `Plan 04-03 applyHeroPreload should preload the WOFF2 hero font: /assets/fonts/PTRootUI-Bold.woff2` (`as="font" crossorigin` instead of `as="image"`).
    4. The "current LCP ms" number is recorded (used as the baseline against the 2500 ms target in V-18/V-19 post-deploy).
  </how-to-verify>
  <resume-signal>
    Type "approved — LCP audit recorded, Plan 04-03 has its preload target" once the file is reviewed and the recommended preload line is unambiguous. If the audit reveals the preload target should NOT be an image (e.g. LCP is a text node and the font is the real bottleneck), explicitly say so before approving — Plan 04-03's action wording will need a small edit.
  </resume-signal>
</task>

</tasks>

<verification>
  - `.planning/phases/04-content-surface/04-LCP-AUDIT.md` exists with `>= 20` lines.
  - The file is referenced from Plan 04-03's `<read_first>` block.
  - The recommended preload-target line is a single unambiguous URL or font path (NOT a hedge like "probably the hero").
</verification>

<success_criteria>
  1. LCP element identified on `/` AND `/en/` (mobile).
  2. Baseline LCP ms recorded for both URLs (the pre-Phase-4 number — used as the regression-floor for V-18 / V-19).
  3. Plan 04-03's hero-preload target is unambiguous and ready to copy into code.
  4. No source code modified in this plan.
</success_criteria>

<output>
  `.planning/phases/04-content-surface/04-LCP-AUDIT.md` is the output. No SUMMARY.md needed — the audit file IS the deliverable.
</output>
