---
phase: 04-content-surface
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - src/app/components/PricingStaticBlock.tsx
  - src/app/pages/PayPage.tsx
  - scripts/entry-server.tsx
  - src/main.tsx
  - scripts/seo-routes.ts
autonomous: false
requirements:
  - GEO-D-3
must_haves:
  truths:
    - "Visiting /pay or /en/pay returns initial HTML containing the pricing facts: 199, 2.99, 300 generations, and feature bullets"
    - "The synchronous <script src=cdn.paddle.com> tag still exists in /pay AND /en/pay (INTEGRATION-CONTRACT §6 invariant)"
    - "Clicking Pay opens the Paddle overlay (no regression vs Phase 3)"
    - "Product schema with offers[] (RUB + USD) appears in /pay AND /en/pay JSON-LD"
    - "PayPage is eager-imported in both main.tsx and entry-server.tsx (SSR-renderable, no React.lazy)"
  artifacts:
    - path: "src/app/components/PricingStaticBlock.tsx"
      provides: "SSR-safe pricing card component with no extension/runtime state"
      min_lines: 80
    - path: "src/app/pages/PayPage.tsx"
      provides: "PayPage now renders <PricingStaticBlock> as the static content layer; runtime extension/Paddle CTA layer remains on top"
      contains: "PricingStaticBlock"
    - path: "scripts/seo-routes.ts"
      provides: "/pay and /en/pay flipped to prerender: \"full\"; Product + Breadcrumb schema added"
      contains: "Product"
  key_links:
    - from: "scripts/entry-server.tsx"
      to: "PayPage"
      via: "eager import + <Route path=/pay> mount"
      pattern: "import PayPage"
    - from: "scripts/prerender.mjs apply-chain"
      to: "applyPaddleScript path gate"
      via: "meta.path === /pay || meta.path === /en/pay (line 192 — preserved)"
      pattern: "applyPaddleScript"
---

<objective>
Implement D-12: promote /pay and /en/pay from head-only prerender to full-prerender tier, so the pricing facts (plan names, prices, feature bullets) land in the initial HTML — the prerequisite for `Product` schema being acceptable to Google's Rich Results Test (per its "schema must reflect visible content" policy). Touches the billing surface, so the plan is isolated and gets explicit Paddle modal verification.

Purpose: Without static pricing copy, `Product` schema is policy-non-compliant and gets ignored. Without `Product` schema, the /pay route can't be cited as a pricing source by AI assistants — losing the most direct GEO conversion lever.
Output: A new `PricingStaticBlock` component extracted from `PayPage.tsx:434-528`, PayPage de-lazyed in `main.tsx`, eager-imported into `entry-server.tsx`, `prerender: "full"` on both pay routes in the manifest, and Product + Breadcrumb schema appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-content-surface/04-CONTEXT.md
@.planning/phases/04-content-surface/04-PATTERNS.md
@.planning/phases/04-content-surface/04-RESEARCH.md
@.planning/phases/04-content-surface/04-VALIDATION.md
@.planning/phases/04-content-surface/04-03-SUMMARY.md
@docs/INTEGRATION-CONTRACT.md
@src/app/pages/PayPage.tsx
@src/main.tsx
@scripts/entry-server.tsx
@scripts/seo-routes.ts
@scripts/prerender.mjs

<interfaces>
From `src/app/pages/PayPage.tsx` (existing — TO REFACTOR):

- Lines 10–17: hardcoded constants `SUPABASE_FUNCTIONS_URL`, `SUPABASE_ANON_KEY`, `CHROME_STORE_URL`, `EXTENSION_IDS` — UNCHANGED. INTEGRATION-CONTRACT §6.
- Lines 100–107: `PricingFeature` helper — moves with the extraction.
- Lines 109–111: `Divider` helper — same treatment.
- Lines 434–528: the two pricing cards JSX — extracted into `PricingStaticBlock`.
- Lines 179–205: extension-detection `useEffect` — stays in PayPage; SSR-safe because already wrapped in useEffect.

From `src/main.tsx` (existing):
- Line 18 `const PayPage = lazy(() => import(...))` — REMOVED.
- New eager import joins lines 5–9.

From `scripts/entry-server.tsx` (existing):
- Lines 9–13: eager imports — ADD PayPage.
- Lines 28–40: Routes block — ADD `<Route path="/pay">` and `<Route path="/en/pay">`. The comment at line 34 ("/pay AND /en/pay are head-only — intentionally NOT mounted here") MUST be updated to reflect the D-12 flip.

From `scripts/seo-routes.ts` (post-Plan-03):
- `/pay` entry: currently `prerender: "head"` and `schema: [ORG_BLOCK]`. FLIP to `prerender: "full"` and EXTEND `schema` to `[ORG_BLOCK, productBlock(...), breadcrumbBlock(...)]`.
- `/en/pay`: same.

From `scripts/prerender.mjs` (UNCHANGED in this plan):
- Line 192 Paddle gate is PATH-based and survives the `head`->`full` flip with no code change. Verification grep in `<verify>` confirms.

Pricing facts for productBlock arg:
- 199 RUB / month — "Pro Monthly RU"
- $2.99 USD / month — "Pro Monthly"
- $0 USD — "Free tier"
- 300 generations / month
- Five feature bullets — sourced from `src/i18n/{ru,en}.json` `pay.pro.feature*` keys.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PricingStaticBlock + de-lazy PayPage in routers</name>
  <files>src/app/components/PricingStaticBlock.tsx, src/app/pages/PayPage.tsx, src/main.tsx, scripts/entry-server.tsx</files>
  <read_first>
    - src/app/pages/PayPage.tsx (focus on lines 100–111 helpers and 434–528 pricing JSX)
    - src/main.tsx (the full file — 82 lines)
    - scripts/entry-server.tsx (the full file — 46 lines)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"src/app/components/PricingStaticBlock.tsx (NEW — D-12 extract)" + §"src/app/pages/PayPage.tsx (MODIFIED — extract PricingStaticBlock)" + §"src/main.tsx (MODIFIED — add new routes, de-lazy PayPage)" + §"scripts/entry-server.tsx (MODIFIED — eager-mount new routes)"
    - docs/INTEGRATION-CONTRACT.md §6 (Paddle SDK init invariant)
    - .planning/codebase/CONVENTIONS.md §"SSR / Prerender Constraints"
  </read_first>
  <action>
    Four-file refactor in one commit:

    (a) CREATE `src/app/components/PricingStaticBlock.tsx`. Lift lines 434–528 of `PayPage.tsx` verbatim. Also lift `PricingFeature` (lines 100–107) and `Divider` (lines 109–111) into the new file as local helpers (not separately exported). Component signature: `export default function PricingStaticBlock({ defaultCurrency }: { defaultCurrency: "RUB" | "USD" })`. Replace runtime-conditional CTA blocks (`extStatus === "ready" ? ...` ternaries inside the cards) with a STATIC fallback CTA — a Chrome-Web-Store anchor `<a href={CHROME_STORE_URL}>` with text from `t("pay.onetime.tryBtn")` / `t("pay.pro.tryBtn")`. The runtime extension-state CTA layer stays in `PayPage.tsx` and renders OUTSIDE (after) the `<PricingStaticBlock>` mount. SSR safety: NO `window`, `localStorage`, `chrome.runtime`, or `navigator` access at render time. `CHROME_STORE_URL` is duplicated locally as a small marketing-string constant — NOT one of the INTEGRATION-CONTRACT-synced extension constants (those stay in PayPage only).

    (b) MODIFY `src/app/pages/PayPage.tsx`. Replace the JSX subtree at lines 434–528 with `<PricingStaticBlock defaultCurrency={currency} />` where `currency` is the existing state. Remove the now-unused `PricingFeature` and `Divider` definitions. Keep all hardcoded extension/Supabase constants at lines 10–17 untouched. Runtime CTA buttons (currency toggle, Paddle.Checkout invocation, status messages, refund link) remain in PayPage as a layered section above or below the static block — preserve current visual order.

    (c) MODIFY `src/main.tsx`. Delete line 18 (`const PayPage = lazy(...)`). Add `import PayPage from "./app/pages/PayPage.tsx";` to the eager block between lines 9 and 10. Update the comment at lines 14–17 to: "Phase 4 D-12: PayPage is now eager (SSR-prerendered body) — lazy block covers only SuccessPage / AccountPage / DownloadSkillPage which remain SPA-only."

    (d) MODIFY `scripts/entry-server.tsx`. Add `import PayPage from "../src/app/pages/PayPage.tsx";` after the existing `RefundPage` import. Add `<Route path="/pay" element={<PayPage />} />` in the RU section (after `/refund` line) and `<Route path="/en/pay" element={<PayPage />} />` in the EN section (after `/en/refund` line). Update the line-34 comment to: "/success /account /dashboard/* are SPA-only — intentionally NOT mounted here. /pay AND /en/pay are NOW SSR-mounted per Phase 4 D-12."

    DO NOT touch `scripts/prerender.mjs` — the existing `applyPaddleScript` path-gate at line 192 already supports both pay routes.

    DO NOT yet flip `prerender: "head" -> "full"` in `seo-routes.ts` — that happens in Task 2 so Task 1 leaves the manifest at `head` (existing behavior) and the build remains green between tasks.
  </action>
  <verify>
    <automated>npm run build && test -f dist/pay/index.html && grep -q "cdn.paddle.com" dist/pay/index.html && grep -q "cdn.paddle.com" dist/en/pay/index.html && ! grep -q "PayPage = lazy" src/main.tsx</automated>
  </verify>
  <done>
    `src/app/components/PricingStaticBlock.tsx` exists with `defaultCurrency` prop and no SSR-unsafe browser API references. `PayPage.tsx` imports and renders it. `main.tsx` eager-imports PayPage (no `lazy()` reference). `entry-server.tsx` mounts `<Route path="/pay">` and `<Route path="/en/pay">`. `npm run build` succeeds. `dist/pay/` exists (head-tier still — body comes in Task 2) and contains the Paddle CDN script.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip /pay and /en/pay to prerender: full and add Product + Breadcrumb schema</name>
  <files>scripts/seo-routes.ts</files>
  <read_first>
    - scripts/seo-routes.ts (focus on the /pay entry at lines 52–68 and /en/pay entry at lines 157–174)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"scripts/seo-routes.ts" /pay D-12 promotion section
    - .planning/phases/04-content-surface/04-RESEARCH.md §"Phase 4 Schema Plan" (Product skeleton)
    - .planning/phases/04-content-surface/04-VALIDATION.md rows V-15, V-16, V-17
  </read_first>
  <action>
    In `scripts/seo-routes.ts`:

    (a) On the `/pay` entry: change `prerender: "head"` to `prerender: "full"`. Extend `schema:` from `[ORG_BLOCK]` (set in Plan 04-03) to `[ORG_BLOCK, productBlock([{name: "Pro Monthly (RU)", price: "199", priceCurrency: "RUB"}, {name: "Pro Monthly", price: "2.99", priceCurrency: "USD"}, {name: "Free tier", price: "0", priceCurrency: "USD"}], SITE_ORIGIN + "/pay"), breadcrumbBlock([{name: "Главная", url: SITE_ORIGIN + "/"}, {name: "Тарифы", url: SITE_ORIGIN + "/pay"}], SITE_ORIGIN + "/pay")]`. The `productBlock` and `breadcrumbBlock` helpers were defined in Plan 04-03 Task 1.

    (b) On the `/en/pay` entry: same `prerender: "full"` flip and same product schema (EN breadcrumb labels `[{name: "Home"}, {name: "Pricing"}]`).

    (c) Run `npm run build`. Verify the generated `dist/pay/index.html` and `dist/en/pay/index.html` contain BOTH:
    - Pricing strings from the SSR pass: regex `199` AND `2\.99` (one feature bullet match optional).
    - `cdn.paddle.com` script.
    - `"@type": "Product"` block.

    If `productBlock` or `breadcrumbBlock` are missing or have a different signature than expected, surface as a Plan-04-03 execution gap (do not patch the helper here — its signature is a Plan-04-03 contract).
  </action>
  <verify>
    <automated>npm run build && grep -q "Product" dist/pay/index.html && grep -q "Product" dist/en/pay/index.html && grep -q "RUB" dist/pay/index.html && grep -q "USD" dist/pay/index.html && grep -q "cdn.paddle.com" dist/pay/index.html && grep -q "cdn.paddle.com" dist/en/pay/index.html && grep -E "(199|2\.99|300)" dist/pay/index.html</automated>
  </verify>
  <done>
    `dist/pay/index.html` and `dist/en/pay/index.html` both contain `"@type": "Product"`, both prices (RUB + USD), the visible 199 / 2.99 / 300 strings, AND the Paddle CDN script. `npm run preview` followed by `curl -s http://localhost:4173/pay` matches the same patterns.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Paddle modal regression check via Playwright MCP</name>
  <files>(no files modified — manual gate)</files>
  <read_first>
    - .planning/phases/04-content-surface/04-VALIDATION.md row V-17 (Paddle modal Playwright check)
    - docs/INTEGRATION-CONTRACT.md §6
    - .planning/codebase/TESTING.md §"Playwright MCP workflow"
  </read_first>
  <what-built>
    Tasks 1 + 2 changed how `/pay` is served (head-tier → full-tier prerender, body content now in initial HTML, PayPage eager-imported on both routers). Per INTEGRATION-CONTRACT §6, the Paddle modal must still open when a user clicks the Pay button. The MUST-HAVE truth "Clicking Pay opens the Paddle overlay (no regression vs Phase 3)" is verified here.

    Executor invokes Playwright MCP (`mcp__playwright__*` or the project's preferred MCP) to:
    1. `npm run build && npm run preview` (Vite preview server on http://localhost:4173).
    2. Navigate to `http://localhost:4173/pay`.
    3. Wait for hydration (mark via `window.__opten_paddle_ready` if present, else 2 s timeout).
    4. Click the Pay button for the RUB tier.
    5. Assert an `iframe[src*="paddle.com"]` (or the Paddle overlay container) becomes visible within 3 s.
    6. Repeat for `http://localhost:4173/en/pay` (clicks the USD tier — currency defaults differ).
    7. Capture console errors during the run — must be 0 React-hydration errors (no #418/#423/#185 etc).
  </what-built>
  <how-to-verify>
    1. Run `npm run build` — exits 0.
    2. Run `npm run preview` in one terminal.
    3. From another shell or directly via Playwright MCP, navigate to http://localhost:4173/pay; visually confirm pricing cards render BEFORE any JS executes (Network tab: disable JS, reload, pricing still shows).
    4. Re-enable JS, click the Pay button on the Pro RU card. Paddle overlay must open with the RUB checkout.
    5. Repeat at http://localhost:4173/en/pay — Paddle overlay opens with USD checkout.
    6. Check browser console — 0 errors, 0 hydration warnings.
    7. `curl -s http://localhost:4173/pay | grep -E "(199|2\\.99|300)"` returns matches.
    8. `curl -s http://localhost:4173/pay | grep "cdn.paddle.com"` returns the script line.
  </how-to-verify>
  <resume-signal>
    Type "approved — Paddle modal opens on /pay and /en/pay, pricing static, console clean" to proceed. If the Paddle modal regressed, capture the exact error in the SUMMARY.md and revert Task 1/Task 2 in a single revert commit before troubleshooting.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/pay` body now SSR-rendered | The pricing strings reach the initial HTML BEFORE Paddle.js executes; risk of stale price drift (visible HTML vs Paddle product configuration). |
| Paddle modal launch path | The Pay button still relies on `window.Paddle.Checkout.open` from the synchronous CDN script. Refactor risk: removing the path-gated `applyPaddleScript` call would break the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (price drift) | Static prices in `PricingStaticBlock` vs Paddle product config | accept | Prices are duplicated from `src/i18n/{ru,en}.json` (already the source of truth for landing); Paddle product config is owned in the extension repo Supabase secrets. Drift surface is identical to pre-Phase-4. |
| T-04-06 | Integrity (Paddle invariant) | `applyPaddleScript` path-gate preservation | mitigate | Task 1 and Task 2 verification commands grep `cdn.paddle.com` in both `dist/pay/index.html` and `dist/en/pay/index.html`. Task 3 Playwright check confirms modal opens end-to-end. |
| T-04-07 | DoS (bundle size) | PayPage now eager-imported into landing bundle? | mitigate | PayPage is eager in `main.tsx` BUT React Router code-splits the route — Vite emits a separate chunk for PayPage when imported but referenced only via `<Route element={<PayPage />} />`. Confirm with `npx vite build` size report; if landing chunk grows by >20 KB, revisit. |
</threat_model>

<verification>
  After all three tasks complete and three commits land:
  - Commit 1: `refactor(pay): extract PricingStaticBlock + eager-mount PayPage in SSR (Phase 4 D-12)`
  - Commit 2: `feat(seo): flip /pay + /en/pay to full prerender with Product schema (Phase 4 D-12)`
  - Commit 3 (if Task 3 surfaces fixes): `fix(pay): ...` — otherwise no commit, just the human-approval signal.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. `curl -s http://localhost:4173/pay | grep -E "(199|2\\.99|300)"` matches at least three patterns.
  3. `grep -q cdn.paddle.com dist/pay/index.html && grep -q cdn.paddle.com dist/en/pay/index.html`.
  4. Playwright MCP confirms Paddle modal opens on both routes; console clean.
  5. Google Rich Results Test on `/pay` reports `Product` recognized, 0 errors.
</verification>

<success_criteria>
  1. `PricingStaticBlock` extracted; PayPage uses it; no SSR-unsafe API at render time.
  2. PayPage eager-imported in main.tsx and entry-server.tsx (no `lazy()` for PayPage).
  3. `/pay` and `/en/pay` are `prerender: "full"` with Product + Breadcrumb schema.
  4. INTEGRATION-CONTRACT §6 preserved — Paddle script in both pay HTML files, modal opens.
  5. Pricing strings visible in initial HTML (verified via curl, not just React rendering).
</success_criteria>

<output>
  After completion, create `.planning/phases/04-content-surface/04-04-SUMMARY.md` covering:
  - Diff summary of PayPage.tsx (which JSX moved out, what stayed).
  - Confirmation that PricingFeature/Divider helpers moved with the extraction.
  - Playwright MCP result for V-17 (screenshot URL or console log).
  - Bundle-size delta (per `rollup-plugin-visualizer` if invoked, else `du -sh dist/assets/*.js`).
  - Rich Results Test result for /pay Product schema.
  - Confirmation that V-15, V-16, V-17 are green and ready for post-deploy verification.
</output>
