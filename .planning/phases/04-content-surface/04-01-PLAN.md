---
phase: 04-content-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - public/robots.txt
autonomous: true
requirements:
  - GEO-D-3
must_haves:
  truths:
    - "Every HTTP response includes X-Frame-Options: SAMEORIGIN"
    - "robots.txt advertises Content-Signal for AI-input/training/retrieval permissions"
    - "No Content-Security-Policy header is silently added"
  artifacts:
    - path: "vercel.json"
      provides: "Edge header configuration with new X-Frame-Options entry"
      contains: "X-Frame-Options"
    - path: "public/robots.txt"
      provides: "Crawler directives extended with Content-Signal line"
      contains: "Content-Signal"
  key_links:
    - from: "vercel.json"
      to: "headers[0].headers"
      via: "appended {key: \"X-Frame-Options\", value: \"SAMEORIGIN\"} entry"
      pattern: "X-Frame-Options"
    - from: "public/robots.txt"
      to: "IETF AI-Preferences draft"
      via: "file-scope Content-Signal directive after Sitemap line"
      pattern: "Content-Signal:"
---

<objective>
Land the two zero-risk sidecar fixes from CONTEXT D-14: ship clickjacking protection via `X-Frame-Options: SAMEORIGIN` in `vercel.json`, and signal Opten's AI-content stance via `Content-Signal:` in `public/robots.txt`. Closes V-20, V-21, V-22.

Purpose: Wave-1 quick wins that derisk the deploy pipeline before the heavier schema and content work in Waves 2–5. No React, no build pipeline, no prerender changes.
Output: One commit per file (two atomic commits) to keep rollback granular.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-surface/04-CONTEXT.md
@.planning/phases/04-content-surface/04-PATTERNS.md
@.planning/phases/04-content-surface/04-VALIDATION.md

<interfaces>
From `vercel.json` (existing headers shape on `/(.*)`):

- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(self "https://*.paddle.com")`

Phase 4 ADDS: `X-Frame-Options: SAMEORIGIN`. Phase 4 MUST NOT add: `Content-Security-Policy` (PROJECT.md "Out of Scope" — Paddle inline-script + nonce coordination is a separate ticket).

From `public/robots.txt`: file ends at line 55 with `Sitemap: https://opten.space/sitemap.xml`. The `Content-Signal:` directive is file-scope (not per-User-agent), per IETF AI-Preferences draft.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append X-Frame-Options: SAMEORIGIN to vercel.json headers</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (the full file — 20 lines)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"vercel.json (MODIFIED — append X-Frame-Options)"
    - .planning/PROJECT.md §"Out of Scope" (CSP rationale)
    - .planning/phases/04-content-surface/04-VALIDATION.md rows V-20, V-22
  </read_first>
  <action>
    Edit `vercel.json` to append exactly one new entry to the `headers[0].headers` array on `/(.*)`: `{ "key": "X-Frame-Options", "value": "SAMEORIGIN" }`. Place it as the fourth header, after `Permissions-Policy`. Preserve trailing comma/JSON syntax — `vercel.json` is strict JSON, no comments. DO NOT add a `Content-Security-Policy` header in the same edit (D-14 explicit prohibition; PROJECT.md "Out of Scope" — Paddle inline-script conflict). DO NOT touch the `rewrites` or `functions` blocks. Per D-14 this implements clickjacking defense for the marketing surface; this header applies site-wide and is harmless to `/account` / `/dashboard/*` which are not iframed.
  </action>
  <verify>
    <automated>node -e "const j = require('./vercel.json'); const h = j.headers[0].headers.map(x => x.key); if (!h.includes('X-Frame-Options')) process.exit(1); if (h.includes('Content-Security-Policy')) { console.error('CSP forbidden in Phase 4'); process.exit(2); } console.log('OK', h.join(','));"</automated>
  </verify>
  <done>
    `vercel.json` parses as valid JSON, `headers[0].headers` contains an `X-Frame-Options` entry with value `SAMEORIGIN`, no `Content-Security-Policy` entry exists. `git diff vercel.json` shows ONLY the appended header and an adjusted trailing comma — no other changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Content-Signal directive to robots.txt</name>
  <files>public/robots.txt</files>
  <read_first>
    - public/robots.txt (the full file — 55 lines)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"public/robots.txt (MODIFIED — append Content-Signal)"
    - .planning/phases/04-content-surface/04-RESEARCH.md (search for "Content-Signal" / "Cloudflare" / "ai-input" — flagged Open Question Q2)
    - .planning/phases/04-content-surface/04-VALIDATION.md row V-21
  </read_first>
  <action>
    Append two lines after the existing `Sitemap: https://opten.space/sitemap.xml` line: (1) a blank line, then (2) a comment header `# Phase 4 D-14: AI-Preferences (IETF draft / Cloudflare proposal)`, then (3) `Content-Signal: search=yes, ai-train=yes, ai-input=yes`. Use `ai-input` (Cloudflare vocabulary per RESEARCH.md Open Question Q2), NOT `ai-retrieval` (CONTEXT D-14 vocabulary mismatch flagged in research). The directive sits at file scope (not under a User-agent block) per the IETF AI-Preferences draft. Do NOT modify any existing per-bot Allow/Disallow blocks. If `ai-input` ends up controversial during review, replacing with `ai-retrieval=yes` is a one-line follow-up — flag this choice in the commit message body.
  </action>
  <verify>
    <automated>grep -E "^Content-Signal:[[:space:]]" public/robots.txt | grep -E "search=yes" | grep -E "ai-train=yes" | grep -E "ai-input=yes|ai-retrieval=yes"</automated>
  </verify>
  <done>
    `public/robots.txt` contains a single `Content-Signal:` line at file scope listing `search=yes`, `ai-train=yes`, and `ai-input=yes` (or `ai-retrieval=yes` if reviewer overrides). The existing 8 per-bot blocks and the Sitemap line are unchanged. `npm run build` succeeds and `dist/robots.txt` (Vite copies `public/*` early in build) contains the new line verbatim.
  </done>
</task>

</tasks>

<verification>
  Both files committed as separate atomic commits (one per file, per Phase 4 "atomic-commit-per-requirement" pattern from `04-PATTERNS.md` §"Atomic Commit Convention"):
  - Commit 1: `chore(headers): add X-Frame-Options: SAMEORIGIN (Phase 4 D-14)`
  - Commit 2: `chore(robots): advertise Content-Signal AI preferences (Phase 4 D-14)`

  Post-build:
  - `npm run build` exits 0.
  - `grep "X-Frame-Options" dist/index.html` returns no matches (header is Vercel-edge-applied, not in HTML — verification proper happens post-deploy via `curl -sI`).
  - `grep "Content-Signal" dist/robots.txt` returns the appended line.

  Post-deploy (out of plan scope — handled by `/gsd-verify-work`):
  - `curl -sI https://opten.space/ | grep -i 'x-frame-options'` returns `SAMEORIGIN`.
  - `curl -s https://opten.space/robots.txt | grep -i 'content-signal'` returns the directive.
</verification>

<success_criteria>
  1. `vercel.json` parses as valid JSON and contains `X-Frame-Options: SAMEORIGIN`.
  2. `public/robots.txt` contains a `Content-Signal:` line at file scope.
  3. NO `Content-Security-Policy` entry exists in `vercel.json` (V-22).
  4. `npm run build` succeeds.
  5. Two atomic commits land.
</success_criteria>

<output>
  After completion, create `.planning/phases/04-content-surface/04-01-SUMMARY.md` covering:
  - Final vocabulary choice (`ai-input` vs `ai-retrieval`) and rationale.
  - Confirmation that V-20, V-21, V-22 acceptance commands pass against `dist/` and `vercel.json`.
  - One-line note on which commit is the rollback target if either header causes a regression after deploy.
</output>
