---
phase: 04-content-surface
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - src/app/pages/AboutPage.tsx
  - scripts/entry-server.tsx
  - src/main.tsx
  - scripts/seo-routes.ts
  - scripts/sitemap.mjs
  - src/i18n/ru.json
  - src/i18n/en.json
  - src/app/App.tsx
  - public/assets/about/founder.jpg
autonomous: false
requirements:
  - GEO-D-1
must_haves:
  truths:
    - "Visiting /about returns ~1000+ words of RU founder content in initial HTML"
    - "/about carries <html lang=\"ru\">, hreflang ru self-canonical (no en alternate per D-02)"
    - "/about JSON-LD contains a Person block with name, jobTitle, worksFor (Org @id), sameAs, AND image"
    - "/about JSON-LD contains an Organization block and a BreadcrumbList block"
    - "/en/about does NOT exist as a prerendered file (dist/en/about/index.html absent)"
    - "On EN routes the About nav link is hidden (D-16: hide-entirely option)"
    - "Founder photo file lands in public/assets/about/ and is referenced in Person.image"
  artifacts:
    - path: "src/app/pages/AboutPage.tsx"
      provides: "RU-only E-E-A-T page — founder bio + photo + credentials + Telegram link"
      min_lines: 120
    - path: "scripts/seo-routes.ts"
      provides: "Manifest entry for /about with Person + Org + Breadcrumb schema and prerender: full"
      contains: "/about"
    - path: "public/assets/about/founder.jpg"
      provides: "Founder photo (≤ 200 KB, web-optimized) for Person.image and visible <img>"
  key_links:
    - from: "src/main.tsx"
      to: "AboutPage"
      via: "eager import + <Route path=/about>"
      pattern: "AboutPage"
    - from: "scripts/entry-server.tsx"
      to: "AboutPage"
      via: "eager import + <Route path=/about>"
      pattern: "AboutPage"
    - from: "src/app/App.tsx (or top-level nav component)"
      to: "<LocalizedLink to=/about>"
      via: "lang gate — render only when lang === ru"
      pattern: "lang === \"ru\""
---

<objective>
Ship the RU-only `/about` E-E-A-T page (GEO-D-1) — founder bio, photo, Telegram contact, credentials — with `Person` schema linked to the Organization graph via `@id`. Closes audit finding H-3.

Purpose: Establish Experience, Expertise, and Trust signals so AI assistants have a citable "who built Opten" surface. Without this page, the Organization JSON-LD has no `founder` resolution target on a public URL.
Output: A new page + manifest entry + Person schema wired with `image` property; nav-link suppression on EN routes per D-16 (hide-entirely option chosen — cleaner UX than "Coming soon" stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-content-surface/04-CONTEXT.md
@.planning/phases/04-content-surface/04-PATTERNS.md
@.planning/phases/04-content-surface/04-RESEARCH.md
@.planning/phases/04-content-surface/04-VALIDATION.md
@.planning/phases/04-content-surface/04-04-SUMMARY.md
@src/app/pages/WelcomePage.tsx
@src/app/pages/PrivacyPage.tsx
@src/app/components/layout/LegalLayout.tsx
@src/i18n/LangContext.tsx
@scripts/seo-routes.ts
@scripts/entry-server.tsx
@scripts/sitemap.mjs

<interfaces>
Reusable schema constants from Plan 04-03 (already in `scripts/seo-routes.ts`):
- `ORG_BLOCK` — Organization (#org)
- `PERSON_FOUNDER_BLOCK` — Person (#person-founder). NOTE: in Plan 04-03 this block had `image:` commented out; Plan 04-05 uncomments and supplies the URL `${SITE_ORIGIN}/assets/about/founder.jpg`.
- `breadcrumbBlock(items, pageId)` — builder.

`EN_SIBLINGS` (src/i18n/paths.ts): `/about` MUST NOT be added (D-02). LocalizedLink will NOT rewrite the href on EN routes — which is correct, because on EN routes the link is hidden anyway (D-16 chosen option).

`hreflang` policy for `/about` (D-02):
- `ru`: `${SITE_ORIGIN}/about` (self-canonical)
- `en`: `${SITE_ORIGIN}/` (fallback to RU landing; no /en/about exists)
- `xDefault`: `${SITE_ORIGIN}/about`

Founder data (sourced from CONTEXT D-01, PROJECT.md, existing `/privacy` legal disclosure):
- Name: Виктор Воронежцев
- Legal form: ИП Воронежцев В.П.
- Job title: Founder, Opten
- Telegram: https://t.me/v_voronezhtsev
- Photo: supplied during this plan's execution; user prompt at Task 0.

Sitemap floor (`scripts/sitemap.mjs:29`): currently `< 12` throws. After Plan 04-05 the count rises to 13 (12 baseline + /about). After Plan 04-06 the count rises to 15 (+2 guide siblings). Bump the floor to 13 in this plan; Plan 04-06 bumps to 15.

D-16 nav-link choice: HIDE ENTIRELY on EN routes — cleaner UX, avoids "Coming soon" English text on a Russian-only page link. The nav link in `src/app/App.tsx` (or the top-level nav component currently rendering the brand row) is wrapped in `{lang === "ru" && <LocalizedLink to="/about">{t("nav.about")}</LocalizedLink>}`. The lang is sourced from `useLang()` per Phase 3 D-05.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Supply founder photo asset</name>
  <files>public/assets/about/founder.jpg</files>
  <read_first>
    - .planning/phases/04-content-surface/04-CONTEXT.md D-03
    - .planning/phases/04-content-surface/04-VALIDATION.md "Manual-Only Verifications" rows
  </read_first>
  <what-built>
    Per D-03, the Person schema MUST have an `image` property AND the page MUST render a visible founder photo for visual E-E-A-T. Claude cannot generate this asset — the user supplies one of:
    (a) A path to an existing photo on disk (any extension; we re-encode to JPEG ≤ 200 KB at 800×800 or 4:3).
    (b) A URL Claude can `fetch` (Telegram avatar, LinkedIn export, etc.).
    (c) An approval to ship without a photo — in which case the `Person.image` property stays commented out and the `<img>` on `/about` falls back to initials or is omitted.

    Once the source is supplied, Claude:
    1. Saves to `public/assets/about/founder.jpg` (JPEG, sRGB, ≤ 200 KB, dimensions ~800×800 or 4:3 aspect).
    2. Optionally creates a WebP sibling `public/assets/about/founder.webp` if `vite-imagetools` flow is reused — but plain JPEG is acceptable for a single decorative image.
    3. Confirms `dist/assets/about/founder.jpg` is copied during `npm run build` (Vite copies all of `public/` to `dist/`).
  </what-built>
  <how-to-verify>
    1. `ls -lh public/assets/about/founder.jpg` reports a file ≤ 200 KB.
    2. `npm run build && ls -lh dist/assets/about/founder.jpg` reports the same file in dist.
    3. Open the file in an image viewer — looks like a real photograph of a person (not a stock placeholder).
    4. If the user chose option (c) "no photo", confirm in the plan task description that `Person.image` stays commented in `PERSON_FOUNDER_BLOCK` and AboutPage renders a `<div className="...">{initials}</div>` placeholder instead of an `<img>`.
  </how-to-verify>
  <resume-signal>
    Type "photo approved at public/assets/about/founder.jpg" OR "shipping without photo — Person.image commented" before proceeding to Task 1.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create AboutPage component with RU content</name>
  <files>src/app/pages/AboutPage.tsx, src/i18n/ru.json, src/i18n/en.json</files>
  <read_first>
    - src/app/pages/WelcomePage.tsx (analog — eager-imported SSR page with embedded body content; per 04-PATTERNS.md "exact" match)
    - src/app/components/layout/LegalLayout.tsx (header/footer chrome pattern — lines 15–51)
    - src/app/components/LocalizedLink.tsx
    - src/app/components/LangSwitcher.tsx
    - src/i18n/LangContext.tsx
    - src/i18n/ru.json + src/i18n/en.json (focus on `nav.*` keys at lines 21–25)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"src/app/pages/AboutPage.tsx (NEW — page, RU-only, SSR)" + §"Internal Navigation"
    - .planning/phases/04-content-surface/04-CONTEXT.md D-01, D-02, D-07
  </read_first>
  <action>
    (a) CREATE `src/app/pages/AboutPage.tsx`. Eager-import contract — file uses only `useT` from `LangContext` for chrome strings; body content is embedded directly in JSX as static Russian text per D-07 (NO LLM-generated copy; the executor writes the body manually). Imports per `04-PATTERNS.md` §"Imports pattern":
    - `import { useT } from "../../i18n/LangContext";`
    - `import LangSwitcher from "../components/LangSwitcher";`
    - `import LocalizedLink from "../components/LocalizedLink";`

    Component structure (per `04-PATTERNS.md` §"Component shape pattern"):
    - Header chrome — copy from `LegalLayout.tsx:15-27` (brand link + LangSwitcher + back-to-home link).
    - Hero block: `<img src="/assets/about/founder.jpg" alt="...">` (or initials placeholder per Task 0 outcome) + founder name + tagline + Telegram CTA.
    - Body: ~1000+ words RU, split into sections — "Откуда взялся Opten" / "Почему именно про промпты" / "Что вы получаете" / "Контакты". The body is HUMAN-WRITTEN per D-07 — the executor surfaces a checkpoint to the user with a draft outline and pauses for human content; OR if the user has already supplied final copy (in `.planning/phases/04-content-surface/04-CONTEXT.md` or out-of-band), uses that verbatim. DO NOT generate the body with an LLM.
    - Credentials sub-section: ИП registration (D-01), Telegram, repo links if public.
    - Footer chrome — copy from `LegalLayout.tsx:44-51` (LocalizedLink to /privacy, /terms, /refund).

    Tailwind classes: copy verbatim from `WelcomePage.tsx` and `LegalLayout.tsx` to maintain visual consistency (dark theme, PT Root UI font, max-w 800px column).

    SSR safety: NO `window`, `localStorage`, `navigator` reads at render time. The page is statically renderable.

    (b) MODIFY `src/i18n/ru.json` — add chrome-only keys: `nav.about: "О проекте"`, `about.tagline: "<short tagline>"`, `about.contactCta: "Написать в Telegram"`, `about.backLink: "Назад на главную"`. Section headings (`about.section.origin`, `about.section.contacts`, etc.) only if the body uses `useT` for them — preferred is to inline the section headings in JSX since the body is RU-only and not interpolated.

    (c) MODIFY `src/i18n/en.json` — add `nav.about: "About"` (used as a placeholder ONLY in case the lang-gate logic in Task 3 is wrong; with D-16's hide-entirely option, this key is never rendered. Adding it keeps the dicts symmetric and prevents `useT` from logging "missing key" warnings if a future plan accidentally renders it). All `about.*` keys are RU-only — DO NOT mirror them into en.json.

    Comment convention: prefix the file header with `// Phase 4 D-01 / D-02: RU-only E-E-A-T page. /en/about deferred to Phase 4.1.`
  </action>
  <verify>
    <automated>npm run build && grep -q "О проекте" src/i18n/ru.json && grep -c "[А-Яа-я]" src/app/pages/AboutPage.tsx && test -f src/app/pages/AboutPage.tsx</automated>
  </verify>
  <done>
    `src/app/pages/AboutPage.tsx` exists, is eager-importable (no top-level browser API access), has ≥ 120 lines including embedded RU body content, references the founder photo path (or initials fallback), includes a Telegram CTA, and uses LegalLayout-style chrome. `npm run build` succeeds. The RU/EN i18n dicts have the new `nav.about` and `about.*` keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register /about route in entry-server.tsx + main.tsx and add manifest entry with Person schema</name>
  <files>scripts/entry-server.tsx, src/main.tsx, scripts/seo-routes.ts, scripts/sitemap.mjs</files>
  <read_first>
    - scripts/entry-server.tsx (the full file)
    - src/main.tsx (the full file)
    - scripts/seo-routes.ts (the full file — post-Plan-04 state)
    - scripts/sitemap.mjs (lines 27–31 floor check)
    - .planning/phases/04-content-surface/04-PATTERNS.md §"scripts/entry-server.tsx (MODIFIED — eager-mount new routes)" + §"src/main.tsx (MODIFIED)" + §"scripts/seo-routes.ts (MODIFIED — add 3 entries...)"
    - .planning/codebase/CONVENTIONS.md §"EN_SIBLINGS SYNC Contract"
  </read_first>
  <action>
    Multi-file edit (single commit):

    (a) MODIFY `scripts/entry-server.tsx`: add `import AboutPage from "../src/app/pages/AboutPage.tsx";` to the eager-import block; add `<Route path="/about" element={<AboutPage />} />` to the RU section of `<Routes>`. DO NOT add an `/en/about` mount — D-02.

    (b) MODIFY `src/main.tsx`: add `import AboutPage from "./app/pages/AboutPage.tsx";` to the eager block; add `<Route path="/about" element={<AboutPage />} />` in the SPA Routes block. DO NOT add `/en/about`.

    (c) MODIFY `scripts/seo-routes.ts`: append a new entry between the RU entries and the EN section (preserve existing comment ordering). The entry follows the shape in `04-PATTERNS.md` §"New /about entry":

    ```
    path: "/about",
    htmlLang: "ru",
    hreflangAlternates: {
      ru: `${SITE_ORIGIN}/about`,
      en: `${SITE_ORIGIN}/`,           // D-02 — fallback to landing
      xDefault: `${SITE_ORIGIN}/about`,
    },
    title: "О проекте Opten — кто стоит за расширением",
    description: "Opten — личный проект В. Воронежцева. История создания, команда, контакты.",
    canonical: `${SITE_ORIGIN}/about`,
    ogTitle: "О проекте Opten",
    ogDescription: "История создания Opten — расширения для оценки промптов.",
    prerender: "full",
    changefreq: "yearly",
    priority: 0.5,
    schema: [ORG_BLOCK, PERSON_FOUNDER_BLOCK, breadcrumbBlock([{name: "Главная", url: SITE_ORIGIN + "/"}, {name: "О проекте", url: SITE_ORIGIN + "/about"}], SITE_ORIGIN + "/about")],
    ```

    In `PERSON_FOUNDER_BLOCK` (defined in Plan 04-03), UNCOMMENT the `image` property and set its value to `${SITE_ORIGIN}/assets/about/founder.jpg` IF the user supplied a photo in Task 0. If Task 0 ended with "shipping without photo", leave the property commented and note this in the SUMMARY.

    NO entry is added for `/en/about` (D-02).

    (d) MODIFY `scripts/sitemap.mjs` line 29: bump the floor from `< 12` to `< 13` (12 baseline + /about). Update the error message string to mention "13 routes (6 RU + 6 EN + /about)" — Plan 04-06 raises this to 15.

    Verify build chain: `npm run build` should emit `dist/about/index.html` AND NOT emit `dist/en/about/index.html`.
  </action>
  <verify>
    <automated>npm run build && test -f dist/about/index.html && ! test -f dist/en/about/index.html && grep -q "Person" dist/about/index.html && grep -q 'lang="ru"' dist/about/index.html && grep -q 'hreflang="en"[^>]*"https://opten.space/"' dist/about/index.html</automated>
  </verify>
  <done>
    `dist/about/index.html` exists with `<html lang="ru">`, hreflang triplet (ru self-canonical, en→landing, x-default→/about), Person + Org + Breadcrumb JSON-LD, RU body content from `AboutPage.tsx`. `dist/en/about/index.html` does NOT exist. Sitemap floor bumped to 13. `sitemap.xml` lists `/about` once (RU only).
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate the About nav link to RU routes only (D-16 hide-entirely option)</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (the full file — focus on the navbar / header section that already renders nav links like "Тарифы")
    - src/app/components/LangSwitcher.tsx
    - src/i18n/LangContext.tsx (focus on `useLang()` signature)
    - .planning/phases/04-content-surface/04-CONTEXT.md D-16
    - .planning/phases/04-content-surface/04-PATTERNS.md §"Internal Navigation" (lang-gate example)
  </read_first>
  <action>
    In `src/app/App.tsx` (landing component which renders the brand row / nav), add the About link AND gate it on `lang === "ru"`. Pattern from `04-PATTERNS.md` §"Internal Navigation":

    ```
    const { lang } = useLang();
    // ...somewhere in the nav row JSX:
    {lang === "ru" && (
      <LocalizedLink to="/about" className="...">
        {t("nav.about")}
      </LocalizedLink>
    )}
    ```

    Place the About link in the existing nav row alongside any current marketing nav links. If `App.tsx` does not currently have a visible nav row (the landing is hero-only), add the link to the footer instead — the footer is reached more reliably than the header for SEO + crawler purposes anyway.

    On `/en/` routes, the gate renders nothing — the user never sees a broken or "Coming soon" link. This is the cleaner of the two D-16 options.

    No changes are needed to `LegalLayout.tsx` because its header/footer is shared with `/privacy`/`/terms`/`/refund` — keeping the About link OUT of LegalLayout means EN legal pages don't show a link to a RU-only page. AboutPage itself uses its own LegalLayout-style chrome and does NOT need its own About link (it IS the About page).

    Add `// Phase 4 D-01 / D-16: About link, RU-only — see PROJECT.md D-02 (no /en/about)` comment above the gated JSX.
  </action>
  <verify>
    <automated>npm run build && grep -q 'lang === "ru"' src/app/App.tsx && grep -q "nav.about" src/app/App.tsx</automated>
  </verify>
  <done>
    `src/app/App.tsx` renders the About link only when `lang === "ru"`. Visiting http://localhost:4173/ (RU) shows the link; visiting http://localhost:4173/en/ does NOT show it. `npm run build` succeeds and `dist/index.html` contains the About anchor while `dist/en/index.html` does NOT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AboutPage content provenance | D-07 forbids LLM-generated copy on E-E-A-T pages; the body must originate from the human user. Risk: executor agent might draft body autonomously and pollute the page. |
| Founder photo asset | Public marketing material on every load of `/about`; minimal risk surface (no PII beyond what's already public). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Repudiation | AI-generated body content damaging E-E-A-T | mitigate | Task 1 action explicitly forbids LLM-generated body; executor must surface checkpoint to user for body copy OR use pre-supplied draft. Manual review row in `04-VALIDATION.md` covers this. |
| T-04-09 | Information disclosure | Founder photo metadata (EXIF) | mitigate | Re-encode JPEG strips EXIF by default if user supplies the original; explicitly note in Task 0 instruction set. |
| T-04-10 | Tampering | hreflang `en` value on /about | mitigate | The `en` hreflang on /about points at `${SITE_ORIGIN}/` (D-02 fallback), NOT at /en/about (which would be a broken link). Verification command in Task 2 greps for this exact form. |
</threat_model>

<verification>
  Four commits to land (one per task; Task 0 = asset commit + Task 1 = page commit + Task 2 = manifest/router commit + Task 3 = nav-gate commit):

  1. `npm run build` exits 0.
  2. `curl -s http://localhost:4173/about | wc -w` ≥ 800.
  3. `curl -s http://localhost:4173/about | grep -E '"@type": ?"Person"'` returns a match (V-02).
  4. `curl -s http://localhost:4173/about | grep -E 'hreflang="ru"|hreflang="en"|hreflang="x-default"'` returns three lines, EN line points at landing (V-03).
  5. `curl -sI http://localhost:4173/en/about` returns SPA fallback HTML (V-04).
  6. Visit /en/ in browser — the About link is absent (D-16 hide option).
  7. Visit / in browser — the About link is present.
  8. Founder photo loads in /about page (or initials placeholder if Task 0 was option c).
  9. Rich Results Test on /about returns 0 errors for Person, Organization, BreadcrumbList.
</verification>

<success_criteria>
  1. `/about` ships ~1000+ words RU (V-01).
  2. Person schema with `image` (if photo supplied), `worksFor` (Org ref), `sameAs`, `jobTitle` (V-02).
  3. `/about` carries `<html lang="ru">`, hreflang only for ru + en→landing + x-default→/about (V-03).
  4. `/en/about` does NOT exist as a prerendered file (V-04).
  5. About nav link hidden on /en/* per D-16 hide-entirely option.
  6. Sitemap floor bumped to 13; `dist/sitemap.xml` lists `/about`.
</success_criteria>

<output>
  After completion, create `.planning/phases/04-content-surface/04-05-SUMMARY.md` covering:
  - Whether the founder photo shipped or was deferred.
  - Word count of `/about` body.
  - Confirmation that Person.image URL resolves (`curl -I` returns 200).
  - Rich Results Test result for /about.
  - Confirmation that V-01, V-02, V-03, V-04 are green.
  - Confirmation of D-16 implementation choice (hide-entirely was chosen).
</output>
