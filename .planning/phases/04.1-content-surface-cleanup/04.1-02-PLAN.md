---
phase: 04.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seo-routes.ts
autonomous: true
requirements:
  - WR-02
  - WR-03
  - WR-05
must_haves:
  truths:
    - "Free-tier Offer is no longer present in productBlock([...]) calls on /pay or /en/pay"
    - "productBlock(...) call ITSELF is preserved on /pay and /en/pay (only the Free-tier ARRAY ENTRY is removed)"
    - "ORG_BLOCK and breadcrumbBlock(...) entries on /pay and /en/pay are unchanged"
    - "prerender: full flag on /pay and /en/pay is preserved (baseline)"
    - "Guide route breadcrumb on /guides/gpt-image-2 and /en/guides/gpt-image-2 is 2 levels (root -> article), no middle Guides/Гайды step"
    - "productBlock throws with file/line context when given a price that is not a finite number"
  artifacts:
    - path: "scripts/seo-routes.ts"
      provides: "Updated productBlock call args (RU + EN /pay) without Free-tier entry"
      contains: "productBlock"
    - path: "scripts/seo-routes.ts"
      provides: "Updated breadcrumbBlock call args on both guide routes (root -> article, no /guides middle step)"
      contains: "breadcrumbBlock"
    - path: "scripts/seo-routes.ts"
      provides: "productBlock function entry validates and parses each price via Number(p.price), throws on non-finite"
      contains: "Number(p.price)"
    - path: "scripts/seo-routes.ts"
      provides: "Updated Phase 4 D-12 comment on both /pay entries reflecting post-revert reality"
      contains: "post-revert f9dfdb1"
  key_links:
    - from: "scripts/seo-routes.ts /pay entry"
      to: "productBlock([Pro Monthly RU, Pro Monthly], pageId)"
      via: "schema array, 2-entry plans param (Free tier dropped)"
      pattern: "productBlock"
    - from: "scripts/seo-routes.ts /en/pay entry"
      to: "productBlock([Pro Monthly RU, Pro Monthly], pageId)"
      via: "schema array, 2-entry plans param (Free tier dropped)"
      pattern: "productBlock"
    - from: "scripts/seo-routes.ts /guides/gpt-image-2 breadcrumb"
      to: "[Главная, gptImage2Guide.ru.title]"
      via: "2-entry items array (middle Гайды step removed)"
      pattern: "breadcrumbBlock"
---

<objective>
Close WR-02 (Free tier in Product offers), WR-03 (breadcrumb to non-existent /guides), and WR-05 (parseFloat without NaN/locale guard) inside `scripts/seo-routes.ts` in one atomic commit. Surgical edits to existing factory calls and the `productBlock` factory body — no schema topology changes, no new factories.

Purpose: WR-02 is reputation-affecting — the Rich Results Test currently shows a $0 "Free tier" Offer on /pay, which conflicts with the user-confirmed B-01 pricing reality (Pro tier only). WR-03 is SEO-damaging — guide route breadcrumb links to `/guides` index that does not exist (soft-404 risk). WR-05 is a defensive guard — `parseFloat("199")` happens to work today, but a future translator typing `"199,99"` or `"199 руб"` would silently min/max NaN. All three are localized to this one file.
Output: One commit updating productBlock call args (drop Free entry on both /pay routes), updating breadcrumb call args (drop /guides middle step on both guide routes), normalizing prices via `Number()` inside `productBlock`, and updating the stale Phase-4-D-12 comment on both /pay entries.

**⛔ BASELINE PRESERVATION (Wave 2 constraint, see CAPTURED-DECISIONS.md):** The `productBlock(...)` CALL itself on /pay + /en/pay MUST stay. So must the sibling `ORG_BLOCK` and `breadcrumbBlock(...)` entries in the same schema array. So must `prerender: "full"`. Only the `{ name: "Free tier", price: "0", currency: "USD" }` ENTRY inside `productBlock([...])` is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@.planning/phases/04-content-surface/04-CONTEXT.md
@scripts/seo-routes.ts

<interfaces>
From `scripts/seo-routes.ts` (existing — TO EDIT):

- Lines 155-197: `productBlock(plans, pageId)` factory. Inputs: `plans: { name: string; price: string; currency: string }[]`. Uses `parseFloat(p.price)` for lowPrice/highPrice computation in the single-currency AggregateOffer branch (lines 183-184). WR-05 fix: normalize at function entry via `const parsed = plans.map(p => { const n = Number(p.price); if (!Number.isFinite(n)) throw new Error(\`productBlock(${pageId}): non-finite price "${p.price}" for plan "${p.name}"\`); return { ...p, _parsedPrice: n }; });`. Replace the `parseFloat(...)` calls in the lowPrice/highPrice reducers with comparisons on `_parsedPrice`. Keep the formatted string `p.price` for schema output (don't replace user-facing prices with the Number).
- Lines 252-268 (`/pay` schema array): the `productBlock([{Pro Monthly (RU)…}, {Pro Monthly…}, {Free tier…}], pageId)` call. Remove the 3rd array entry (`{ name: "Free tier", price: "0", currency: "USD" }`); array becomes 2 entries.
- Lines 247: comment `// Phase 4 D-12: flipped from "head" so PricingStaticBlock body lands in initial HTML`. UPDATE to: `// Phase 4 D-12 (post-revert f9dfdb1): full prerender keeps the runtime-conditional pricing cards in initial HTML with visible prices, so Product schema validates against visible content.`
- Lines 487-504 (`/en/pay` schema array): same productBlock call shape, same Free-tier 3rd entry. Same removal.
- Lines 484: comment on the EN `/pay` `prerender: "full"` line — currently `// Phase 4 D-12`. UPDATE to the same post-revert wording above (or, if it is already minimal, expand to match for symmetry).
- Lines 438-446 (`/guides/gpt-image-2` schema array, breadcrumbBlock call): currently 3-entry items array: `[{Главная, /}, {Гайды, /guides}, {gptImage2Guide.ru.title, /guides/gpt-image-2}]`. Remove the middle entry; array becomes 2 entries: `[{Главная, /}, {gptImage2Guide.ru.title, /guides/gpt-image-2}]`.
- Lines 644-653 (`/en/guides/gpt-image-2` schema array, breadcrumbBlock call): currently 3-entry items array: `[{Home, /en/}, {Guides, /en/guides}, {gptImage2Guide.en.title, /en/guides/gpt-image-2}]`. Remove the middle entry; array becomes 2 entries.

Two-entry productBlock arg flows through the `currencies.size === 1` branch ONLY when both currencies match — in this case `{ Pro Monthly (RU): RUB }` and `{ Pro Monthly: USD }` give `currencies.size === 2`, so the resulting offers shape is the raw `plans.map(...)` array — not an AggregateOffer. That is acceptable per Google's Rich Results spec ("either shape on Product"). Sanity check post-build: `dist/pay/index.html` has both `RUB` and `USD` strings in the Product schema JSON.

If a future single-currency case is desired, AggregateOffer min/max math now operates on `_parsedPrice` (the validated Number) — no NaN/locale risk.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop Free-tier Offer + collapse guide breadcrumb + harden productBlock price parsing + update D-12 comment</name>
  <files>scripts/seo-routes.ts</files>
  <read_first>
    - scripts/seo-routes.ts (the full file — 655 lines, but focus on: lines 155-197 productBlock factory; lines 247 + 252-268 /pay entry; lines 430-446 RU guide breadcrumb; lines 484 + 487-504 /en/pay entry; lines 638-653 EN guide breadcrumb)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 2: scripts/seo-routes.ts — Product schema + breadcrumb + price safety" + §"⛔ BASELINE — DO NOT REGRESS DURING 4.1"
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 2 (scripts/seo-routes.ts)" + §"Baseline preservation (the hard wall)"
    - .planning/phases/04-content-surface/04-CONTEXT.md §D-09 (Schema entity graph) and §D-12 (PayPage promotion)
  </read_first>
  <action>
    Five edits to `scripts/seo-routes.ts`, all in one atomic commit:

    (a) **Harden `productBlock` against non-finite prices (WR-05).** At the very top of the function body (line 156, right after `export function productBlock(plans, pageId) {`), add:
    - A `parsed` derivation: `const parsed = plans.map((p) => { const n = Number(p.price); if (!Number.isFinite(n)) { throw new Error('productBlock(' + pageId + '): non-finite price "' + p.price + '" for plan "' + p.name + '" — expected a number-parseable string (e.g. "199" or "2.99")'); } return { ...p, _n: n }; });`
    - Use `parsed` throughout the function body where price comparisons are needed. Specifically: replace `plans.reduce((min, p) => (parseFloat(p.price) < parseFloat(min) ? p.price : min), plans[0].price)` at line 183 with a parsed-driven equivalent: compute `const minN = parsed.reduce((m, p) => Math.min(m, p._n), Infinity); const minPrice = parsed.find((p) => p._n === minN).price;` (use the original formatted string for `lowPrice` output, but pick it via the parsed numeric). Same for `highPrice`.
    - In the multi-currency `plans.map(...)` branch (lines 188-194), keep using `p.name` / `p.price` / `p.currency` from the original input (not parsed) — the output schema must surface the original string price, not a normalized number. The `parsed` value is for internal validation + min/max math only.
    - DO NOT remove the existing `parseFloat`-driven reducers without replacing the math; the function must keep producing the same output shape for non-Free-tier cases.

    (b) **Drop Free-tier entry from RU `/pay` productBlock call (WR-02).** In the `path: "/pay"` RouteMeta entry (RU `/pay`, lines 234-269), inside the `schema:` array, locate the `productBlock([...], ...)` call. The current array has 3 entries: `{Pro Monthly (RU), 199, RUB}, {Pro Monthly, 2.99, USD}, {Free tier, 0, USD}`. **Remove ONLY the 3rd entry** (the `Free tier` one — `{ name: "Free tier", price: "0", currency: "USD" }`). Array becomes 2 entries. DO NOT change the `pageId` (2nd arg). DO NOT remove the `productBlock(...)` call itself, the `ORG_BLOCK` sibling, or the `breadcrumbBlock(...)` sibling — all stay.

    (c) **Drop Free-tier entry from EN `/en/pay` productBlock call (WR-02).** Mirror edit on the `path: "/en/pay"` RouteMeta entry (lines 470-505). Same surgical removal of the 3rd `{ Free tier }` array element from the productBlock call. Schema topology unchanged otherwise.

    (d) **Update the Phase 4 D-12 comment on both /pay entries.** On line 247 (RU `/pay` `prerender: "full"` line) and line 484 (EN `/en/pay` `prerender: "full"` line), replace the current `// Phase 4 D-12` style comment with: `// Phase 4 D-12 (post-revert f9dfdb1): full prerender keeps the runtime-conditional pricing cards in initial HTML with visible prices, so Product schema validates against visible content.` Use the same wording on both lines for symmetry.

    (e) **Collapse 3-level guide breadcrumb to 2 levels (WR-03) on both guide routes.** In the RU `/guides/gpt-image-2` RouteMeta entry (lines 414-447), in the `breadcrumbBlock([...], pageId)` call, the items array currently has 3 entries: `[{Главная, SITE_ORIGIN+/}, {Гайды, SITE_ORIGIN+/guides}, {gptImage2Guide.ru.title, SITE_ORIGIN+/guides/gpt-image-2}]`. **Remove ONLY the middle entry** (`{ name: "Гайды", url: SITE_ORIGIN + "/guides" }`). Array becomes 2 entries: `[{Главная, SITE_ORIGIN+/}, {gptImage2Guide.ru.title, SITE_ORIGIN+/guides/gpt-image-2}]`. Mirror on the EN `/en/guides/gpt-image-2` entry (lines 620-654): remove `{ name: "Guides", url: SITE_ORIGIN + "/en/guides" }`. Both arrays become 2 entries (root → article).

    DO NOT touch any other RouteMeta entry, any other schema factory (faqPageBlock, howToBlock, personBlock — untouched), the `prerender: "full"` flag on either /pay route (baseline), any of the EXTENSION_IDS / SUPABASE_URL / SUPABASE_ANON_KEY constants (INTEGRATION-CONTRACT), or the `applyPaddleScript` invocation in `prerender.mjs` (Wave 1 territory).
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -c '"name": "Free tier"' dist/pay/index.html)" -eq 0 ] && [ "$(grep -c '"name": "Free tier"' dist/en/pay/index.html)" -eq 0 ] && [ "$(grep -c '"item": "https://opten.space/guides"' "dist/guides/gpt-image-2/index.html")" -eq 0 ] && [ "$(grep -c '"item": "https://opten.space/en/guides"' "dist/en/guides/gpt-image-2/index.html")" -eq 0 ] && grep -q '"@type": "Product"' dist/pay/index.html && grep -q '"@type": "Product"' dist/en/pay/index.html && grep -q 'RUB' dist/pay/index.html && grep -q 'USD' dist/pay/index.html && grep -q 'cdn.paddle.com' dist/pay/index.html && grep -q 'cdn.paddle.com' dist/en/pay/index.html && grep -q '"@type": "BreadcrumbList"' "dist/guides/gpt-image-2/index.html" && grep -q '"@type": "BreadcrumbList"' "dist/en/guides/gpt-image-2/index.html"</automated>
  </verify>
  <done>
    `dist/pay/index.html` and `dist/en/pay/index.html` no longer contain `"name": "Free tier"` in the Product schema; both still contain `"@type": "Product"`, both currency codes (RUB + USD), the `cdn.paddle.com` Paddle CDN script (INTEGRATION-CONTRACT §6 preserved), and the visible `199` / `2.99` / `299` pricing strings (baseline preserved). `dist/guides/gpt-image-2/index.html` and `dist/en/guides/gpt-image-2/index.html` no longer contain a breadcrumb link to `/guides` or `/en/guides` (soft-404 risk eliminated); both still contain `"@type": "BreadcrumbList"` with exactly 2 `ListItem` entries. `npm run build` succeeds with zero warnings. The productBlock function throws (manual smoke: temporarily replace `"199"` with `"199,99"` in one productBlock call, `npm run build` fails with a clear non-finite-price error pointing at the pageId; revert).
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. `grep -c '"name": "Free tier"' dist/pay/index.html dist/en/pay/index.html` returns 0 for both.
  3. `grep -c '"item": "https://opten.space/guides"' dist/guides/gpt-image-2/index.html` returns 0; same for the EN sibling with `/en/guides`.
  4. `grep -q 'cdn.paddle.com' dist/pay/index.html && grep -q 'cdn.paddle.com' dist/en/pay/index.html` passes — Paddle CDN injection preserved (INTEGRATION-CONTRACT §6).
  5. `dist/pay/index.html` still contains visible `199`, `2.99`, `299`, `300` (or equivalents — the pricing cards body) — baseline preserved.
  6. productBlock throws on non-finite price (manual smoke per `<done>`).
  7. Comment update on lines 247 + 484 is in place (grep `post-revert f9dfdb1` returns 2 hits in `scripts/seo-routes.ts`).
</verification>

<success_criteria>
  1. WR-02 closed — no Free-tier Offer in either /pay JSON-LD Product block.
  2. WR-03 closed — guide breadcrumbs no longer reference non-existent /guides index.
  3. WR-05 closed — productBlock validates and parses prices at function entry; non-finite throws.
  4. Phase 4 D-12 comment updated to post-revert reality on both /pay entries.
  5. Baseline preserved — productBlock call shape, ORG_BLOCK / breadcrumbBlock siblings, prerender: "full", Paddle script, visible pricing strings ALL unchanged.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-02-SUMMARY.md` covering:
  - Diff line count for `scripts/seo-routes.ts`.
  - Confirmation that all four grep gates pass post-build.
  - Sample JSON-LD Product block from `dist/pay/index.html` (truncated) showing 2-currency offers without Free tier.
  - Sample BreadcrumbList block from `dist/guides/gpt-image-2/index.html` showing 2 ListItem entries.
  - Result of the non-finite-price smoke test (build fail confirmed + revert).

  Commit message: `fix(04.1): tighten Product/Offer schema + collapse guide breadcrumb to 2 levels (WR-02, WR-03, WR-05)`
</output>
</content>
</invoke>