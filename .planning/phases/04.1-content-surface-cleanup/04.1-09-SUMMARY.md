---
phase: 04.1
plan: 09
status: complete
commit: d6a93e4
approved_by_user: 2026-05-17
---
# 04.1-09 Summary: detectExtension race + lang-aware FAQ anchor

**Closes:** WR-04 (detectExtension race condition), IN-02 (hardcoded `/#faq` crosses language boundary)
**Commit:** `d6a93e4`
**Files changed:** `src/app/pages/PayPage.tsx` (+11 / −3)

## What changed

### WR-04 — `detectExtension` race fix
- Added `let resolved = false;` closure flag declared before the `for (const id of EXTENSION_IDS)` loop.
- Added `if (resolved) return;` as the first statement inside the `chrome.runtime.sendMessage` callback.
- Added `resolved = true;` before `setExtStatus("ready")` in the success branch.
- Added `resolved = true;` before `setExtStatus("not_logged_in")` in the not-logged-in branch.
- Gated both `setExtStatus("not_installed")` terminal-state sets (the callback's and the synchronous `catch`) on `!resolved && tried >= EXTENSION_IDS.length`.

### IN-02 — lang-aware FAQ anchor
- Added `const homeHash = lang === "en" ? "/en/#faq" : "/#faq";` next to the existing `useLang()` destructure (line 119).
- Replaced `<a href="/#faq">` with `<a href={homeHash}>` on line 394.
- Verified no other `<a href="/#…">` exists on PayPage (only that one anchor was hardcoded).

## Verification

### Source-side grep gates
- `let resolved = false` present in PayPage.tsx — PASS
- `homeHash` present in PayPage.tsx — PASS
- No `href="/#faq"` literal — PASS

### Forbidden-change gates (BASELINE preservation)
- No `PricingStaticBlock` — PASS
- No `ctaRail` — PASS
- `typeof window` SSR guard preserved — PASS
- `EXTENSION_IDS` constant unchanged — PASS
- No `EXTENSION_IDS` / `SUPABASE_URL` / `SUPABASE_ANON_KEY` added or removed in the diff — PASS

### Post-build gates
- `npm run build` exits 0; 15 routes prerendered — PASS
- `dist/pay/index.html` contains `cdn.paddle.com` — PASS
- `dist/en/pay/index.html` contains `cdn.paddle.com` — PASS
- `dist/pay/index.html` contains visible `199` price — PASS
- `dist/pay/index.html` contains visible `299` price — PASS
- `dist/pay/index.html` contains `chromewebstore.google.com` SSR CTA anchor — PASS
- `dist/en/pay/index.html` contains `chromewebstore.google.com` SSR CTA anchor — PASS

### Diff size
- `git diff src/app/pages/PayPage.tsx | wc -l` → 57 lines (raw `git diff` output)
- Net code mutations (excluding context lines + diff headers): ~10 lines
- Plan cap was "≤ ~40 lines"; raw diff output exceeded that by ~17 but the actual code change is well under the spirit of the cap. User reviewed and approved.

## Approval audit trail

- Diff surfaced to user 2026-05-17 with full forbidden-change and post-build gate results.
- User selected: "Approve commit (Recommended)".
- Commit landed as `d6a93e4` immediately after approval.

## Notes / deviations

- Raw `git diff | wc -l` reports 57 lines (over the plan's 40-line cap); the cap was interpreted as "actual code mutation ≈ 10 lines", which is well within scope. The seed expected ~20 lines and the implementation came in even leaner.
- No other deviations.

## Out of scope (deferred to Wave 12b or later)

- Sitewide grep for other `<a href="/#…">` literals across the site — landing FAQ block anchor + LangSwitcher anchors are owned by other components, not PayPage. If a similar lang-boundary bug exists elsewhere it's a separate ticket.
