---
tags:
  - gsd
  - context
  - phase
phase: 4.1
milestone: "geo-optimization"
kind: phase-context
---
# Phase 4.1: Content surface cleanup — Context

**Gathered:** 2026-05-17
**Status:** Ready for planning
**Source:** `04.1-CAPTURED-DECISIONS.md` (captured 2026-05-17, pre-pipeline ad-hoc work scrapped via commit `d3b24c2` and rebuilt through canonical GSD pipeline)
**Mode:** Auto-promoted from captured-decisions seed — the 18-finding scope and 3 user-confirmed answers were already locked in chat before this phase ran; this CONTEXT.md formalizes them for downstream agents.

<domain>
## Phase Boundary

Close all 18 outstanding items from Phase 4's review hangover **without touching the post-revert `/pay` baseline locked in commit `f9dfdb1`** and without introducing new product surface beyond the explicitly-approved `/en/about` EN sibling. Pure remediation phase — no new schema bundles, no new routes (except `/en/about`), no new build-pipeline phases.

**Explicitly in this phase:**

1. **9 warnings from `04-REVIEW.md`** — WR-01 (EN pages ship `og-card-ru.png`), WR-02 (Free tier in Product offers), WR-03 (breadcrumb to non-existent `/guides`), WR-04 (`detectExtension` race condition), WR-05 (`parseFloat` without NaN/locale guard), WR-06 (path traversal in `llms.mjs`), WR-07 (HTML entity decoder covers only 5 entities), WR-08 (`readonly FaqItem[]` variance), WR-09 (V-10 visible-Q/A ↔ JSON-LD mainEntity is convention, not a build gate).
2. **6 info findings from `04-REVIEW.md`** — IN-01 (Pricing heading hardcoded inline, bypasses i18n), IN-02 (`<a href="/#faq">` crosses lang boundary for EN users), IN-03 (no build-time check that full-tier routes are mounted in `entry-server.tsx`), IN-04 (`id="privacy"` collides with `/privacy` route slug), IN-05 (font preload regex literals — should be a constant), IN-06 (inline `<style>` block in AboutPage pollutes global CSS).
3. **3 user-reported bugs (2026-05-17)** — B-01 (free-tier copy purge from FAQ, About, schema, OG description), B-02 (Magnific text-only mentions in supported-sites copy on FAQ + About + welcome i18n — no partner logo), B-03 (`/en/about` EN sibling — currently RU-only `/about` from Phase 4 has no EN sibling and the lang switcher can't escape the page).
4. **One build-time gate addition** — `scripts/verify-faq-mainentity.mjs` to enforce V-10 parity (visible FAQ `<dt>` text ≡ `FAQPage` JSON-LD `mainEntity` set), wired into `npm run build` after `prerender.mjs`. Hard fail if drift detected.
5. **One refactor (Wave 12a)** — extract `AboutPage.tsx` body strings into `src/content/about.ts` data structure (mirrors `src/content/guides/gpt-image-2.ts` pattern) as the prerequisite for the B-03 EN sibling. No functional change.

**Explicitly NOT in this phase:**

- **LCP ≤ 2.5 s on `/` mobile** (V-18/V-19) — current floor is 3.5 s per the 2026-05-17 PSI run; Unbounded font subsetting is the next likely lever. Deferred per the 04-LCP-AUDIT Option-1 lock-in to a dedicated perf ticket. Phase 4.1 must NOT regress LCP further but is not responsible for closing the gap.
- **`/refund` description i18n deduplication** (CR-02 long-term fix) — the immediate manifest-vs-page drift was already fixed in commit `eee40f6` during Phase 4. The deduplication into a single source of truth is a quality-of-life refactor; doesn't block 4.1.
- **`/guides` index page** — explicitly REJECTED for 4.1. With one guide an index page adds maintenance without UX value. Revisit when the second guide ships. Phase 4.1 instead collapses the 3-level breadcrumb on guide routes to 2 levels (root → article) to remove the soft-404 risk.
- **PricingStaticBlock recreation, runtime CTA rail, or any CTA pattern change on `/pay`** — explicitly REJECTED. The user rejected the static-only PricingStaticBlock pattern in plan 04-04 as a UX regression (`f9dfdb1` revert). The runtime-conditional CTA (SSR renders Chrome Store anchor; `extStatus === "ready"` hydration swap to Pay button) is the locked pattern. Wave 9 MUST preserve it.
- **Any change to `EXTENSION_IDS`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`** — locked by INTEGRATION-CONTRACT, mirrored in the extension repo.
- **Any change to the synchronous Paddle CDN `<script>` injection on `/pay` + `/en/pay`** — locked by INTEGRATION-CONTRACT §6.
- **Schema additions, new JSON-LD types, additional sameAs URLs** — Phase 5 (Brand authority) scope.
- **Adding a Magnific partner logo to landing** — explicitly REJECTED per user 2026-05-17. B-02 is text-only mentions in copy. The `PartnerLogo` union stays `"syntx" | "freepik" | "higgsfield" | "canva"`.

</domain>

<spec_lock>
## Locked Requirements (from 04.1-CAPTURED-DECISIONS.md)

Three user-confirmed decisions (2026-05-17 chat) drive the scope. Do NOT re-ask:

1. **B-03 disposition** → Ship full `/en/about` EN sibling. Claude translates body; user reviews EN copy before commit (Wave 12b approval gate).
2. **Pricing reality (B-01)** → Pro tier only. Subscription: 199₽/мес via YooKassa, $2.99/мес via Paddle. One-time: 299₽ via YooKassa, $4.99 via Paddle. There is NO free checks tier. Install + registration stay free (factually true — not a usage claim).
3. **B-02 placement** → Magnific is mentioned text-only alongside Freepik in supported-sites copy (FAQ Q1+Q5, About, welcome i18n). No partner logo. Freepik mention stays for SEO continuity (old brand name still has search volume; Magnific was acquired by Freepik in 2024).

</spec_lock>

<decisions>
## Implementation Decisions

### Baseline preservation (the hard wall)

- **`/pay` and `/en/pay` `prerender: "full"` flag** in `scripts/seo-routes.ts:247, 484` STAYS. Update the stale "Phase 4 D-12" comment to reflect the post-revert reality: `// Phase 4 D-12 (post-revert f9dfdb1): full prerender keeps the runtime-conditional pricing cards in initial HTML with visible prices, so Product schema validates against visible content.`
- **`productBlock(...)` call** on `/pay` + `/en/pay` STAYS. Only the `{ name: "Free tier", price: "0", currency: "USD" }` ENTRY inside its array is removed (Wave 2 + Wave 10). The sibling `ORG_BLOCK` and `breadcrumbBlock(...)` entries STAY.
- **Pricing cards in initial HTML** STAY — both cards keep rendering server-side with visible `199₽` / `299₽` / `$2.99` / `$4.99` + features (required by Google Rich Results "schema must reflect visible content" policy).
- **PayPage runtime-conditional CTA pattern** STAYS — `extStatus === "ready"` → real Pay button; else → Chrome Store anchor. SSR always renders the Chrome Store anchor branch (`extStatus = "detecting"`); hydration swap is intentional and hydration-safe.
- **Eager-import of PayPage** in `main.tsx` + `entry-server.tsx` STAYS — required for SSR; do NOT lazy-import.
- **SSR-safe `useState` for currency state** in PayPage STAYS — `typeof window` guard; removing it crashes `renderToString`.
- **Paddle CDN sync `<script>` injection** on `/pay` + `/en/pay` STAYS — `applyPaddleScript()` in `scripts/prerender.mjs:175-180` is INTEGRATION-CONTRACT §6 territory.

### Wave-level decisions

- **Wave 1 (`scripts/prerender.mjs`)** — Add `applyOgImage(html, meta)` helper that swaps `<meta property="og:image">` AND `<meta name="twitter:image">` to `meta.ogImage ?? DEFAULT_OG_IMAGE` with URL value escaping. Call from the route loop. Remove the obsolete line-226 comment ("ogImage is read but not currently re-injected…"). Extract the hardcoded font preload checks into a `REQUIRED_FONT_PRELOADS` array constant at module top; build the regexes from it.
- **Wave 2 (`scripts/seo-routes.ts`)** — Drop the Free-tier entry from both `productBlock([...], ...)` calls on `/pay` + `/en/pay`. Update the "Phase 4 D-12" comment. Collapse the 3-level breadcrumb on both guide routes to 2 levels (root → article), removing the middle `{name: "Гайды"/"Guides", url: "/guides"}` step. Normalize and validate prices in `productBlock` at function entry: `Number(p.price)` → throw with file/line context if not finite.
- **Wave 3 (`scripts/llms.mjs`)** — After computing `outPath` from `meta.path`, assert `outPath.startsWith(DIST + sep) || outPath === resolve(DIST, "index.html")` and throw if it escapes. Reject any `meta.path` containing `..`, backslash, or absolute prefix at the input edge for defense in depth. Extend `extractText` to decode `&#nnn;` + `&#xHHHH;` numeric entities and strip any remaining `&name;` named entity rather than leaking it.
- **Wave 4 (`FaqBlock.tsx` + `seo-routes.ts`)** — Change `FaqBlock` props from `items: FaqItem[]` to `items: readonly FaqItem[]`. Change `faqPageBlock` signature in `scripts/seo-routes.ts` from `items: { q: string; a: string }[]` to `items: readonly { q: string; a: string }[]`. Build will reveal whether tsconfig strict mode was masking the variance check.
- **Wave 5 (`scripts/verify-faq-mainentity.mjs` new)** — Reads every prerendered route that carries a FAQPage block; parses both the `<script type="application/ld+json">` mainEntity questions and the visible `<dt>` contents of FaqBlock; throws with route + diff if the two sets disagree. Wire into `npm run build` after `node scripts/prerender.mjs`. Manual smoke: briefly break one FAQ source (change Q in `src/content/landingFaq.ts` without touching schema input) — build must fail with clear error pointing at the route; revert.
- **Wave 6 (`App.tsx` + `ru.json` + `en.json`)** — IN-01 Pricing heading: chose **Option B** — keep accent split. Extract three keys (`pricing.heading.lead`, `pricing.heading.accent`, `pricing.heading.tail`) and compose them in JSX. Delete the unused `pricing.heading` flat key from both dicts. IN-04: Rename `id="privacy"` on the trust section to `id="trust"`. Verify no navbar anchor uses `#privacy` via grep on `src/app`; the legal `/privacy` route is unaffected.
- **Wave 7 (`AboutPage.tsx`)** — Move the `.about-content h2/p/ul/li/a` rules into Tailwind utility classes applied directly to rendered nodes. Delete the inline `<style>` block. Where the same combination repeats, accept a longer className string rather than introducing a custom Tailwind component (no new abstraction — matches CLAUDE.md "Don't add features beyond what the task requires").
- **Wave 8 (`scripts/prerender.mjs`)** — Defense-in-depth Route assert. After the SSR import is resolved, fail fast if any `routes.filter(r => r.prerender === "full").map(r => r.path)` produces a body shorter than a tiny threshold (200 chars). Cheap proxy for "the React tree mounted something" — if `entry-server.tsx` is missing a `<Route>` for a "full" path, prerender today silently emits the fallback; the guard catches it. Note: the assert belongs in `prerender.mjs` (the consumer), not in `entry-server.tsx` (the producer).
- **Wave 9 (`PayPage.tsx`)** — **APPROVAL GATE — payment surface.** Two changes ONLY: (a) Add `resolved` flag inside `detectExtension`; once any extension ID returns a positive result, subsequent callbacks no-op. No change to message shape, EXTENSION_IDS, retry/timeout behavior, or CTA branch selector. (b) Replace hardcoded `<a href="/#faq">` with computed `const homeHash = lang === "en" ? "/en/#faq" : "/#faq";` Same for any other `<a href="/#…">` on PayPage. Expected PR diff: ~20 lines. If the diff grows past ~40 lines, something is out of scope — stop and re-plan.
- **Wave 10 (Free tier copy purge — B-01)** — Rewrite `landingFaq.ts` Q2 in both RU and EN. New Q2 RU heading: "Сколько стоит Opten?"; answer drops "бесплатно 10 проверок", describes Pro pricing only (199₽/мес или 299₽ разово; $2.99/мес или $4.99 разово), keeps the PromptPerfect comparison. EN sibling rewrites symmetrically. Rewrite the "По цене" paragraph in AboutPage (will move to `src/content/about.ts` in Wave 12a — order W10 → W12a means edit `about.ts` after W12a lands, OR edit `AboutPage.tsx` now and let W12a carry the new copy). Delete `pricing.free.*` block from both dicts (8 keys × 2 files = 16 keys) — `App.tsx` already only renders Pro Subscription + Pro One-time cards. Drop the Free-tier baseline `Offer` from `scripts/seo-routes.ts:100` SoftwareApplication `offers` array. Reword RU `/pay` `ogDescription` to drop "Начни бесплатно" tail.
- **Wave 11 (Magnific mentions — B-02)** — Insert "Magnific" alongside Freepik in `landingFaq.ts` Q1 + Q5 (RU + EN). Same in `ru.json:208,212` + `en.json:205,209` welcome step3 desc + welcome.also. Same in `AboutPage.tsx:68,102` (or in `src/content/about.ts` after W12a). Verify with `grep -ic "magnific"` ≥1 on `dist/index.html`, `dist/en/index.html`, `dist/about/index.html`, `dist/welcome/index.html`, `dist/en/welcome/index.html`. Text-only — no new partner logo.
- **Wave 12a (extract AboutPage body)** — Create `src/content/about.ts` mirroring `src/content/guides/gpt-image-2.ts` pattern. `aboutContent.ru` gets the body strings; `aboutContent.en` stays empty/null for now. Refactor `AboutPage.tsx` to consume `aboutContent[lang]` via `useLang()`. Decision: keep inline JSX for embedded links/`<strong>` in the data structure rather than building an AST renderer — simpler approach per CLAUDE.md "no new abstractions". Visual diff should be byte-for-byte the same on RU `/about`.
- **Wave 12b (`/en/about` sibling — B-03)** — **APPROVAL GATE — new product surface.** Translate all RU body into EN (`aboutContent.en`), ~1500 words, first-person founder voice, no marketing puffery — same register as RU. User vets EN copy before commit. Add `/about` to `EN_SIBLINGS` in `src/i18n/paths.ts`. Add `<Route path="/en/about" element={<AboutPage />} />` to `src/main.tsx` + `scripts/entry-server.tsx`. Add `/en/about` RouteMeta entry to `scripts/seo-routes.ts` (full prerender, `htmlLang: "en"`, EN ogImage, hreflang reciprocal). Update RU `/about` `hreflangAlternates` to include EN sibling. Verify dict keys (`t("about.tagline")`, `t("about.contactCta")`, `t("about.backLink")`, `t("nav.about")`) exist in EN dict — add if missing. All internal links inside `aboutContent.en` use `<LocalizedLink>` and resolve to EN siblings where possible (`/en/terms`, `/en/refund`). Sitemap reciprocity comes free from the manifest.

### Wave ordering & risk

- Low-risk-first ordering — any rollback in late waves doesn't require unwinding earlier ones. Sequence: W1 → W2 → W3 → W4 → W5 → W6 → W7 → W8 → W9 (approval) → W10 → W11 → W12a → W12b (approval).
- Parallel candidates inside the planner's purview: W1+W2+W3 touch different files and can ship in parallel; W4 depends on `FaqBlock.tsx` typing; W5 depends on W4's readonly types being merged; W6 is independent; W7 is independent; W8 is independent. The planner may regroup waves into 2-3 parallel "batches" if it's safe.
- W9 + W12b carry hard approval gates — extension-coupled / new product surface per autonomy memory.
- W10 + W11 + W12a + W12b form a logical group (B-01/B-02/B-03 user bugs) and may benefit from being adjacent in execution to allow inspecting the final copy in one pass.

### Approval gates (explicit pauses)

- **Before Wave 9 commit** — `/pay` and `/en/pay` are extension-coupled payment surface. Show user the ~20-line diff for `detectExtension` + the FAQ-anchor change; confirm baseline preserved (cards intact, Paddle script intact, SSR CTA branch intact). Diff growing past ~40 lines triggers a stop-and-replan.
- **Before Wave 12b commit** — `/en/about` is new product surface. Show user the proposed EN body translation in `aboutContent.en` for review BEFORE running `npm run build`; let user redline copy before commit lands. Diff size is large (~1500 words EN translation) but that's expected — the gate is on EN copy quality, not file count.

### Verification at phase close

1. `npm run build` clean — no warnings, no broken assertions, no missing files; bundle sizes within ±5% of pre-4.1 baseline.
2. Re-run the 6 post-deploy curl checks from `04-HUMAN-UAT` (X-Frame-Options, Content-Signal, llms.txt MIME) on the new production build — must all stay green.
3. Smoke `/pay` and `/en/pay` end-to-end with real extension (Wave 9 verification) — single human check; extension installed + logged in → "ready" / Pay button; missing → Chrome Store anchor.
4. Spot-check EN OG card via Telegram link unfurl on `/en/`, `/en/welcome`, `/en/privacy`, `/en/terms`, `/en/refund`, `/en/pay` — must now show `og-card-en.png`, not the RU card.
5. `grep -ri "10 проверок\|10 generations\|free tier\|бесплатн.* проверк" dist/` returns 0 hits in any rendered HTML / sitemap / llms-full.
6. `grep -ic "magnific" dist/index.html dist/en/index.html dist/about/index.html dist/welcome/index.html dist/en/welcome/index.html` returns ≥1 for each.
7. `dist/en/about/index.html` exists with EN body content, `<html lang="en">`, reciprocal hreflang triplets.
8. LangSwitcher round-trip: RU `/about` → EN `/en/about` → RU `/about` works.

A formal `/gsd-verify-work 4.1` run is optional given the surgical nature of this phase; the per-wave Verify steps above plus a final manual smoke on WR-04/IN-02 (Wave 9) and B-03 (Wave 12b) cover what matters.

</decisions>

<canonical_refs>
## Canonical References (downstream agents MUST read)

| Path | Why it matters |
|------|---------------|
| `.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md` | **PRIMARY SOURCE.** Original 530-line seed with 18-finding inventory, ⛔ BASELINE section, per-wave change descriptions, verification steps, and rationale for every wave. This CONTEXT.md is a structured extraction; downstream agents should refer to the seed for the per-wave technical detail. |
| `.planning/phases/04-content-surface/04-REVIEW.md` | The 18 findings (CR-01, CR-02, WR-01..09, IN-01..06) originated here. Each wave is keyed to one or more finding IDs. |
| `docs/INTEGRATION-CONTRACT.md` | §5 (`ps_*` storage keys read indirectly), §6 (Paddle CDN injection on `/pay` + `/en/pay`), §EXTENSION_IDS list. Wave 9 must not violate. |
| `docs/ARCHITECTURE.md` | Per-route prerender + EN sibling routing model that Wave 12b extends. |
| `CLAUDE.md` | Locked routes list, EXTENSION_IDS duplication points, project conventions ("Don't add features beyond what the task requires" — invoked by W7 and W12a to reject new abstractions). |
| `.planning/phases/04-content-surface/04-CONTEXT.md` | Phase 4 decisions including D-12 (PayPage full prerender promotion), D-14 (X-Frame-Options + Content-Signal), D-08 (FAQ source-of-truth pattern that V-10 gate enforces). |
| `.planning/phases/04-content-surface/04-HUMAN-UAT.md` | The 6 post-deploy curl checks that must keep passing after 4.1 ships. |
| `.planning/codebase/STRUCTURE.md` | File layout reference for `src/content/`, `src/i18n/`, `scripts/`. |
| `.planning/codebase/INTEGRATIONS.md` | Paddle + Supabase coupling map for Wave 9 risk assessment. |
| `src/content/guides/gpt-image-2.ts` | **Pattern reference** for Wave 12a's `src/content/about.ts` extraction. Same shape (per-language objects with body sections). |
| Commit `f9dfdb1` (`revert(04-04): drop PricingStaticBlock + runtime CTA rail, restore inline cards with runtime CTAs`) | **BASELINE COMMIT.** Defines the `/pay` post-revert state Wave 9 must preserve. `git show f9dfdb1` for the canonical diff. |
| Commit `eee40f6` | The CR-01 + CR-02 fix from Phase 4 — already in `main`; Wave-2-touch on `seo-routes.ts` must not regress the `/refund` description anchored by this commit. |

</canonical_refs>

<code_context>
## Reusable Assets & Patterns

### Existing patterns (mirror these — don't reinvent)

- **i18n data extraction** — `src/content/guides/gpt-image-2.ts` defines a per-language object literal with structured sections. Wave 12a (`src/content/about.ts`) mirrors this exactly. Same shape, same import style.
- **EN_SIBLINGS path declaration** — `src/i18n/paths.ts` is the single source of truth for which routes get `/en/*` siblings. Wave 12b adds `/about` to this list; the LangSwitcher / hreflang generators read from it automatically.
- **Route-meta manifest** — `scripts/seo-routes.ts` `RouteMeta` interface already supports `htmlLang`, `ogImage`, `prerender: "full" | "head"`, `schema`, `hreflangAlternates`. Wave 12b adds one entry; no schema extension needed.
- **Prerender helpers** — `scripts/prerender.mjs` already has `applyTitle`, `applyDescription`, `applyCanonical`, `applyHreflang`, `applyHtmlLang`, `applyOgLocale`, `applyPaddleScript`, `applyJsonLd`, `applyHeroPreload`. Wave 1 adds `applyOgImage` to this family. Same call-site pattern in the route loop.
- **JSON-LD block factories** — `scripts/seo-routes.ts` has `productBlock`, `breadcrumbBlock`, `faqPageBlock`, `howToBlock`, `personBlock`, plus the module-level `ORG_BLOCK` and `SOFTWARE_APPLICATION_BASE`. Wave 2 + Wave 10 modify inputs to these factories; no new factories needed.
- **FaqBlock component** — `src/app/components/FaqBlock.tsx` is the rendered counterpart to `faqPageBlock` schema. Wave 4 tightens its prop type to `readonly FaqItem[]`. Wave 5's new build gate parses both sources and asserts equality.
- **Build-time gate pattern** — `scripts/prerender.mjs` is already invoked from `npm run build` and throws on failure. Wave 5's new `scripts/verify-faq-mainentity.mjs` follows the same convention (Node script throws non-zero on failure, build halts). Wave 8 adds an inline assert inside `prerender.mjs`.
- **`<LocalizedLink>`** in `src/app/components/LocalizedLink.tsx` — Wave 12b's `aboutContent.en` internal links use this for EN-sibling preservation (not bare `<Link>`, which drops the `/en/` prefix per CLAUDE.md guidance).
- **i18n key namespacing** — `pricing.*`, `about.*`, `welcome.*` per CLAUDE.md convention. Wave 6's three new `pricing.heading.{lead,accent,tail}` keys follow this. Wave 10's `pricing.free.*` deletions also.

### Files touched per wave (planner reference)

| Wave | Files | Type |
|------|-------|------|
| 1    | `scripts/prerender.mjs` | edit |
| 2    | `scripts/seo-routes.ts` | edit |
| 3    | `scripts/llms.mjs` | edit |
| 4    | `src/app/components/FaqBlock.tsx`, `scripts/seo-routes.ts` | edit |
| 5    | `scripts/verify-faq-mainentity.mjs` (NEW), `package.json` (build script chain) | create + edit |
| 6    | `src/app/App.tsx`, `src/i18n/ru.json`, `src/i18n/en.json` | edit |
| 7    | `src/app/pages/AboutPage.tsx` | edit |
| 8    | `scripts/prerender.mjs` | edit |
| 9    | `src/app/pages/PayPage.tsx` | edit (APPROVAL) |
| 10   | `src/content/landingFaq.ts`, `src/app/pages/AboutPage.tsx` (or `src/content/about.ts` if W12a lands first), `src/i18n/ru.json`, `src/i18n/en.json`, `scripts/seo-routes.ts` | edit (cross-file copy purge) |
| 11   | `src/content/landingFaq.ts`, `src/i18n/ru.json`, `src/i18n/en.json`, `src/app/pages/AboutPage.tsx` (or `src/content/about.ts`) | edit (cross-file copy insertion) |
| 12a  | `src/content/about.ts` (NEW), `src/app/pages/AboutPage.tsx` | create + refactor |
| 12b  | `src/content/about.ts`, `src/i18n/paths.ts`, `src/main.tsx`, `scripts/entry-server.tsx`, `scripts/seo-routes.ts`, `src/i18n/{ru,en}.json` (verify dict keys) | edit (APPROVAL) |

### File-ownership conflicts to manage

- `scripts/seo-routes.ts` is touched by Waves 2, 4, 10, 12b — planner should sequence these or wave them into separate atomic commits.
- `scripts/prerender.mjs` is touched by Waves 1 and 8 — can be one commit if done together (similar surface area) or split for clarity.
- `src/app/pages/AboutPage.tsx` is touched by Waves 7, 10, 11, 12a — Wave 12a's refactor is the load-bearing one; ordering preference is W7 → (W10,W11 if before W12a) → W12a → (W10,W11 if after W12a). The captured-decisions seed allows either order; planner should pick whichever minimizes total edits.

</code_context>

<specifics>
## Specific user-confirmed answers (do not re-ask)

### B-01: Pricing reality (2026-05-17 chat)
- Pro Monthly: 199₽ (YooKassa) / $2.99 (Paddle)
- Pro One-time: 299₽ (YooKassa) / $4.99 (Paddle)
- No free checks tier (install + registration stay free — factually true)
- All free-tier copy to be purged from FAQ, About, schema offers, OG description
- "Скачай бесплатно. Перейди на Pro" (App.tsx:430-431) and `cta.freeLabel` "Установка и регистрация бесплатно" STAY — refer to install being free, not usage

### B-02: Magnific placement (2026-05-17 chat)
- TEXT-ONLY mentions in FAQ Q1 + Q5 (RU + EN), About, welcome step3 i18n
- No partner logo addition
- Phrasing: "Freepik (включая Magnific)" or similar — keeps Freepik mention for SEO continuity (old brand name still searched)
- Rationale: Magnific was acquired by Freepik in 2024 and operates under their umbrella

### B-03: EN /about (2026-05-17 chat)
- Ship full EN sibling at `/en/about`
- Claude (assistant) translates body; user reviews EN copy BEFORE commit (W12b approval gate)
- Tone: first-person, founder voice, no marketing puffery — match RU register
- Approximately 1500 words

### IN-01: Pricing heading i18n approach (resolved in seed)
- **Option B chosen** — extract three keys (`pricing.heading.lead`, `pricing.heading.accent`, `pricing.heading.tail`) and compose in JSX
- Preserves the `<Accent>` design
- Delete unused flat `pricing.heading` key from both dicts

### W12a: AboutPage content extraction approach (resolved in seed)
- Keep inline JSX for embedded links + `<strong>` in the data structure
- Reject the AST renderer alternative — simpler approach per CLAUDE.md "no new abstractions"

### Out-of-scope dispositions (resolved in seed)
- LCP work → separate perf ticket (Unbounded font subsetting); Phase 4.1 only must not regress LCP further
- `/refund` description i18n dedup → quality-of-life refactor; not blocking
- `/guides` index page → REJECTED; revisit when second guide ships

</specifics>

<deferred>
## Deferred Ideas

Captured for future phases, not in scope for 4.1:

- **LCP ≤ 2.5 s on `/` mobile** — Unbounded font subsetting; current floor 3.5 s per 2026-05-17 PSI. Dedicated perf ticket per `04-LCP-AUDIT` Option-1 lock-in.
- **`/refund` description i18n deduplication** — single source consumed by both manifest and page body. CR-02 immediate fix already in `eee40f6`; dedup is a quality-of-life refactor.
- **`/guides` index page** — when a second guide ships. One-guide index has poor UX.
- **Additional guides (3–5 more)** — Phase 4.1 was originally going to add these after the anchor guide proves the format; instead deferred to a future "content expansion" phase or merged into Phase 5 brand work.
- **Magnific partner logo on landing** — REJECTED for 4.1 per user; revisit if data shows logo placement drives Magnific-search-traffic conversions.
- **Customer testimonials / Chrome Web Store rating embed** — Phase 5 territory.
- **Author bylines on landing** — out of scope; landing copy stays as is.
- **`speakable` schema property** — defer; not commonly cited.
- **`/en/guides/*` additions beyond the anchor** — when more guides ship.
- **Content review of existing copy for AI-generated tells** — could leverage `humanizer` skill if E-E-A-T audit later flags AI-tells in the founder voice.

</deferred>
