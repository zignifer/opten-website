---
phase: 04.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/llms.mjs
autonomous: true
requirements:
  - WR-06
  - WR-07
must_haves:
  truths:
    - "scripts/llms.mjs rejects any meta.path containing '..', backslash, or absolute prefix at input edge"
    - "scripts/llms.mjs asserts that the computed outPath stays inside DIST (or equals DIST/index.html) before reading the file"
    - "scripts/llms.mjs extractText decodes &#nnn; and &#xHHHH; numeric HTML entities + strips any remaining named &name; entity rather than leaking it as raw text"
    - "dist/llms-full.txt contains no raw '&<alphanumeric>;' substrings (every entity is decoded or stripped)"
    - "llms.txt and llms-full.txt still emit at end of build"
  artifacts:
    - path: "scripts/llms.mjs"
      provides: "Path-traversal guard at the meta.path input edge + outPath escape check"
      contains: "startsWith"
    - path: "scripts/llms.mjs"
      provides: "extractText extended with numeric entity decoder + named-entity stripper"
      contains: "&#"
  key_links:
    - from: "bodyForRoute"
      to: "outPath escape assertion"
      via: "startsWith(DIST + sep) || outPath === resolve(DIST, 'index.html') guard before readFile"
      pattern: "startsWith"
    - from: "extractText"
      to: "numeric + named entity handlers"
      via: ".replace chain for &#nnn;, &#xHHHH;, and final &[a-zA-Z][a-zA-Z0-9]*; stripper"
      pattern: "&#"
---

<objective>
Close WR-06 (path traversal in scripts/llms.mjs) and WR-07 (HTML entity decoder only covers 5 entities) with one atomic commit. Defensive hardening of an existing postbuild script — no behavioral change to the happy path, just guards for inputs that should never occur during normal operation.

Purpose: WR-06 — `meta.path` is sourced from the SSR-bundled manifest, but if a future edit injects an unexpected `meta.path` value (e.g., `"../../etc/passwd"`), `resolve(DIST, "../../...")` would silently write outside `dist/`. A `startsWith(DIST + sep)` guard catches this at the file-write boundary. WR-07 — `extractText` currently decodes only `&nbsp;`, `&amp;`, `&lt;`, `&gt;`, `&quot;`, `&#39;`. Any future entity (e.g., `&hellip;`, `&mdash;`, `&#8217;`) leaks into `dist/llms-full.txt` as literal text, polluting LLM context. Broader handling: decode numeric entities (`&#nnn;`, `&#xHHHH;`), strip any remaining named entity (`&name;`) rather than leak.
Output: One commit adding a path-traversal guard to `bodyForRoute` and broadening the entity decoder in `extractText`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@scripts/llms.mjs

<interfaces>
From `scripts/llms.mjs` (existing — TO EDIT):

- Lines 11-14: module constants. `DIST = resolve(ROOT, "dist")` (used as the escape boundary).
- Lines 68-82: `function extractText(html)` — current decode chain: `&nbsp;` → space, `&amp;` → `&`, `&lt;` → `<`, `&gt;` → `>`, `&quot;` → `"`, `&#39;` → `'`. WR-07 extends this chain.
- Lines 84-99: `async function bodyForRoute(meta)` — computes `outPath` via `resolve(DIST, meta.path.replace(/^\//, ""), "index.html")` (lines 88-90, with `/` and `/en/` special-cased). WR-06 fix: assert escape boundary at this point.
- Add `import { sep } from "node:path"` (currently `dirname`, `resolve` are imported on line 8); `sep` is the platform path separator needed for the `startsWith(DIST + sep)` guard on Windows + POSIX.
- Special case from the existing code: `outPath` for `/` is `resolve(DIST, "index.html")` (no subdirectory). Guard must allow this exact path; the `startsWith(DIST + sep)` check would reject it (because it equals DIST + "/index.html" with no internal separator after DIST). Express the guard as: `outPath === resolve(DIST, "index.html") || outPath.startsWith(DIST + sep)`.
- WR-06 defense-in-depth: ALSO reject the input at the EDGE — before computing outPath. Check `meta.path` for `..` substring, backslash (`\\`), or absolute-prefix indicators (`/^[a-zA-Z]:/` for Windows drives, `/^\//.test(meta.path)` is fine because all valid paths start with `/` — distinguish "leading single slash" from "absolute disk path"). Easiest: reject any `meta.path` matching `/\.\.|\\/`. Throw with a clear error: `llms.mjs: refusing suspicious meta.path "<value>" (contains '..' or backslash — possible path traversal)`.

WR-07 implementation notes:
- Numeric decimal entity `&#nnn;`: `replace(/&#(\d+);/g, (_, n) => String.fromCodePoint(Number(n)))`. Validate via `Number(n)` finite-and-positive — if not, leave as literal? Per the seed's intent, this is defensive — even invalid numeric entities should be stripped, not leaked. Implementation: if `Number.isFinite(n) && n <= 0x10FFFF` decode, else strip.
- Numeric hex entity `&#xHHHH;` / `&#XHHHH;`: `replace(/&#x([0-9a-fA-F]+);/gi, (_, h) => String.fromCodePoint(parseInt(h, 16)))`. Same finite-validation guard.
- Catch-all named entity stripper `&[a-zA-Z][a-zA-Z0-9]*;`: replace with empty string. This must run AFTER all the explicit named-entity decoders (`&nbsp;`, `&amp;`, `&lt;`, `&gt;`, `&quot;`, `&#39;`) so the explicit decoders win. Note: `&#39;` is numeric, not named — it is already covered by the new numeric decoder, so the existing `&#39;` line can either stay (no harm — runs first) or be deleted (cleaner). Pick the cleaner approach: KEEP the existing five named decoders + the existing `&#39;` line (back-compat with the historical chain), add the numeric decoders BEFORE the catch-all stripper, add the catch-all stripper LAST in the entity chain (before the `\\s+` whitespace collapse).

Final extractText chain order (after edits):
1. `<script>` strip
2. `<style>` strip
3. `<!-- -->` strip
4. `<[^>]+>` strip
5. `&nbsp;` → space (existing)
6. `&amp;` → `&` (existing)
7. `&lt;` → `<` (existing)
8. `&gt;` → `>` (existing)
9. `&quot;` → `"` (existing)
10. `&#39;` → `'` (existing)
11. **NEW** numeric decimal entity decoder (`&#nnn;`)
12. **NEW** numeric hex entity decoder (`&#xHHHH;`)
13. **NEW** catch-all named entity stripper (`&[a-zA-Z][a-zA-Z0-9]*;`)
14. `\\s+` collapse (existing)
15. `trim()` (existing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add path-traversal guard + broaden HTML entity decoder in scripts/llms.mjs</name>
  <files>scripts/llms.mjs</files>
  <read_first>
    - scripts/llms.mjs (the full file — 128 lines)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 3: scripts/llms.mjs — traversal guard + HTML entity decoder"
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 3 (scripts/llms.mjs)"
  </read_first>
  <action>
    Two surgical edits to `scripts/llms.mjs` in one atomic commit:

    (a) **Add `sep` to the `node:path` import on line 8.** Change `import { dirname, resolve } from "node:path";` to `import { dirname, resolve, sep } from "node:path";`.

    (b) **WR-06 path-traversal guard in `bodyForRoute`.** Modify `async function bodyForRoute(meta)` (line 84):
    - At the very top of the function body, add an input-edge rejection: `if (meta.path.includes("..") || meta.path.includes("\\\\")) { throw new Error('llms.mjs: refusing suspicious meta.path "' + meta.path + '" (contains \\'..\\'or backslash — possible path traversal)'); }`
    - After the existing `outPath` computation (currently lines 85-90), but BEFORE the `try { html = await readFile(outPath, "utf-8"); }` block, add the escape-boundary assertion: `if (outPath !== resolve(DIST, "index.html") && !outPath.startsWith(DIST + sep)) { throw new Error('llms.mjs: outPath "' + outPath + '" escapes DIST "' + DIST + '" for meta.path "' + meta.path + '"'); }`
    - Both guards throw; the existing try/catch around readFile only catches missing-file errors, not these — the throws propagate out and fail the build (correct behavior — better than silently writing to a wrong location).

    (c) **WR-07 entity decoder broadening in `extractText`.** Modify `function extractText(html)` (line 68). After the existing `&#39;` → `'` line (currently line 79) and BEFORE the `\\s+` collapse line (currently line 80), insert three new `.replace()` calls:
    1. Numeric decimal entity decoder:
       `.replace(/&#(\\d+);/g, (_, n) => { const num = Number(n); if (!Number.isFinite(num) || num < 0 || num > 0x10FFFF) return ""; try { return String.fromCodePoint(num); } catch { return ""; } })`
    2. Numeric hex entity decoder:
       `.replace(/&#x([0-9a-fA-F]+);/gi, (_, h) => { const num = parseInt(h, 16); if (!Number.isFinite(num) || num < 0 || num > 0x10FFFF) return ""; try { return String.fromCodePoint(num); } catch { return ""; } })`
    3. Catch-all named entity stripper:
       `.replace(/&[a-zA-Z][a-zA-Z0-9]*;/g, "")`

    All three chain into the existing fluent `.replace().replace()...` sequence — preserve the chain shape; just insert three new lines. Keep the existing `&#39;` → `'` line intact (the new numeric decimal decoder would also match it, but the existing line runs first so its behavior is preserved — harmless overlap).

    DO NOT touch the section grouping (`SECTIONS` array), the `SIZE_CAP_BYTES` legal-truncation logic, the prerendered-routes floor check (line 21), or the file-write/console output at the bottom.
  </action>
  <verify>
    <automated>npm run build && test -f dist/llms.txt && test -f dist/llms-full.txt && grep -q "sep" scripts/llms.mjs && grep -qE "&#\\(\\\\d+\\);|&#\\(\\[0-9a-fA-F\\]" scripts/llms.mjs && ! grep -qE '&[a-zA-Z][a-zA-Z0-9]+;' dist/llms-full.txt</automated>
  </verify>
  <done>
    `scripts/llms.mjs` imports `sep` from `node:path`, has a top-of-function path-traversal guard rejecting `..` or `\\` in `meta.path`, has an outPath-escape assertion after the outPath computation, and has three new entity decoder/stripper `.replace()` calls in `extractText` (numeric decimal, numeric hex, named-entity catch-all). `npm run build` succeeds and emits both `dist/llms.txt` + `dist/llms-full.txt`. `dist/llms-full.txt` contains no surviving `&<alphanumeric>;` substrings — every named entity is either decoded by an explicit handler or stripped by the catch-all. Manual smoke: temporarily change a guide RouteMeta entry path to `"../escaped"` and confirm `npm run build` fails with the suspicious-path error; revert.
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. `dist/llms.txt` and `dist/llms-full.txt` both exist and are non-trivial in size.
  3. `grep -E '&[a-zA-Z][a-zA-Z0-9]*;' dist/llms-full.txt` returns 0 matches.
  4. Manual smoke: meta.path = `"../escaped"` in a temporary test build fails with the expected error message (revert after).
  5. No behavior change to llms.txt grouping, section ordering, or size-cap truncation logic.
</verification>

<success_criteria>
  1. WR-06 closed — path traversal guards land at both the input edge (meta.path) and the boundary (outPath).
  2. WR-07 closed — extractText broadly handles numeric entities and strips any remaining named entity.
  3. No regression — llms.txt + llms-full.txt still generate at end of build; content shape preserved for happy path.
  4. Defensive only — no behavior change for valid manifest inputs.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-03-SUMMARY.md` covering:
  - Diff line count for `scripts/llms.mjs`.
  - Confirmation that the grep against `dist/llms-full.txt` for surviving entities returns 0 hits.
  - Sample of the smoke test (suspicious-path build failure + revert).
  - Confirmation that llms.txt route count and section grouping is unchanged from pre-4.1 baseline (`grep -c '##' dist/llms.txt` ≥ 6).

  Commit message: `fix(04.1): harden llms.mjs — path-traversal guard + broader HTML entity decoder (WR-06, WR-07)`
</output>
</content>
</invoke>