---
phase: 04.1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/App.tsx
  - src/i18n/ru.json
  - src/i18n/en.json
autonomous: true
requirements:
  - IN-01
  - IN-04
must_haves:
  truths:
    - "Landing Pricing heading is composed from three i18n keys (pricing.heading.lead, pricing.heading.accent, pricing.heading.tail) in BOTH RU and EN — no hardcoded RU/EN strings inline in App.tsx Pricing function"
    - "<Accent>...</Accent> wrap is preserved on the accent portion of the heading"
    - "The flat unused `pricing.heading` key is removed from both ru.json and en.json"
    - "The trust section on the landing has id=trust (not id=privacy) — collision with /privacy route slug eliminated"
    - "No navbar anchor uses #privacy; #pricing and #faq still scroll correctly"
  artifacts:
    - path: "src/i18n/ru.json"
      provides: "Three new keys pricing.heading.lead/accent/tail in RU"
      contains: "pricing.heading.lead"
    - path: "src/i18n/en.json"
      provides: "Three new keys pricing.heading.lead/accent/tail in EN"
      contains: "pricing.heading.lead"
    - path: "src/app/App.tsx"
      provides: "Pricing() function uses t(pricing.heading.lead) + Accent(t(pricing.heading.accent)) + t(pricing.heading.tail); trust section id renamed from privacy to trust"
      contains: "pricing.heading.lead"
  key_links:
    - from: "Pricing() function SectionTitle JSX"
      to: "three i18n keys composed in order"
      via: "t() calls + <Accent> wrap on the middle key"
      pattern: "pricing.heading.accent"
    - from: "trust section <section> tag"
      to: "id=trust attribute"
      via: "rename from id=privacy to id=trust on the trust block (around App.tsx line 393)"
      pattern: "id=\"trust\""
---

<objective>
Close IN-01 (Pricing heading hardcoded inline, bypasses i18n) and IN-04 (`id="privacy"` collides with `/privacy` route slug) with one atomic commit. Three files touched together — App.tsx + both i18n dicts must move in lockstep.

Purpose: IN-01 — `App.tsx` currently inlines the RU/EN pricing heading in JSX (lines 428-438), with `<Accent>` wrapping the middle word(s). This bypasses the i18n pipeline used everywhere else on the page; future copy translations require code changes. The existing flat `pricing.heading` key in both dicts is dead code (no `t("pricing.heading")` call exists). IN-04 — `<section id="privacy">` on the trust block creates a slug collision with the `/privacy` legal route. Renaming to `id="trust"` clears the collision; no navbar anchor references `#privacy` (confirmed earlier via grep).

Decision per CONTEXT D-Wave-6: **Option B** — keep the `<Accent>` design split. Three keys: `pricing.heading.lead`, `pricing.heading.accent`, `pricing.heading.tail`. Compose in JSX as `{t("pricing.heading.lead")}<Accent>{t("pricing.heading.accent")}</Accent>{t("pricing.heading.tail")}`. Delete the unused flat `pricing.heading` key.

Output: One commit modifying 3 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@src/app/App.tsx
@src/i18n/ru.json
@src/i18n/en.json

<interfaces>
From `src/app/App.tsx`:
- Line ~393: `<section id="privacy" className="bg-[#011417] px-5 py-[70px] md:py-24">` — TRUST/privacy block (per the comment at lines 387-388, this is the section originally mislabeled). Rename `id="privacy"` to `id="trust"`. Do NOT change className, py spacing, or surrounding `<h2>` / `<PrivacyItem>` content; the section's visual identity is preserved.
- Line ~418-459: `function Pricing()` — contains the hardcoded RU/EN heading split. Existing structure (RU branch lines 428-432): `<span className="md:hidden">Скачай бесплатно. <Accent>Перейди на Pro,</Accent> когда будешь готов.</span>` + a `md:inline` variant on lines 430-432 with `<br />` separators.
- Line ~422-423: `const isRu = lang === "ru"` is available; can use this branch logic for the three keys (which exist per-locale in the dicts).
- Existing `useT()` hook is already in scope (line 419: `const t = useT();`).
- Existing `<Accent>` component (defined somewhere in App.tsx — find via Grep) wraps a string-typed child with the brand accent color.

From `src/i18n/ru.json`:
- Line 61: `"pricing.heading": "Начни бесплатно. Перейди на Pro, когда будешь готов.",` — DELETE this key (unused dead code).
- Add three new keys with the natural RU phrasing split (matching the existing JSX text — "Скачай бесплатно. " / "Перейди на Pro," / " когда будешь готов." — note the spaces and punctuation distribution must produce the same rendered text when composed). Insert near the other `pricing.*` keys (group at lines 61-90 area; reserve a stable position right after line 61 where the flat key was). Suggested values:
  - `"pricing.heading.lead": "Скачай бесплатно. "`
  - `"pricing.heading.accent": "Перейди на Pro,"`
  - `"pricing.heading.tail": " когда будешь готов."`

From `src/i18n/en.json`:
- Line 61: `"pricing.heading": "Start free. Upgrade to Pro when you're ready.",` — DELETE.
- Add three EN keys matching the existing JSX text — "Start Free. " / "Upgrade To Pro" / " When You're Ready.":
  - `"pricing.heading.lead": "Start Free. "`
  - `"pricing.heading.accent": "Upgrade To Pro"`
  - `"pricing.heading.tail": " When You're Ready."`

Note: the existing JSX has both mobile (`md:hidden`) and desktop (`md:inline`) variants with `<br />` separators on the desktop variant. For Option B with i18n composition, keep the same dual-variant structure but use the i18n keys in both branches. The desktop variant inserts a `<br />` after `pricing.heading.lead` (`{t("pricing.heading.lead")}<br /><Accent>...</Accent><br />{t("pricing.heading.tail")}`), while the mobile variant doesn't. This preserves the design exactly — the i18n keys carry the same words; only the JSX whitespace/break differs between viewports.

Whitespace handling: the trailing space in `pricing.heading.lead` and the leading space in `pricing.heading.tail` are intentional to keep the composed text spacing identical to the current inline version. JSON requires explicit trailing/leading spaces inside the quotes. On the desktop variant where `<br />` separates the parts, the leading/trailing spaces in lead/tail are visually irrelevant (because of the line break) but harmless.

Grep gate for IN-04: confirm `grep -n '#privacy' src/app/**/*.tsx` returns 0 hits in the `<a href>` / `to=` form (only the renamed `id="trust"` should remain, plus any legitimate `/privacy` route reference for the legal page which is unrelated). The legal route `/privacy` continues to work — only the SECTION id is renamed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compose Pricing heading from i18n keys + rename trust section id + delete dead pricing.heading key</name>
  <files>src/app/App.tsx, src/i18n/ru.json, src/i18n/en.json</files>
  <read_first>
    - src/app/App.tsx (focus on lines 387-460 — the trust section + Pricing function; also find <Accent> component definition by Grep `function Accent` or `const Accent`)
    - src/i18n/ru.json (lines 55-95 — the pricing.* group)
    - src/i18n/en.json (lines 55-95 — the pricing.* group)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 6: src/app/App.tsx + src/i18n/{ru,en}.json — Pricing heading i18n + section id rename"
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 6"
    - Run `grep -rn "#privacy" src/app/` — verify no navbar anchor link points at the old id (collision check)
  </read_first>
  <action>
    Three changes in one atomic commit:

    (a) **Add three new keys to `src/i18n/ru.json`** (insert right after the existing line 61 flat `"pricing.heading"`):
    - `"pricing.heading.lead": "Скачай бесплатно. ",`
    - `"pricing.heading.accent": "Перейди на Pro,",`
    - `"pricing.heading.tail": " когда будешь готов.",`

    Then **DELETE the flat line 61 `"pricing.heading"` key** (the one being replaced). Order doesn't matter to JSON consumers; just keep the file valid JSON (trailing commas not allowed in the last property of an object — preserve JSON syntax).

    (b) **Mirror in `src/i18n/en.json`** — add three new keys, delete the flat `"pricing.heading"`:
    - `"pricing.heading.lead": "Start Free. ",`
    - `"pricing.heading.accent": "Upgrade To Pro",`
    - `"pricing.heading.tail": " When You're Ready.",`

    (c) **Refactor `src/app/App.tsx` `Pricing()` function** (around lines 418-459):
    - Keep the existing `const t = useT(); const { lang } = useLang(); const isRu = lang === "ru";` lines.
    - Replace the hardcoded RU/EN conditional inside `<SectionTitle>` (lines 427-439) with a single i18n-driven composition that works for both languages. The 3 keys exist in both dicts, so no `isRu` branching is needed for the heading itself.
    - Keep the dual mobile/desktop variants — they only differ in `<br />` separators:
      - Mobile (`md:hidden`): `<span className="md:hidden">{t("pricing.heading.lead")}<Accent>{t("pricing.heading.accent")}</Accent>{t("pricing.heading.tail")}</span>`
      - Desktop (`md:inline`): `<span className="hidden md:inline">{t("pricing.heading.lead")}<br /><Accent>{t("pricing.heading.accent")}</Accent><br />{t("pricing.heading.tail")}</span>`
    - The `isRu` const is still needed for the existing `onetimePrice` / `proPrice` ternaries below — do NOT delete it.

    (d) **Rename the trust section id (IN-04)** around App.tsx line 393:
    - Find `<section id="privacy" className="bg-[#011417] px-5 py-[70px] md:py-24">` (around line 393 — the TRUST section, NOT the legal `/privacy` route).
    - Change `id="privacy"` to `id="trust"`.
    - Update the `id={\`${id}-heading\`}` form? No — that pattern is only inside `FaqBlock`. The trust section directly hardcodes its `id`. Just the single attribute swap. Confirm via Grep that no `href="#privacy"` exists anywhere in `src/app/` — if a stray ref does turn up, rename it too in the same commit. The existing comment block at lines 387-388 explains why the id was originally "privacy" (mislabel); UPDATE that comment to a one-liner noting the IN-04 rename: `// IN-04: section renamed from id="privacy" to id="trust" to avoid slug collision with the /privacy legal route.` (preserve the comment about the rename history if helpful, but the new comment should be the load-bearing one).

    DO NOT touch any other section id on the page (`#features`, `#pricing`, `#faq` — all stay). DO NOT touch the `pricing.free.*` keys in either dict (that purge is Wave 10 territory). DO NOT remove the existing `<Accent>` component or change its rendering.
  </action>
  <verify>
    <automated>npm run build && node -e "const r = require('./src/i18n/ru.json'); const e = require('./src/i18n/en.json'); if (r['pricing.heading'] !== undefined || e['pricing.heading'] !== undefined) { console.error('flat pricing.heading still present'); process.exit(1); } if (!r['pricing.heading.lead'] || !r['pricing.heading.accent'] || !r['pricing.heading.tail']) { console.error('missing RU pricing.heading.* keys'); process.exit(1); } if (!e['pricing.heading.lead'] || !e['pricing.heading.accent'] || !e['pricing.heading.tail']) { console.error('missing EN pricing.heading.* keys'); process.exit(1); } console.log('OK');" && ! grep -q 'id="privacy"' src/app/App.tsx && grep -q 'id="trust"' src/app/App.tsx && grep -q 'Скачай бесплатно' dist/index.html && grep -q 'Upgrade To Pro' dist/en/index.html</automated>
  </verify>
  <done>
    `src/i18n/ru.json` and `src/i18n/en.json` both have three new `pricing.heading.{lead,accent,tail}` keys and the flat `pricing.heading` key is removed. `src/app/App.tsx` `Pricing()` composes the heading from the three keys (both mobile + desktop variants, `<Accent>` wrap preserved). The trust section's `<section>` tag has `id="trust"`, not `id="privacy"`. `npm run build` succeeds. `dist/index.html` (RU landing) shows the composed heading with RU text + `<Accent>` markup; `dist/en/index.html` shows the EN equivalent. No `href="#privacy"` link exists anywhere in `src/app/`.
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. Flat `pricing.heading` key is gone from both dicts.
  3. Three new `pricing.heading.{lead,accent,tail}` keys exist in both dicts and parse as valid JSON.
  4. App.tsx Pricing() uses `t("pricing.heading.lead")`, `t("pricing.heading.accent")`, `t("pricing.heading.tail")` with `<Accent>` wrap on the middle.
  5. App.tsx trust section has `id="trust"` (not `id="privacy"`).
  6. No `href="#privacy"` exists in `src/app/**`.
  7. Build emits RU + EN landing with the composed heading visible.
  8. `#pricing` navbar link still scrolls to the Pricing section (no rename there); `#faq` navbar link still scrolls to FaqBlock.
</verification>

<success_criteria>
  1. IN-01 closed — Pricing heading routed through i18n via three keys.
  2. IN-04 closed — `id="privacy"` slug collision eliminated by renaming the trust section to `id="trust"`.
  3. Dead `pricing.heading` flat key removed from both dicts.
  4. No regression — design (Accent wrap) preserved; mobile + desktop variants both work; nav anchors still resolve.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-06-SUMMARY.md` covering:
  - Diff line counts for the three files.
  - Confirmation that the rendered text on RU + EN landings matches the pre-4.1 version (visual byte-for-byte parity for the heading copy, accounting only for whitespace/punctuation distribution between the three keys).
  - Grep result for `href="#privacy"` (should be 0).

  Commit message: `chore(04.1): route Pricing heading through i18n + rename trust section id from "privacy" to "trust" (IN-01, IN-04)`
</output>
</content>
</invoke>