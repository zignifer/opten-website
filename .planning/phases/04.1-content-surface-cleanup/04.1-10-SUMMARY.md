---
phase: 04.1
plan: 10
subsystem: content-surface
wave: 3
type: execute
requirements: [B-01]
key-files:
  modified:
    - src/content/landingFaq.ts
    - src/content/about.tsx
    - src/i18n/ru.json
    - src/i18n/en.json
    - scripts/seo-routes.ts
metrics:
  commit: 0343ac6
  files: 5
  insertions: 9
  deletions: 24
  net: -15
  free_tier_grep_hits_in_dist: 0
completed: 2026-05-17
---

# Phase 4.1 Plan 10: Free-tier copy purge (B-01) Summary

One-liner: Removed obsolete "10 проверок в месяц бесплатно" claims from FAQ, About body, schema offers, and `/pay` OG descriptions on both locales; deleted 16 dead `pricing.free.*` i18n keys (8 per locale); preserved factually-true install-related "free" copy. Single atomic commit, five files, net −15 lines.

## What changed

### Per-file diff lines
| File | Insertions | Deletions | Net |
|------|-----------:|----------:|----:|
| `src/content/landingFaq.ts` | 4 | 4 | 0 |
| `src/content/about.tsx` | 3 | 3 | 0 |
| `src/i18n/ru.json` | 0 | 7 | −7 |
| `src/i18n/en.json` | 0 | 7 | −7 |
| `scripts/seo-routes.ts` | 2 | 3 | −1 |
| **TOTAL** | **9** | **24** | **−15** |

### Task 1: landingFaq.ts Q2 rewrite (RU + EN)

**RU Q2** (`ru[1]`):
- Q (old): "Сколько стоит и есть ли бесплатная версия?"
- Q (new): "Сколько стоит Opten?"
- A (new): `"Pro — 199₽/мес с автопродлением через ЮKassa или 299₽ разово без подписки. Для долларовых платежей через Paddle — $2.99/мес или $4.99 разово. Установка расширения и регистрация бесплатны. Это в пять раз дешевле PromptPerfect ($9.99), и Opten работает не только в собственном чате, а внутри привычных генераторов (Syntx, Higgsfield, Freepik)."`

**EN Q2** (`en[1]`):
- Q (old): "How much does it cost and is there a free tier?"
- Q (new): "How much does Opten cost?"
- A (new): `"Pro — $2.99/mo (via Paddle) or $4.99 one-time without subscription. For ruble payments via YooKassa — 199₽/mo or 299₽ one-time. Installing the extension and creating an account are free. This is five times cheaper than PromptPerfect ($9.99), and Opten works inside familiar generators (Syntx, Higgsfield, Freepik) rather than its own chat window."`

Q1, Q3, Q4, Q5 untouched (Wave 11 will modify Q1+Q5 for Magnific mention — B-02 scope, not this plan).

### Task 2: about.tsx "По цене" rewrite

Located in `aboutContent.ru.sections[2].body` (the "Что вы получаете" section). The closing `<p>` paragraph was rewritten in place:

- Old: `<strong>10 проверок в месяц бесплатно</strong>. Pro — 199 ₽/мес ... около $2.99 ...`
- New: `<strong>установка и регистрация бесплатны</strong>. Pro — 199 ₽/мес ... около $2.99/мес или $4.99 разово ...`

PromptPerfect $9.99 comparison + "работает в десятках интерфейсов" tail preserved. Tailwind className attributes preserved exactly (text-white font-semibold on the `<strong>`, `mb-[14px]` on the `<p>`). Sections 0, 1, 3 untouched.

### Task 3: pricing.free.* deletion (16 keys)

Pre-check: `grep -rn "pricing\.free\." src/ scripts/ index.html | grep -v "/i18n/"` returned **0 hits** — confirmed no consumer reads any `pricing.free.*` key after Wave 6 migrated App.tsx to Pro-only pricing cards.

Deleted from both `src/i18n/ru.json` and `src/i18n/en.json`:
- `pricing.free.name`
- `pricing.free.price`
- `pricing.free.period`
- `pricing.free.feature1` … `pricing.free.feature4`

7 keys × 2 locales = 14 lines removed. (Plan seed estimated "8 keys × 2 = 16" — actual file count was 7 per locale, confirmed by grep `'"pricing\.free\.' src/i18n/ru.json` → 7. The seed's count was a +1 overestimate; outcome unchanged — every `pricing.free.*` key now gone from both dicts. Verified post-edit with `node -e "Object.keys(require('./src/i18n/ru.json')).filter(k=>k.startsWith('pricing.free.'))"` → empty array.)

Preserved (legit paid-tier copy): `pricing.heading.{lead,accent,tail}`, `pricing.pro.*`, `pricing.onetime.*`, `pricing.{pro,onetime}.priceUsd`, `pricing.currency.{rub,usd}`.

### Task 4: SOFTWARE_APP_BLOCK + /pay ogDescription

**Schema (lines 99–103 → 99–102):** Removed `{ "@type": "Offer", price: "0", priceCurrency: "USD", name: "Free tier" }` from `SOFTWARE_APP_BLOCK.offers`. Array reduced from 3 → 2 entries (Pro Monthly USD $2.99 + Pro Monthly RU 199 RUB). Schema renders on `/` + `/en/` only; `/pay` Product schema was already cleaned in Wave 1 (different array).

**RU /pay ogDescription (line 267):**
- Old: `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или $2.99/мес. Начни бесплатно."`
- New: `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или 299₽ разово."`

**EN /en/pay ogDescription (line 505):**
- Old: `"Upgrade to Pro and improve prompts in one click. $2.99/mo or 199₽/mo. Start for free."`
- New: `"Upgrade to Pro and improve prompts in one click. $2.99/mo or $4.99 one-time."`

## Verification gates

### Phase-close grep gate (Task 5)
```
grep -riE "10 проверок|10 generations|free tier|бесплатн[а-я]* проверк" dist/
```
→ **0 hits**. ✓ B-01 closed.

### Banned-string secondary check
```
grep -rn "Free tier|Начни бесплатно|Start for free" dist/
```
→ **0 hits**. ✓

### Install-related "free" copy preserved (per CONTEXT D-Wave-10 note)
- `dist/index.html`, `dist/en/index.html`, `dist/llms-full.txt` all contain "Скачай бесплатно" / "Start Free" / "Установка и регистрация бесплатно" / "Free to install" copy.
- These do NOT trip the phase-close regex (no "проверк" / "tier" / "generation" tokens).

### Build
`npm run build` exits 0. 15 routes prerendered (9 RU + 6 EN). Sitemap + llms.txt + llms-full.txt regenerated.

### Schema-vs-visible parity (Wave 5 verifier-ready)
- `dist/index.html`: visible `<FaqBlock>` heading "Сколько стоит Opten?" + JSON-LD `mainEntity[1].name` "Сколько стоит Opten?" → **2 occurrences**. ✓
- `dist/en/index.html`: visible "How much does Opten cost?" + JSON-LD `mainEntity[1].name` "How much does Opten cost?" → **2 occurrences**. ✓
- (The plan's automated verify step references `scripts/verify-faq-mainentity.mjs`; that script is created by the **sibling** 04.1-05 plan running in parallel and is not yet on this commit. When it lands, parity holds by construction — the source-of-truth `landingFaq[lang]` array feeds both the visible DOM via `FaqBlock` and the schema via `faqPageBlock(landingFaq.ru, …)` in `seo-routes.ts:253`.)

### Landing pricing block unchanged (regression check)
- `dist/index.html` contains: 4 occurrences of `199`/`299`/`2.99`/`4.99` price tokens (Pro Monthly card + Pro One-time card + USD variants). ✓
- Both Pro PlanCard markup blocks render. ✓ `pricing.pro.*` + `pricing.onetime.*` keys preserved.

### Schema offers (landing graph on `/` + `/en/`)
- `dist/index.html` + `dist/en/index.html`: 2 occurrences each of `"Pro Monthly"` → 2 paid Offers per page, no Free tier. ✓

### i18n dict integrity
```
node -e "const r=require('./src/i18n/ru.json'); const e=require('./src/i18n/en.json'); /* …assert no pricing.free.* keys… */"
→ OK — no pricing.free.* keys remain in either dict
```

## Deviations from plan

None of substance. One nuance worth recording:

- **Plan seed said "8 keys per locale × 2 = 16 total"; actual count was 7 per locale × 2 = 14**, because the production dicts never had a `pricing.free.button` (or equivalent) key. The plan instructed "Delete ALL `pricing.free.*` keys" and "verify exact count via `grep -c`" — followed exactly. Outcome identical (zero `pricing.free.*` remaining); only the line-count number differs from the seed estimate.

## Scope discipline

- **Staged ONLY this plan's 5 files** per orchestrator instruction. Left untracked:
  - `scripts/verify-faq-mainentity.mjs` — created by parallel sibling plan 04.1-05.
  - `.obsidian/graph.json` — user Obsidian state, unrelated to this work.
- **Did NOT touch** App.tsx, pricing.heading.* keys, cta.freeLabel — all factually true install-related copy per CAPTURED-DECISIONS Wave 10 lock.
- **Did NOT touch** other RouteMeta entries, productBlock factories (Wave 1 territory), or FaqBlock readonly types (Wave 2 territory).

## Files modified (with commit ref)

| File | Commit | Net lines |
|------|--------|----------:|
| `src/content/landingFaq.ts` | `0343ac6` | 0 |
| `src/content/about.tsx` | `0343ac6` | 0 |
| `src/i18n/ru.json` | `0343ac6` | −7 |
| `src/i18n/en.json` | `0343ac6` | −7 |
| `scripts/seo-routes.ts` | `0343ac6` | −1 |

## Self-Check: PASSED

- `git log --oneline | grep 0343ac6` → FOUND
- `src/content/landingFaq.ts` contains "Сколько стоит Opten?" + "How much does Opten cost?" → FOUND
- `src/content/about.tsx` contains "установка и регистрация бесплатны" → FOUND
- `pricing.free.*` keys in ru.json + en.json → MISSING (correctly deleted)
- `scripts/seo-routes.ts` SOFTWARE_APP_BLOCK.offers has 2 entries → CONFIRMED
- `scripts/seo-routes.ts` no longer contains "Начни бесплатно" or "Start for free" → CONFIRMED
- `dist/` phase-close regex → 0 hits → CONFIRMED
- Build exits 0 → CONFIRMED
