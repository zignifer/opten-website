---
phase: 04.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prerender.mjs
autonomous: true
requirements:
  - WR-01
  - IN-05
must_haves:
  truths:
    - "Every prerendered EN route (/en/, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/pay, /en/guides/gpt-image-2) ships og-card-en.png in <meta property=og:image> and <meta name=twitter:image>"
    - "Every prerendered RU route still ships og-card-ru.png"
    - "Font preload regression guard still throws if PT-Root-UI_VF.woff2 or Unbounded-VF.woff2 preload tags are missing"
    - "The obsolete line-226 comment about ogImage not being re-injected is removed"
  artifacts:
    - path: "scripts/prerender.mjs"
      provides: "applyOgImage helper + REQUIRED_FONT_PRELOADS constant"
      contains: "applyOgImage"
    - path: "scripts/prerender.mjs"
      provides: "REQUIRED_FONT_PRELOADS array constant at module top"
      contains: "REQUIRED_FONT_PRELOADS"
  key_links:
    - from: "scripts/prerender.mjs route loop"
      to: "applyOgImage(html, meta)"
      via: "function call after applyOgLocale and before applyJsonLd"
      pattern: "applyOgImage"
    - from: "applyHeroPreloadGuard"
      to: "REQUIRED_FONT_PRELOADS constant"
      via: "constant-driven regex iteration (replaces inlined FONT_PRELOAD_REGEXES literal pair)"
      pattern: "REQUIRED_FONT_PRELOADS"
---

<objective>
Close WR-01 (EN routes shipping og-card-ru.png) and IN-05 (font preload regex literals should be constants) inside `scripts/prerender.mjs`. One file, two surgical changes, one atomic commit.

Purpose: WR-01 is customer-visible — Telegram/Slack link unfurls on EN URLs (/en/, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/pay) currently show the RU OG card, even though the manifest already carries `ogImage: DEFAULT_OG_IMAGE_EN` and the EN card is in `public/`. IN-05 is a low-friction cleanup that pairs naturally with the same file edit. Both are Wave 1 — completely independent of any other wave.
Output: One commit adding `applyOgImage` to the prerender helper family + lifting font preload literals to a `REQUIRED_FONT_PRELOADS` array constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@scripts/prerender.mjs
@scripts/seo-routes.ts

<interfaces>
From `scripts/prerender.mjs` (existing apply* helper family — mirror this style):

- `applyHtmlLang(html, meta)` / `applyHreflang(html, meta)` / `applyOgLocale(html, meta)` — all take (html, meta), do a regex-anchor swap, and throw if the anchor is missing (Rule 1 "fail-fast on missing anchor" — see existing implementations).
- Existing route loop at lines 235-266 invokes helpers in sequence: applyMeta → applyHtmlLang → applyHreflang → applyOgLocale → applyJsonLd → applyHeroPreloadGuard → applyModulePreload → applySafariPreloadFallback → (conditional applyPaddleScript) → applyMarker → (conditional applyBody).
- `applyOgImage` must run BEFORE `applyJsonLd` / `applyMarker` / `applyPaddleScript` (which all consume `</head>`-adjacent content), and BEFORE the `ogImage` value is consumed elsewhere. Logical placement: right after `applyOgLocale` (both touch og:* meta tags).
- Existing line 226 comment: `// (ogImage is read but not currently re-injected — index.html already has og-card-ru.png hardcoded; Phase 4+ may swap this per-route)`. DELETE this comment as part of this plan.

From `scripts/seo-routes.ts`:
- `DEFAULT_OG_IMAGE = "https://opten.space/og-card-ru.png"` (already exported, already imported by prerender.mjs line 23).
- `DEFAULT_OG_IMAGE_EN = "https://opten.space/og-card-en.png"` (already exported but NOT currently imported by prerender.mjs).
- 7 EN routes carry explicit `ogImage: DEFAULT_OG_IMAGE_EN` in their RouteMeta entries (/en/, /en/pay, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/guides/gpt-image-2).
- RU routes do NOT set `ogImage` — they fall through to `DEFAULT_OG_IMAGE` via `meta.ogImage ?? DEFAULT_OG_IMAGE` (line 237).

Source index.html `<head>` (the swap target) — from index.html (verify by reading first 30 lines):
- `<meta property="og:image" content="https://opten.space/og-card-ru.png" />`
- `<meta name="twitter:image" content="https://opten.space/og-card-ru.png" />`
- These two tags both need swapping when `meta.ogImage` is set to the EN card.

Existing `escapeAttr(s)` helper at line 27 handles `&`, `"`, `<` — use it on the URL value passed into the meta tags.

`FONT_PRELOAD_REGEXES` constant at lines 222-225 is currently an array of two literal regexes used by `applyHeroPreloadGuard` at lines 226-233. Per IN-05 + per CONTEXT D-decision for Wave 1, lift the two paths (`/fonts/PT-Root-UI_VF.woff2`, `/fonts/Unbounded-VF.woff2`) into a `REQUIRED_FONT_PRELOADS = ["/fonts/PT-Root-UI_VF.woff2", "/fonts/Unbounded-VF.woff2"]` string array at module top; build the regexes inline from it (e.g. `new RegExp('<link\\\\s+rel="preload"\\\\s+href="' + escapeRegex(path) + '"[^>]*as="font"')`) or via a small `pathToPreloadRegex(path)` helper. Keep the same fail-fast semantics: throw with the same error message shape (`prerender(<path>): missing font preload <regex> — Phase 2.2 regression`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add applyOgImage helper + REQUIRED_FONT_PRELOADS constant + delete stale comment</name>
  <files>scripts/prerender.mjs</files>
  <read_first>
    - scripts/prerender.mjs (the full file — 268 lines; pay particular attention to: lines 22-23 imports, lines 27-32 escape helpers, lines 35-84 existing apply* helpers as style reference, lines 222-233 FONT_PRELOAD_REGEXES + applyHeroPreloadGuard, lines 235-266 the route loop)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 1: scripts/prerender.mjs — OG image swap + font preload constants"
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 1 (scripts/prerender.mjs)"
    - scripts/seo-routes.ts lines 46-48 (DEFAULT_OG_IMAGE / DEFAULT_OG_IMAGE_EN exports) and the 7 EN RouteMeta entries that set ogImage (line 463, 483, 519, 547, 575, 603, 633)
    - index.html (first 40 lines — to locate the exact og:image / twitter:image source markup the regex must match)
  </read_first>
  <action>
    Four edits to `scripts/prerender.mjs` in a single commit:

    (a) **Extend the named import on line 23** from `const { routes, SITE_ORIGIN, DEFAULT_OG_IMAGE } = await import(...)` to include `DEFAULT_OG_IMAGE_EN`. The import shape becomes `const { routes, SITE_ORIGIN, DEFAULT_OG_IMAGE, DEFAULT_OG_IMAGE_EN } = await import(pathToFileURL(MANIFEST_BUNDLE).href);`. Note: `DEFAULT_OG_IMAGE_EN` is not consumed by `applyOgImage` itself (the manifest already sets per-route `ogImage: DEFAULT_OG_IMAGE_EN`); the import is defensive — keeps the symbol bound in case Wave 12b needs it and so the import list mirrors the manifest's public surface.

    (b) **Add `applyOgImage(html, meta)` helper** between the existing `applyOgLocale` function (ends line 84) and `applyMeta` (starts line 86). Signature: `function applyOgImage(html, meta) { ... }`. The helper:
    - Resolves the URL via `const ogImageUrl = meta.ogImage ?? DEFAULT_OG_IMAGE;` (matches the current route-loop fallback at line 237).
    - Defines two regex anchors: one for `<meta property="og:image" content="..." />` and one for `<meta name="twitter:image" content="..." />`. Use the same `[^"]*` content matchers as `applyMeta` uses for `og:title`/`og:description`. Allow optional self-closing slash via `\\s*\\/?>` to match the source markup.
    - Asserts BOTH anchors exist (Rule 1 fail-fast — mirror `applyHtmlLang` / `applyOgLocale` pattern): `if (!ogImageRe.test(html)) throw new Error(...)` and same for twitter. Error message format: `prerender(${meta.path}): no <meta property="og:image"> matched. index.html structure changed?` (and analogous for twitter:image).
    - Performs both replacements: `html = html.replace(ogImageRe, '<meta property="og:image" content="' + escapeAttr(ogImageUrl) + '" />');` and same for twitter (with `name="twitter:image"`).
    - Returns the mutated `html`.

    (c) **Call `applyOgImage` from the route loop** by inserting `html = applyOgImage(html, meta);` immediately AFTER the existing `html = applyOgLocale(html, meta);` line and BEFORE `html = applyJsonLd(html, meta);`. Add a one-line comment: `// Phase 4.1 WR-01: swap og:image + twitter:image per-route (EN routes ship og-card-en.png; RU routes keep og-card-ru.png)`.

    (d) **Delete the stale line-226 comment** (currently `// (ogImage is read but not currently re-injected — index.html already has og-card-ru.png hardcoded; Phase 4+ may swap this per-route)`). Replace it with nothing — just remove the line. The surrounding `const ogImage = meta.ogImage ?? DEFAULT_OG_IMAGE;` line at line 237 also becomes dead code (the value is now resolved inside `applyOgImage`). Remove that line too. The route loop's first 3 lines (the `for (const meta of routes) {` plus the ogImage resolve plus the obsolete comment) collapse to just `for (const meta of routes) {` followed immediately by `let html = applyMeta(template, meta);`.

    (e) **Lift the font preload literals to a `REQUIRED_FONT_PRELOADS` constant** (IN-05). Replace the inline `FONT_PRELOAD_REGEXES` array at lines 222-225 with:
    - A module-top constant (placed near the existing module constants — between `DIST = resolve(...)` line 18 and the `SSR_BUNDLE` line, or near the other apply* helpers — place near applyHeroPreloadGuard for locality): `const REQUIRED_FONT_PRELOADS = ["/fonts/PT-Root-UI_VF.woff2", "/fonts/Unbounded-VF.woff2"];`
    - Inside `applyHeroPreloadGuard`, derive regexes from the constant: iterate `for (const fontPath of REQUIRED_FONT_PRELOADS) { const re = new RegExp('<link\\\\s+rel="preload"\\\\s+href="' + fontPath.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '"[^>]*as="font"'); if (!re.test(html)) throw new Error(...); }`. Use a small local `escapeRegex(s)` helper if cleaner — define it at module scope alongside `escapeAttr` / `escapeHtml`. The error message format MUST remain `prerender(${meta.path}): missing font preload <fontPath> — Phase 2.2 regression. See .planning/phases/04-content-surface/04-LCP-AUDIT.md.` (use `fontPath` rather than the regex literal for clearer diagnostics).
    - DELETE the now-unused `FONT_PRELOAD_REGEXES` constant.

    Do NOT touch any other helper, the route loop apply* ordering (aside from inserting applyOgImage), or any of the existing comments. Do NOT change the `meta.path === "/pay" || meta.path === "/en/pay"` Paddle gate (line 247) — INTEGRATION-CONTRACT §6 territory.
  </action>
  <verify>
    <automated>npm run build && grep -q "applyOgImage" scripts/prerender.mjs && grep -q "REQUIRED_FONT_PRELOADS" scripts/prerender.mjs && ! grep -q "FONT_PRELOAD_REGEXES" scripts/prerender.mjs && grep -q 'og-card-en.png' dist/en/index.html && grep -q 'og-card-en.png' dist/en/welcome/index.html && grep -q 'og-card-en.png' dist/en/pay/index.html && grep -q 'og-card-en.png' dist/en/privacy/index.html && grep -q 'og-card-en.png' dist/en/terms/index.html && grep -q 'og-card-en.png' dist/en/refund/index.html && grep -q 'og-card-en.png' "dist/en/guides/gpt-image-2/index.html" && grep -q 'og-card-ru.png' dist/index.html && grep -q 'og-card-ru.png' dist/welcome/index.html && grep -q 'og-card-ru.png' dist/pay/index.html && ! grep -q "ogImage is read but not currently re-injected" scripts/prerender.mjs</automated>
  </verify>
  <done>
    `scripts/prerender.mjs` contains `applyOgImage` helper, the constant `REQUIRED_FONT_PRELOADS`, no `FONT_PRELOAD_REGEXES` literal, and no stale "ogImage is read but not currently re-injected" comment. `npm run build` succeeds. All 7 EN HTML files emit `og-card-en.png` in both og:image and twitter:image meta tags; all 7 RU HTML files (excluding `/about` which is RU-only and untouched) still emit `og-card-ru.png` in both meta tags. The applyHeroPreloadGuard still throws (smoke: temporarily comment out one font preload `<link>` in `index.html`, run `npm run build`, see fail; revert).
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. EN HTML files contain `og-card-en.png` in BOTH og:image and twitter:image meta tags. Sanity grep: `grep -c 'og-card-en.png' dist/en/index.html` returns 2 (one for og:image, one for twitter:image); `grep -c 'og-card-ru.png' dist/en/index.html` returns 0.
  3. RU HTML files still contain `og-card-ru.png` in BOTH og:image and twitter:image meta tags.
  4. `scripts/prerender.mjs` has `applyOgImage` (new), `REQUIRED_FONT_PRELOADS` (new), no `FONT_PRELOAD_REGEXES` (deleted), no stale ogImage comment (deleted).
  5. `applyHeroPreloadGuard` still throws if a font preload `<link>` is deleted from `index.html` (manual smoke; revert).
</verification>

<success_criteria>
  1. WR-01 closed — EN routes ship og-card-en.png in their per-route HTML.
  2. IN-05 closed — font preload paths live in a single `REQUIRED_FONT_PRELOADS` constant.
  3. Existing behavior preserved — RU routes still ship og-card-ru.png; font preload guard still throws on regression; route-loop ordering for other apply* helpers unchanged.
  4. No new external dependencies; no behavior change for RU routes; no impact on /pay Paddle script injection or the SSR body path.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-01-SUMMARY.md` covering:
  - The exact diff (line counts added/removed in `scripts/prerender.mjs`).
  - The 7 EN HTML files confirmed to ship og-card-en.png + the 6 RU HTML files confirmed to still ship og-card-ru.png (route list).
  - Confirmation that the font-preload guard still throws when a preload `<link>` is removed.
  - Note that the commit message matches the seed line.

  Commit message: `fix(04.1): swap og:image per-route in prerender + lift font preload checks to constants (WR-01, IN-05)`
</output>
</content>
</invoke>