---
tags:
  - gsd
  - discussion-log
  - phase
phase: 4.1
milestone: "geo-optimization"
kind: discussion-log
---
# Phase 4.1: Content surface cleanup — Discussion Log

**Date:** 2026-05-17
**Mode:** Auto-promoted from captured-decisions seed (no interactive discussion this session)
**Source:** `04.1-CAPTURED-DECISIONS.md` (created 2026-05-17 during the ad-hoc chat that closed Phase 4 UAT and expanded into cleanup scope)

## Why this log is short

Phase 4.1's gray areas were resolved in a chat session BEFORE the canonical GSD pipeline was invoked. The captured-decisions seed (`04.1-CAPTURED-DECISIONS.md`) contains 530 lines of pre-decided scope, baseline-preservation rules, per-wave change descriptions, and verification steps. The user pre-confirmed three key disposition questions (EN /about, pricing reality, Magnific placement). Then the user invoked `/gsd-autonomous` with explicit "work without stopping for clarifying questions" instructions.

This discuss-phase run therefore did NOT ask new questions. It promoted the seed into the canonical `04.1-CONTEXT.md` format that downstream agents (planner, executor, verifier) consume. All decisions traced below are sourced from the chat that produced the seed.

## Decisions reflected in CONTEXT.md (source-of-truth chain)

### 1. EN /about disposition (B-03)
- **Question asked (in chat):** Ship full `/en/about` EN sibling now, or defer to a later phase?
- **User answer:** Ship full EN sibling. Claude translates body; user reviews EN copy before commit.
- **Captured in CONTEXT:** `<spec_lock>` decision #1, `<decisions>` Wave 12b, `<specifics>` B-03 block.
- **Approval gate:** Wave 12b commit requires user to redline EN copy before `npm run build`.

### 2. Pricing reality (B-01)
- **Question asked (in chat):** What is the current pricing? The site advertises "10 free checks" but the FAQ also mentions Pro tiers — which is authoritative?
- **User answer:** Pro only — 199₽/мес (YooKassa), $2.99/мес (Paddle), 299₽ one-time (YooKassa), $4.99 one-time (Paddle). No free checks tier exists. Install + registration remain free (factually true; not a usage claim).
- **Captured in CONTEXT:** `<spec_lock>` decision #2, `<decisions>` Wave 10, `<specifics>` B-01 block.
- **Followup note in seed:** App.tsx:430-431 "Скачай бесплатно. Перейди на Pro" and `cta.freeLabel` "Установка и регистрация бесплатно" STAY — they refer to install being free, not usage. Wave 10 reviewer must not over-purge.

### 3. Magnific placement (B-02)
- **Question asked (in chat):** Magnific is the upscaler/relighter Freepik acquired in 2024 — add it to supported-sites copy with a partner logo, with text only, or split into a Freepik logo + Magnific text note?
- **User answer:** Text only — no partner logo. Magnific named alongside Freepik in copy on FAQ, About, and welcome i18n. Freepik mention stays for SEO continuity (old brand name still has search volume).
- **Captured in CONTEXT:** `<spec_lock>` decision #3, `<decisions>` Wave 11, `<specifics>` B-02 block.

### 4. Pricing heading i18n approach (IN-01)
- **Question:** Restore `t("pricing.heading")` as a single flat key (Option A — accepts plain text, loses `<Accent>` wrap) or extract three keys and compose in JSX (Option B — preserves design)?
- **Resolution in seed:** **Option B chosen.** Preserves design. Delete unused flat `pricing.heading` key.
- **Captured in CONTEXT:** `<decisions>` Wave 6, `<specifics>` IN-01 block.

### 5. AboutPage content extraction approach (W12a)
- **Question:** When extracting `AboutPage.tsx` body to `src/content/about.ts`, how to handle embedded `<strong>` and `<a>` inline nodes — keep inline JSX in the data structure (simpler) or build a small AST renderer (cleaner separation)?
- **Resolution in seed:** Keep inline JSX. Simpler per CLAUDE.md "no new abstractions". The file already uses JSX-aware imports anyway.
- **Captured in CONTEXT:** `<decisions>` Wave 12a, `<specifics>` W12a block.

### 6. `/guides` index page
- **Question:** Should the 3-level breadcrumb on guide routes (`Home → Guides → Article`) be fixed by collapsing to 2 levels OR by adding a `/guides` index page?
- **Resolution in seed:** Collapse to 2 levels. With one guide an index page adds maintenance without UX value. Revisit when second guide ships.
- **Captured in CONTEXT:** `<decisions>` Wave 2, `<deferred>` block.

### 7. LCP work scope (V-18/V-19)
- **Question:** LCP on `/` mobile is 3.5 s (target 2.5 s). Should Phase 4.1 attempt to close it?
- **Resolution in seed:** No. Defer to a dedicated perf ticket (Unbounded font subsetting is the next likely lever). Phase 4.1 must NOT regress LCP further but is not responsible for closing the gap. Per `04-LCP-AUDIT` Option-1 lock-in.
- **Captured in CONTEXT:** `<domain>` "Explicitly NOT in this phase", `<deferred>` block.

### 8. /refund i18n dedup (CR-02 long-term)
- **Question:** CR-02 immediate fix (`eee40f6`) addressed the manifest-vs-page drift. Should Phase 4.1 also dedup the description into a single source of truth?
- **Resolution in seed:** No. Quality-of-life refactor; doesn't block 4.1.
- **Captured in CONTEXT:** `<deferred>` block.

### 9. Wave 9 + Wave 12b approval gates
- **Decision (autonomy memory):** Per `feedback_autonomy_and_caution.md`, extension-coupled surfaces (payments, account, integration contract) and new product surface additions require explicit user approval before commit. W9 touches `PayPage.tsx`; W12b adds `/en/about`.
- **Captured in CONTEXT:** `<decisions>` "Approval gates" section + per-wave notes.

## Items consciously NOT discussed (covered by other artifacts)

- **Per-wave file edits in technical detail** — already documented in `04.1-CAPTURED-DECISIONS.md` per wave. CONTEXT.md `<decisions>` summarizes; the seed is the implementation reference.
- **Verification commands** — already documented per wave in the seed and at phase close in `<decisions>` "Verification at phase close".
- **Baseline preservation specifics** — the seed's ⛔ BASELINE section is canonical. CONTEXT.md `<decisions>` "Baseline preservation" condenses to a checklist; downstream agents read the seed for the rationale (commit `f9dfdb1` revert intent).

## Scope-creep items rejected during seed creation

- **Adding a Magnific partner logo** → text-only mentions only (user decision, B-02).
- **Recreating `PricingStaticBlock`** → permanent reject; runtime-conditional CTA is the locked pattern.
- **Adding `/guides` index page** → no — one guide doesn't need an index. Revisit later.
- **Promoting LCP fix into 4.1** → no — dedicated perf ticket scope.
- **Bundling CR-02 dedup refactor into 4.1** → no — separate quality-of-life work.

All rejected items are captured in `<deferred>` so they're not lost.
