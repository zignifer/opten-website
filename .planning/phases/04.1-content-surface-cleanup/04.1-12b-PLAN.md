---
phase: 04.1
plan: 12b
type: execute
wave: 4
depends_on:
  - "04.1-01"
  - "04.1-10"
  - "04.1-11"
  - "04.1-12a"
files_modified:
  - src/content/about.tsx
  - src/i18n/paths.ts
  - src/main.tsx
  - scripts/entry-server.tsx
  - scripts/seo-routes.ts
  - src/i18n/en.json
autonomous: false
requirements:
  - B-03
must_haves:
  truths:
    - "src/content/about.tsx aboutContent.en is populated with the full EN translation of the founder/about body (≈1500 words, 4 sections matching the RU structure)"
    - "src/i18n/paths.ts EN_SIBLINGS includes '/about'"
    - "src/main.tsx mounts <Route path=/en/about element={<AboutPage />}/>"
    - "scripts/entry-server.tsx mounts <Route path=/en/about element={<AboutPage />}/>"
    - "scripts/seo-routes.ts adds a /en/about RouteMeta entry (htmlLang: en, ogImage: DEFAULT_OG_IMAGE_EN, prerender: full, hreflang reciprocal pointing back at /about)"
    - "scripts/seo-routes.ts RU /about hreflangAlternates.en updated from SITE_ORIGIN+/ to SITE_ORIGIN+/en/about (reciprocal pair)"
    - "src/i18n/en.json contains the 3 about.* keys (about.tagline, about.contactCta, about.backLink) — adds if missing"
    - "dist/en/about/index.html exists, has <html lang=en>, EN body content, hreflang triplets (RU sibling + EN self + x-default), og-card-en.png in og:image + twitter:image"
    - "Sitemap.xml includes both /about and /en/about with reciprocal xhtml:link annotations"
    - "LangSwitcher on /about navigates to /en/about; on /en/about navigates back to /about"
    - "RU /about body content is byte-for-byte unchanged (this wave only updates the hreflang.en field on the RU entry; the RU body in src/content/about.tsx is untouched)"
    - "All internal LocalizedLink references inside aboutContent.en resolve to EN siblings where possible (/en/terms, /en/refund)"
  artifacts:
    - path: "src/content/about.tsx"
      provides: "aboutContent.en populated with EN translation (replacing the W12a null sentinel)"
      contains: "aboutContent"
    - path: "src/i18n/paths.ts"
      provides: "EN_SIBLINGS includes /about"
      contains: "/about"
    - path: "src/main.tsx"
      provides: "/en/about Route mount"
      contains: "/en/about"
    - path: "scripts/entry-server.tsx"
      provides: "/en/about Route mount"
      contains: "/en/about"
    - path: "scripts/seo-routes.ts"
      provides: "/en/about RouteMeta entry + updated RU /about hreflang.en"
      contains: "/en/about"
    - path: "src/i18n/en.json"
      provides: "about.tagline + about.contactCta + about.backLink keys (added if missing)"
      contains: "about.tagline"
  key_links:
    - from: "RU /about entry hreflangAlternates.en"
      to: "SITE_ORIGIN + '/en/about'"
      via: "field update from SITE_ORIGIN + '/' (D-02 landing fallback) to SITE_ORIGIN + '/en/about' (true sibling)"
      pattern: "/en/about"
    - from: "EN_SIBLINGS"
      to: "LangSwitcher round-trip + LocalizedLink prefix preservation"
      via: "set membership"
      pattern: "/about"
    - from: "scripts/seo-routes.ts /en/about RouteMeta"
      to: "scripts/entry-server.tsx <Route path=/en/about> + src/main.tsx <Route path=/en/about>"
      via: "manifest declaration drives prerender; router mounts drive SSR + client render"
      pattern: "/en/about"
---

<objective>
Close B-03 (EN `/about` sibling — currently RU-only `/about` from Phase 4 has no EN sibling and the lang switcher can't escape the page). **APPROVAL GATE — new product surface.**

**⛔ This plan adds a NEW prerendered route. STOP before commit and request explicit user approval at two points: (1) EN translation copy review BEFORE running npm run build; (2) Route registration approval BEFORE commit.**

Purpose: User-reported 2026-05-17 — "мы добавили вкладку в 4й фазе о проекте но на английском её нет. Также на этой странице я не могу переключить язык". Phase 4 shipped `/about` as RU-only (per Phase 4 D-01/D-02). This wave delivers the EN sibling end-to-end: EN translation (~1500 words, first-person founder voice), route registration on both routers, manifest entry with EN ogImage + reciprocal hreflang, EN_SIBLINGS extension so LangSwitcher round-trips properly, and i18n EN-dict keys verified.

Depends on:
- Plan 04.1-01 (Wave 1) — applyOgImage must be live so `/en/about` gets `og-card-en.png` baked into the prerendered HTML.
- Plan 04.1-10 (Wave 3) — free-tier copy purged from `aboutContent.ru` so the EN translation doesn't propagate dead claims.
- Plan 04.1-11 (Wave 3) — Magnific mention added to `aboutContent.ru` so the EN translation includes it.
- Plan 04.1-12a (Wave 2) — `aboutContent.en = null` sentinel exists, ready to be populated.

Output: One commit modifying 6 files atomically AFTER user approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@.planning/phases/04-content-surface/04-CONTEXT.md
@.planning/phases/03-bilingual-routing/03-CONTEXT.md
@src/content/about.tsx
@src/i18n/paths.ts
@src/main.tsx
@scripts/entry-server.tsx
@scripts/seo-routes.ts
@src/i18n/en.json
@src/i18n/ru.json
@src/app/pages/AboutPage.tsx

<interfaces>
From `src/content/about.tsx` (post-W12a + W10 + W11):
- `aboutContent.ru` has 4 sections fully populated, post-purge (no free-tier copy), with Magnific mentions.
- `aboutContent.en` = `null` sentinel. This wave replaces null with the full EN translation.

EN translation requirements per CONTEXT D-Wave-12b + the user-confirmed B-03 disposition:
- ~1500 words total (matches RU length roughly — preserves register).
- First-person, founder voice; no marketing puffery; same register as RU.
- 4 sections matching RU heading structure (Откуда взялся Opten / Почему именно про промпты / Что вы получаете / Контакты):
  - "Where Opten came from" / "Why prompts specifically" / "What you get" / "Contact" — or the executor may refine these to more natural EN phrasings (e.g., "How Opten came to be" / "Why I focused on prompts" / "What you get" / "Get in touch"). User will see the EN copy in the approval gate and can redline.
- Inline links: YouTube `https://youtube.com/@v.voronezhtsev`, Telegram `https://t.me/v_voronezhtsev` — preserved.
- Internal links use `<LocalizedLink to="/terms">` and `<LocalizedLink to="/refund">` — when rendered on the `/en/about` route, `LocalizedLink` will rewrite these to `/en/terms` + `/en/refund` automatically (the on-EN-route prefix-preservation logic in `src/i18n/paths.ts localizeHref()`). No need to hardcode `/en/...` in the data file.
- Legal mention ("ИП Воронежцев В.П." / "IE Nikolai Shupletsov / Paddle Merchant of Record") — translate with care; preserve all factual details.
- Pricing facts in section 3 (post-W10): 199₽/мес via YooKassa, 299₽ one-time via YooKassa, $2.99/mo via Paddle, $4.99 one-time via Paddle, install + registration free, PromptPerfect $9.99 comparison.
- Supported sites mention (post-W11): Syntx, Higgsfield, Freepik (Magnific is part of Freepik).
- 7 `<li>` bullet items matching RU structure (Score 0-100, Up to 5 specific issues, Auto-enhance, Multimodal, Auto-detect model, Privacy, RU/EN UI).
- 4 `<h2>` headings mapped via the AboutSection.heading field.

Tailwind className styling (mirror RU sections post-W7):
- Each `<p>`: `mb-[14px]`
- Each `<strong>`: `text-white font-semibold`
- Each `<a>` and `<LocalizedLink>`: `text-[#9cfb51] underline hover:text-white transition-colors`
- `<ul>`: `mt-[8px] mb-[16px] pl-[24px] list-disc`
- Each `<li>`: `mb-[8px]`

From `src/i18n/paths.ts`:
- Line 9: `export const EN_SIBLINGS = new Set<string>([` ... 7 entries: `"/"`, `"/welcome"`, `"/pay"`, `"/privacy"`, `"/terms"`, `"/refund"`, `"/guides/gpt-image-2"`. **ADD `"/about"`** as the 8th entry. Place between `/refund` and `/guides/gpt-image-2` for grouping (legal/personal pages above; guides below).

From `src/main.tsx`:
- Line 62: `<Route path="/about" element={<AboutPage />} />` (RU mount, existing).
- Lines 66-72: EN sibling block. **ADD `<Route path="/en/about" element={<AboutPage />} />`** — place after `<Route path="/en/refund" element={<RefundPage />} />` to group with the other EN legal/marketing routes (above `/en/guides/:slug`).

From `scripts/entry-server.tsx`:
- Line 38: `<Route path="/about" element={<AboutPage />} />` (RU mount).
- Lines 41-47: EN sibling block. **ADD `<Route path="/en/about" element={<AboutPage />} />`** in the EN section, after `<Route path="/en/refund" element={<RefundPage />} />`, before `<Route path="/en/pay" ...>` or `<Route path="/en/guides/:slug" ...>` (group with EN legal/marketing).
- The line 40 comment ("Phase 4 D-12: ... Phase 4 D-01/D-02: /about is RU-only — no /en/about mount.") — UPDATE to reflect the new state: `"Phase 4 D-12: /pay AND /en/pay are SSR-mounted. Phase 4 D-06: /guides/:slug bilingual. Phase 4.1 B-03: /en/about now mounted (RU-only restriction lifted). /success /account /dashboard/* remain SPA-only — intentionally NOT mounted here."`

From `scripts/seo-routes.ts`:
- RU `/about` entry (lines 382-411): `hreflangAlternates.en` is currently `${SITE_ORIGIN}/` (the D-02 fallback to landing because no EN sibling existed). **UPDATE** to `${SITE_ORIGIN}/en/about` (reciprocal pair). `hreflangAlternates.ru` + `xDefault` stay as `${SITE_ORIGIN}/about`. NO body content change for RU `/about` — only the hreflang.en field.
- **ADD** a new EN `/en/about` RouteMeta entry. Place in the EN section, BEFORE the `/en/guides/gpt-image-2` entry (lines 620+ pre-edit). Structure (mirror the RU `/about` entry but with EN field values):
  ```
  {
    path: "/en/about",
    htmlLang: "en",
    hreflangAlternates: {
      ru: `${SITE_ORIGIN}/about`,
      en: `${SITE_ORIGIN}/en/about`,
      xDefault: `${SITE_ORIGIN}/about`,
    },
    title: "About Opten — who's behind the extension",
    description: "Opten is a personal project by Vlad Voronezhtsev, AI blogger and creator of the Chrome extension for prompt scoring. Origin story, contacts, legal details.",
    canonical: `${SITE_ORIGIN}/en/about`,
    ogTitle: "About Opten",
    ogDescription: "Origin story of Opten — the extension for scoring prompts against specific AI models.",
    ogImage: DEFAULT_OG_IMAGE_EN,
    prerender: "full",
    changefreq: "yearly",
    priority: 0.5,
    schema: [
      ORG_BLOCK,
      PERSON_FOUNDER_BLOCK,
      breadcrumbBlock(
        [
          { name: "Home", url: `${SITE_ORIGIN}/en/` },
          { name: "About", url: `${SITE_ORIGIN}/en/about` },
        ],
        `${SITE_ORIGIN}/en/about`,
      ),
    ],
  },
  ```

From `src/i18n/en.json`:
- `nav.about` exists (line 141: `"nav.about": "About"`).
- **about.tagline / about.contactCta / about.backLink are MISSING from EN dict** (RU dict has them at lines 142-144; EN dict does not — verified by grep). ADD these three keys to EN dict:
  - `"about.tagline": "Founder, Opten. AI blogger, creator of the prompt-scoring extension."`
  - `"about.contactCta": "Message on Telegram"`
  - `"about.backLink": "Back to home"`

Place these EN keys in the same area as the RU dict (after `nav.about` on line 141, before whatever follows). The AboutPage `<header>` chrome already calls `t("about.tagline")` / `t("about.contactCta")` / `t("about.backLink")` — if the EN dict is missing these, the page would render the literal key strings (e.g., "about.tagline") instead of translations, which is the current broken state for `/en/about` ONCE the route exists. Adding the keys fixes the rendering.

From `scripts/sitemap.mjs`:
- The sitemap is built from the routes manifest (`r.prerender !== "none"`). Adding the `/en/about` RouteMeta entry above propagates the new URL into the sitemap automatically + the `xhtml:link` reciprocal pair via `hreflangAlternates`. No manual sitemap edit needed.

Approval gate sequence:
1. **EN copy review** (BEFORE `npm run build`): executor presents the `aboutContent.en` translation as a diff or quoted block; user reviews + redlines + approves the EN text. Tone: first-person founder voice, no marketing puffery, ~1500 words, 4 sections matching RU structure with all factual claims preserved (pricing, Telegram, YouTube, legal).
2. **Route registration approval** (BEFORE commit, AFTER `npm run build` succeeds): executor presents the 5 small file edits (paths.ts + main.tsx + entry-server.tsx + seo-routes.ts /en/about entry + RU /about hreflang.en swap + en.json key additions) and the post-build assertions; user confirms; executor commits.

Forbidden changes (APPROVAL GATE constraints):
- Any change to RU `/about` body content (`aboutContent.ru` is post-W10/W11 — must be left alone; only the RU `hreflangAlternates.en` field on the seo-routes.ts entry changes).
- Any new EN sibling for routes other than `/about` (e.g., do NOT add `/en/account`, `/en/success`, `/en/dashboard/*` — those stay RU-only per Phase 3 D-03 lock).
- Any change to locked routes (`/welcome`, `/pay`, `/success`, `/account`, `/dashboard/download-skill` paths unchanged).
- Any new schema bundle beyond what mirrors the RU `/about` entry (Org + PersonFounder + Breadcrumb — same 3 blocks, EN breadcrumb labels).
- Any change to AboutPage.tsx (post-W7 + W12a it consumes `aboutContent[lang] ?? aboutContent.ru` — once `aboutContent.en` is populated, `/en/about` renders EN content without any AboutPage.tsx change).

Expected diff size: large (~1500 words EN translation in about.tsx + 5-10 small line edits elsewhere). The EN copy is the bulk; route/manifest edits total ~20 lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Draft aboutContent.en EN translation in src/content/about.tsx</name>
  <files>src/content/about.tsx</files>
  <read_first>
    - src/content/about.tsx (full file — post-W7 + W10 + W11 + W12a; aboutContent.ru is the source for translation)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 12b: /en/about EN sibling (B-03) — REQUIRES APPROVAL" item 1
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 12b" + §"B-03: EN /about (2026-05-17 chat)"
    - .planning/phases/04-content-surface/04-CONTEXT.md §D-01 / §D-02 (RU-only /about — now lifted)
  </read_first>
  <action>
    Populate `aboutContent.en` in `src/content/about.tsx`:

    1. Replace `const en: AboutContent | null = null;` (the W12a sentinel) with `const en: AboutContent = { sections: [ ... ] };`.
    2. Translate all 4 sections from `aboutContent.ru` to EN. Constraints:
       - Match RU section count (4) and heading order (Where Opten came from → Why prompts specifically → What you get → Contact, or equivalent EN phrasings).
       - First-person, founder voice. No marketing puffery. Same register as RU (informal-but-precise; technical specifics preserved verbatim — Midjourney 7/8/niji/video, GPT Image 2, etc.).
       - Preserve all numerical claims: 60 skill files, 0-100 scoring, up to 5 issues, 199₽/mo, 299₽ one-time, $2.99/mo, $4.99 one-time, PromptPerfect $9.99 comparison.
       - Preserve all factual links: youtube.com/@v.voronezhtsev, t.me/v_voronezhtsev.
       - Internal `<LocalizedLink to="/terms">` and `<LocalizedLink to="/refund">` — KEEP `to="/terms"` and `to="/refund"`; LocalizedLink will rewrite to `/en/terms` / `/en/refund` automatically on the EN route.
       - Mention Magnific alongside Freepik (B-02 mirror in EN — section 1 + section 2).
       - DO NOT mention any free-checks tier (B-01 mirror in EN — install/registration are free, but no monthly free quota).
       - Translate "ИП Воронежцев Владислав Павлович, ИНН 723016676391, Тюмень" carefully — preserve the legal entity name verbatim (it's a registered Russian sole-proprietorship; do not invent an English translation). Acceptable EN form: `"Legally, Opten operates in Russia under IE Voronezhtsev V.P. (Russian sole-proprietorship; tax ID 723016676391, Tyumen). For international payments via Paddle — IE Nikolai Shupletsov as Merchant of Record (standard Paddle setup; unrelated to the product itself)."`
       - Pricing paragraph (section 3 closing — post-W10): translate the new copy ("install and registration are free; Pro is 199₽/mo via YooKassa or 299₽ one-time; via Paddle around $2.99/mo or $4.99 one-time; five times cheaper than PromptPerfect $9.99...").
    3. Tailwind classNames on each JSX node — mirror the RU sections exactly (post-W7):
       - `<p className="mb-[14px]">`, `<strong className="text-white font-semibold">`, `<a className="text-[#9cfb51] underline hover:text-white transition-colors" target="_blank" rel="noopener noreferrer">`, `<LocalizedLink className="text-[#9cfb51] underline hover:text-white transition-colors" to="/terms">`, etc.
       - `<ul className="mt-[8px] mb-[16px] pl-[24px] list-disc">`, `<li className="mb-[8px]">`.
    4. EN headings (suggested — executor may refine):
       - "How Opten came to be"
       - "Why I focused on prompts"
       - "What you get"
       - "Get in touch"
    5. EN 7 bullet items (mirror RU semantics — Score 0–100 / Up to five specific issues / Auto-enhance / Multimodal / Auto-detect model / Privacy / RU/EN UI).
  </action>
  <verify>
    <automated>grep -q "aboutContent" src/content/about.tsx && ! grep -q "const en: AboutContent | null = null" src/content/about.tsx && node -e "require('child_process').execSync('npx tsc --noEmit src/content/about.tsx 2>&1 || true', {stdio: 'inherit'}); console.log('typecheck attempted (Vite will catch real issues at build)');"</automated>
  </verify>
  <done>
    `src/content/about.tsx` has a fully populated `aboutContent.en` with 4 sections, ~1500 words, first-person founder voice, all factual claims preserved (pricing, links, legal entity, Magnific mention, no free-tier claim). TypeScript types compile (the type `AboutContent` matches; no `null` sentinel remains). File still exports `aboutContent = { ru, en } as const;`. Do NOT proceed to build/route registration until the EN copy passes the approval gate (Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: APPROVAL GATE #1 — review EN translation copy before build</name>
  <files>(no files modified — review gate)</files>
  <read_first>
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 12b" approval discussion
    - Memory: feedback_autonomy_and_caution.md (new product surface = approval pause)
  </read_first>
  <what-built>
    Task 1 populated `aboutContent.en` in `src/content/about.tsx`. The translation is approximately 1500 words across 4 sections matching the RU structure. All factual claims preserved (Pro pricing per the W10 reality, Magnific mention per W11, install/registration free, no free-checks tier).

    Executor presents the EN copy in a readable form: either as a `git diff src/content/about.tsx` block, or as a quoted text-only excerpt (heading + paragraph snippets) of each of the 4 sections, depending on what's clearer for review.
  </what-built>
  <how-to-verify>
    User reads through `aboutContent.en` section by section. Checks:
    1. **Voice and tone** — first-person, founder voice, no marketing puffery. Same register as the RU body.
    2. **Factual accuracy** — pricing (199₽/mo, 299₽ one-time, $2.99/mo, $4.99 one-time), 60 skill files, install + registration free, PromptPerfect $9.99 comparison, Magnific named alongside Freepik, supported sites match RU (Syntx, Higgsfield, Freepik/Magnific).
    3. **No free-tier claims** — must not say "10 checks per month free" anywhere (B-01 invariant in EN mirror).
    4. **Legal entity translation** — IE Voronezhtsev V.P. preserved; tax ID + Tyumen preserved; IE Nikolai Shupletsov as Paddle MoR mentioned.
    5. **Embedded links** — Telegram `@v_voronezhtsev`, YouTube `@v.voronezhtsev` URLs intact.
    6. **Internal links** — uses `<LocalizedLink to="/terms">` and `<LocalizedLink to="/refund">` (not hardcoded `/en/...`); LocalizedLink handles the prefix.
    7. **No invented details** — no fake credentials, fake stats, or padding sentences.

    If user redlines any section, the executor re-edits Task 1 and re-presents. Multiple rounds are acceptable here — the EN copy must be right before proceeding.
  </how-to-verify>
  <resume-signal>
    Type "approved — EN copy final" to proceed to Task 3 (route registration + build).
    Redirects: "redline section X: <feedback>" → executor re-edits and re-presents.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register /en/about route + add manifest entry + extend EN_SIBLINGS + verify EN i18n keys</name>
  <files>src/i18n/paths.ts, src/main.tsx, scripts/entry-server.tsx, scripts/seo-routes.ts, src/i18n/en.json</files>
  <read_first>
    - src/i18n/paths.ts (the full file — 70 lines)
    - src/main.tsx (the full file)
    - scripts/entry-server.tsx (the full file — 53 lines)
    - scripts/seo-routes.ts (focus on RU /about lines 382-411 + the EN section starting at line 449 + the existing /en/guides/gpt-image-2 entry as a placement reference around line 620)
    - src/i18n/en.json (line 141 — confirm nav.about exists; check whether about.tagline/contactCta/backLink already exist — they do NOT per pre-check; add them)
    - src/i18n/ru.json lines 142-144 (RU equivalents for translation reference)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 12b" items 2-6
    - .planning/phases/03-bilingual-routing/03-CONTEXT.md §D-01 / §D-02 (URL-prefix strategy) — confirm the EN sibling pattern
  </read_first>
  <action>
    Five small edits in sequence (all in the same eventual commit):

    (a) **`src/i18n/paths.ts`** — add `"/about"` to EN_SIBLINGS. Edit line 9-17:
    ```
    export const EN_SIBLINGS = new Set<string>([
      "/",
      "/welcome",
      "/pay",
      "/privacy",
      "/terms",
      "/refund",
      "/about",  // Phase 4.1 B-03: /en/about EN sibling
      "/guides/gpt-image-2",
    ]);
    ```

    (b) **`src/main.tsx`** — add `<Route path="/en/about" element={<AboutPage />} />` to the EN sibling block (after line 71 `<Route path="/en/refund" element={<RefundPage />} />`, before line 72 `<Route path="/en/guides/:slug" element={<GuidePage />} />`). The `AboutPage` import is already at line 11; no new import needed.

    (c) **`scripts/entry-server.tsx`** — mirror: add `<Route path="/en/about" element={<AboutPage />} />` to the EN section (after line 45 `<Route path="/en/refund" element={<RefundPage />} />`, before line 46 `<Route path="/en/pay" ...>` or line 47 `<Route path="/en/guides/:slug" ...>`). Update the line 40 comment to reflect the new state: replace `"Phase 4 D-01/D-02: /about is RU-only — no /en/about mount."` substring with `"Phase 4.1 B-03: /en/about now mounted (RU-only restriction lifted)."`

    (d) **`scripts/seo-routes.ts`** — two sub-edits:
    - **Update RU `/about` `hreflangAlternates.en`** from `${SITE_ORIGIN}/` to `${SITE_ORIGIN}/en/about`. Update the comment block above the entry (lines 380-381) from `"// hreflang.en falls back to landing per D-02 (NOT to a non-existent /en/about)."` to `"// Phase 4.1 B-03: /en/about EN sibling now exists; hreflang.en points at it (no longer the D-02 fallback to landing)."`.
    - **Add new `/en/about` RouteMeta entry** in the EN section (place before the `/en/guides/gpt-image-2` entry — group with other EN legal/marketing routes for manifest readability). Use the structure laid out in the `<interfaces>` block (htmlLang: "en", hreflangAlternates reciprocal, EN title/description/ogTitle/ogDescription, DEFAULT_OG_IMAGE_EN, prerender: "full", changefreq: "yearly", priority: 0.5, schema with ORG_BLOCK + PERSON_FOUNDER_BLOCK + EN breadcrumbBlock).

    (e) **`src/i18n/en.json`** — add three missing about.* keys. Place near the existing line 141 `"nav.about": "About"`:
    - `"about.tagline": "Founder, Opten. AI blogger, creator of the prompt-scoring extension."`
    - `"about.contactCta": "Message on Telegram"`
    - `"about.backLink": "Back to home"`

    Run `npm run build` after all 5 edits. Build must succeed end-to-end. The new build chain (post-Wave 5) includes the verify-faq-mainentity gate — it should pass since /en/about does not carry a FAQPage block. The Wave 8 SSR-body-length assert must pass for the new /en/about (full-tier route, mounted on both routers — body should be thousands of chars).

    DO NOT touch:
    - `aboutContent.ru` in `src/content/about.tsx` (unchanged this wave — only `.en` was populated in Task 1).
    - Any other RouteMeta entry in `scripts/seo-routes.ts`.
    - Any other EN_SIBLINGS check or LangSwitcher logic.
    - `src/app/pages/AboutPage.tsx` (post-W12a it already reads `aboutContent[lang] ?? aboutContent.ru`; no change needed for the EN render path).
  </action>
  <verify>
    <automated>npm run build && test -f dist/en/about/index.html && grep -q '<html lang="en">' dist/en/about/index.html && grep -c "hreflang" dist/en/about/index.html | grep -qE "^[3-9]" && grep -q "og-card-en.png" dist/en/about/index.html && grep -q "/about" src/i18n/paths.ts && grep -q "/en/about" scripts/seo-routes.ts && grep -q "/en/about" scripts/entry-server.tsx && grep -q "/en/about" src/main.tsx && grep -q "about.tagline" src/i18n/en.json && grep -q "/en/about" dist/sitemap.xml && grep -q "About" dist/en/about/index.html</automated>
  </verify>
  <done>
    All 5 files edited cleanly. `npm run build` succeeds end-to-end (including the new Wave 5 + Wave 8 build gates). `dist/en/about/index.html` exists, has `<html lang="en">`, contains EN body content (titles, paragraphs, links), carries 3 hreflang triplet entries (RU sibling + EN self + x-default), uses `og-card-en.png` in og:image + twitter:image (Wave 1 helper). `dist/sitemap.xml` includes both `/about` and `/en/about` with reciprocal `<xhtml:link>` annotations (the sitemap.mjs generator picks these up automatically from the manifest's hreflangAlternates fields). LangSwitcher on `/about` (RU) routes to `/en/about`; LangSwitcher on `/en/about` routes back to `/about`. RU `/about` body content is unchanged (only its hreflang.en field was updated).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: APPROVAL GATE #2 — review route registration + build artifacts before commit</name>
  <files>(no files modified — final commit gate)</files>
  <read_first>
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 12b" approval discussion
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Approval gates (explicit pauses)"
  </read_first>
  <what-built>
    Task 3 added 5 small edits: EN_SIBLINGS extension (paths.ts), Route mounts (main.tsx + entry-server.tsx), manifest /en/about entry + RU /about hreflang.en update (seo-routes.ts), and 3 EN i18n keys (en.json). `npm run build` produced `dist/en/about/index.html` with EN body, EN hreflang, EN og:image, and the sitemap was extended with the reciprocal entry.

    Executor presents:
    - `git diff` for the 5 files modified in Task 3 (concise — should be ~30 lines total excluding the large `aboutContent.en` body which is in Task 1's diff).
    - Spot-check assertions output (the verify block).
    - Sample lines from `dist/en/about/index.html` confirming the EN body rendered.
    - Sample lines from `dist/sitemap.xml` showing the new `/en/about` entry with xhtml:link.
  </what-built>
  <how-to-verify>
    User confirms:
    1. **Diff size and scope:** `git diff src/i18n/paths.ts src/main.tsx scripts/entry-server.tsx scripts/seo-routes.ts src/i18n/en.json | wc -l` — should be under ~60 lines. Larger diffs in seo-routes.ts (the new manifest entry) and en.json (the 3 key additions) are expected.
    2. **No forbidden changes:**
       - `git diff src/content/about.tsx` shows ONLY the `aboutContent.en` body population (Task 1 work); `aboutContent.ru` is byte-for-byte unchanged. Confirm via grep `git diff src/content/about.tsx | grep -E "^\\+.*sections\\[0\\]|^-.*sections\\[0\\]"` should not match RU section content.
       - `git diff src/app/pages/AboutPage.tsx` is empty (no change to AboutPage.tsx).
       - `git diff EXTENSION_IDS SUPABASE_URL SUPABASE_ANON_KEY` not in any diff.
       - No new EN sibling added beyond `/about` (e.g., grep diff for `/en/account`, `/en/success`, `/en/dashboard` — must be 0 hits).
    3. **Build artifacts:**
       - `test -f dist/en/about/index.html` — PASS
       - `grep -c '<html lang="en">' dist/en/about/index.html` returns exactly 1
       - `grep -c 'hreflang' dist/en/about/index.html` returns at least 3 (RU sibling + EN self + x-default)
       - `grep -c 'og-card-en.png' dist/en/about/index.html` returns at least 2 (og:image + twitter:image — Wave 1 invariant)
       - `grep -c '/en/about' dist/sitemap.xml` returns at least 2 (one for `<loc>`, plus xhtml:link entries)
       - LangSwitcher round-trip: `npm run preview`, visit `http://localhost:4173/about`, click LangSwitcher → URL should change to `/en/about`. Click again → back to `/about`. (Manual smoke if Playwright not handy.)
    4. **EN content rendering:** spot-check that `dist/en/about/index.html` has the EN section headings ("How Opten came to be" etc.) and paragraph text — NOT the literal `about.tagline` string or RU body content.

    If anything fails, executor re-edits the offending step. If all gates pass, user types approval → executor commits.
  </how-to-verify>
  <resume-signal>
    Type "approved — Wave 12b ready to commit" to proceed.
    Redirects: "build artifact issue at <X>" → executor diagnoses; "EN copy regression at <Y>" → re-edit aboutContent.en (back to Task 1); "diff too large" → investigate and trim.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New prerendered route | `/en/about` is a new public URL. It is static prerendered EN content — no new endpoints, no new auth surface, no extension messaging, no payments. Threat surface is identical to the existing 14 prerendered routes. |
| Internal link rewrite by LocalizedLink | On `/en/about`, `<LocalizedLink to="/terms">` is rewritten to `/en/terms` by `src/i18n/paths.ts localizeHref()` — no user-controlled input flows here (the to= prop is static at JSX site), so no open-redirect risk. |
| EN translation accuracy | Wave 12b approval gate #1 requires user review of the EN body to prevent factual drift / mistranslation / accidental free-tier claim regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-12b-01 | Tampering (RU /about regression) | aboutContent.ru in src/content/about.tsx | mitigate | Task 1 explicitly only populates aboutContent.en; Task 3 diff scope limits to 5 specific files; approval gate #2 confirms `aboutContent.ru` is byte-for-byte unchanged in git diff |
| T-04.1-12b-02 | Information disclosure (factual drift in EN copy) | aboutContent.en body | mitigate | Approval gate #1 requires user review of EN copy before build; redlines + re-edits allowed; no free-tier claim must survive (B-01 mirror); no invented credentials |
| T-04.1-12b-03 | Integrity (LangSwitcher round-trip break) | EN_SIBLINGS set in src/i18n/paths.ts | mitigate | Manual round-trip smoke confirmed during approval gate #2; the existing `toEnTarget` / `toRuTarget` functions handle the new `/about` entry automatically once added to the set |
| T-04.1-12b-04 | Denial (build failure from missing manifest fields) | scripts/seo-routes.ts new entry | mitigate | TypeScript RouteMeta interface enforces all required fields at build time; Wave 8's SSR-body-length assert catches any missing entry-server.tsx Route mount |
</threat_model>

<verification>
  One atomic commit lands AFTER two explicit user approvals (Tasks 2 + 4).

  Acceptance gates (post-commit):
  1. `npm run build` exits 0; all new build gates (Wave 5 FAQ verifier, Wave 8 SSR body assert) pass.
  2. `dist/en/about/index.html` exists with `<html lang="en">`, ≥3 hreflang entries, og-card-en.png in og:image + twitter:image, EN body content (section headings + paragraphs in EN).
  3. `dist/about/index.html` is byte-for-byte unchanged (RU /about body untouched; only its hreflang.en field updated in HTML to point at /en/about).
  4. `dist/sitemap.xml` includes /en/about with reciprocal xhtml:link annotation.
  5. EN i18n dict has `about.tagline`, `about.contactCta`, `about.backLink`.
  6. LangSwitcher round-trip (manual): `/about` ↔ `/en/about`.
  7. `src/app/pages/AboutPage.tsx` is unchanged in this commit (renders EN automatically via `aboutContent[lang]`).
</verification>

<success_criteria>
  1. B-03 closed — /en/about exists as a full prerendered EN sibling with reciprocal hreflang.
  2. EN copy approved by user (~1500 words, founder voice, no marketing puffery, no free-tier claims, Magnific named).
  3. RU /about body unchanged.
  4. LangSwitcher round-trip works.
  5. APPROVAL GATES #1 + #2 passed in sequence; commit only after gate #2 approval.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-12b-SUMMARY.md` covering:
  - Final word count of `aboutContent.en` body.
  - Diff sizes for the 5 small files (paths.ts, main.tsx, entry-server.tsx, seo-routes.ts, en.json).
  - User's approval messages for both gates (for the audit trail).
  - All 7 acceptance gates' grep/test results.
  - Confirmation that the RU /about diff is exactly the 1 line in seo-routes.ts hreflang.en field (no body content change).

  Commit message: `feat(04.1): add /en/about — full EN sibling for the founder page (B-03)`
</output>
</content>
</invoke>