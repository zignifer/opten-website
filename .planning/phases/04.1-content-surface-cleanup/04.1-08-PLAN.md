---
phase: 04.1
plan: 08
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - scripts/prerender.mjs
autonomous: true
requirements:
  - IN-03
must_haves:
  truths:
    - "scripts/prerender.mjs asserts that every prerender=full route produces an SSR body longer than a minimum threshold (200 chars)"
    - "If a full-tier route is missing its <Route> in scripts/entry-server.tsx (so renderToString returns an empty fragment), the build fails with a clear error pointing at the route path"
    - "npm run build still succeeds on current source (all 9 full-tier routes mount cleanly)"
  artifacts:
    - path: "scripts/prerender.mjs"
      provides: "Inline assert after the applyBody / renderRoute call — throws on suspiciously-short SSR body for full-tier routes"
      contains: "SSR body too short"
  key_links:
    - from: "route loop full-tier branch"
      to: "rendered.length threshold check"
      via: "after `const rendered = renderRoute(meta.path)` — assert rendered.length > 200 chars or throw"
      pattern: "renderRoute"
---

<objective>
Close IN-03 (no build-time check that full-tier routes are mounted in `scripts/entry-server.tsx`) with one atomic commit. Defensive build guard inside `scripts/prerender.mjs`.

Purpose: today, if a contributor adds a new full-tier RouteMeta entry in `scripts/seo-routes.ts` but forgets to add the corresponding `<Route path="...">` in `scripts/entry-server.tsx`, `renderRoute()` silently emits an empty React fragment, prerender writes a head-only HTML body for that route, and the regression goes undetected until a crawler hits the empty page. A cheap heuristic — body length > 200 chars — catches this at build time. Per CAPTURED-DECISIONS Wave 8: the assert lives in `prerender.mjs` (the consumer), not in `entry-server.tsx` (the producer).

Depends on Plan 04.1-01 to avoid `scripts/prerender.mjs` file-ownership collision. Wave 1 ships the OG image + font preload constant changes; this plan in Wave 2 adds the assert.

Output: One commit adding ~5 lines inside the existing route loop's full-tier branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@scripts/prerender.mjs
@scripts/entry-server.tsx

<interfaces>
From `scripts/prerender.mjs` (post-Wave 1):
- Route loop full-tier branch lines 251-254 (pre-4.1, may have shifted slightly after Wave 1):
  ```
  if (meta.prerender === "full") {
    const rendered = renderRoute(meta.path);
    html = applyBody(html, rendered);
  }
  ```
- `renderRoute(path)` from `scripts/entry-server.tsx` returns `renderToString(<StaticRouter>...</Routes></StaticRouter>)`. If no `<Route>` matches the given path, `<Routes>` renders nothing (or the suspense fallback `<RouteLoading />` if any), and `renderToString` returns a very short string (`<div></div>` or the loader markup, typically 50-100 chars).
- The current happy-path SSR body for `/about`, `/`, `/en/`, etc. is several thousand characters. A 200-char floor cleanly distinguishes empty/fallback from real content.

From `scripts/entry-server.tsx`:
- Lines 24-52: `function renderRoute(path: string): string { return renderToString(<StaticRouter>...</Routes></StaticRouter>); }`.
- Currently mounts: /, /welcome, /privacy, /terms, /refund, /pay, /about, /guides/:slug, /en/, /en/welcome, /en/privacy, /en/terms, /en/refund, /en/pay, /en/guides/:slug — 15 paths (14 unique full-tier prerendered routes + 1 catch-all for the dynamic guide slug).

Match against the `scripts/seo-routes.ts` manifest: post-Phase-4 has 14 prerendered routes (per the floor check in `llms.mjs` line 21). All 14 are `prerender: "full"`. (`prerender: "head"` exists in the type but no current route uses it post-revert.) (Note: there is no `prerender: "head"` route today — verify before relying on this; if any route was `"head"`, the assert must skip it.)

Threshold rationale (200 chars): a real SSR body for the simplest legal page (`/refund`) is several thousand chars. The empty React `<Suspense fallback={<RouteLoading />}>` markup is well under 200 chars. The 200-char threshold has comfortable margin and won't trigger on legitimate small pages.

Error message format:
`prerender(${meta.path}): SSR body is suspiciously short (${rendered.length} chars). Expected >200 chars for full-tier route — entry-server.tsx may be missing a <Route> for this path.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add SSR-body-length assert to full-tier prerender branch</name>
  <files>scripts/prerender.mjs</files>
  <read_first>
    - scripts/prerender.mjs (the full file, focus on the route loop bottom — lines ~235-266 in the pre-Wave-1 form, may have shifted by ±5 lines after Wave 1)
    - scripts/entry-server.tsx (the full file, 53 lines)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 8: scripts/entry-server.tsx — defense-in-depth Route assert" (note: the seed clarifies the assert belongs in prerender.mjs, not entry-server.tsx)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 8"
  </read_first>
  <action>
    Single edit to `scripts/prerender.mjs` in one atomic commit:

    **Add an inline assertion inside the existing full-tier branch.** Locate the block:
    ```
    if (meta.prerender === "full") {
      const rendered = renderRoute(meta.path);
      html = applyBody(html, rendered);
    }
    ```
    Add an assertion between the `renderRoute` call and the `applyBody` call:
    ```
    if (meta.prerender === "full") {
      const rendered = renderRoute(meta.path);
      // Phase 4.1 IN-03: defense-in-depth — if entry-server.tsx is missing a <Route> for this path,
      // renderToString returns an empty fragment / Suspense fallback (~50 chars). The 200-char threshold
      // distinguishes that from a real rendered React tree (legitimate body is ~thousands of chars even
      // for the smallest legal page).
      if (rendered.length < 200) {
        throw new Error(`prerender(${meta.path}): SSR body is suspiciously short (${rendered.length} chars). Expected >200 chars for full-tier route — entry-server.tsx may be missing a <Route> for this path.`);
      }
      html = applyBody(html, rendered);
    }
    ```

    DO NOT change `applyBody` itself, the route loop structure, the `applyPaddleScript` gate, or any other apply* helper. DO NOT lower the threshold below 200 — that's the safety margin agreed in CONTEXT D-Wave-8. DO NOT extend the assert to non-full routes (no current route has `prerender: "head"`, but the gate type allows it — preserve that flexibility).
  </action>
  <verify>
    <automated>npm run build && grep -q "SSR body is suspiciously short" scripts/prerender.mjs && grep -q "Phase 4.1 IN-03" scripts/prerender.mjs</automated>
  </verify>
  <done>
    `scripts/prerender.mjs` contains the SSR-body-length assert inside the full-tier branch with the Phase 4.1 IN-03 marker. `npm run build` succeeds (current source has no missing routes — all 14 full-tier routes mount cleanly and their SSR bodies are well over 200 chars). Manual smoke: comment out one `<Route path="/about">` in `scripts/entry-server.tsx`, run `npm run build` — must fail with the clear error message naming `/about`. Revert.
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0.
  2. `grep "SSR body is suspiciously short" scripts/prerender.mjs` returns 1 hit.
  3. Smoke test: commenting out `<Route path="/about">` in `entry-server.tsx` causes build to fail with the expected error message (revert).
  4. No regression in other prerender behavior — applyBody, applyMarker, applyPaddleScript ordering preserved.
</verification>

<success_criteria>
  1. IN-03 closed — defense-in-depth assert catches missing entry-server.tsx routes at build time.
  2. No false positives — current source still builds clean.
  3. The assert is local to the prerender consumer; no producer-side change.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-08-SUMMARY.md` covering:
  - Diff line count (~5 lines added).
  - Smallest SSR body size observed across the 14 full-tier routes (to validate the 200-char threshold has comfortable margin — `console.log` the length per route during one test build, capture the min).
  - Captured smoke-test failure message.

  Commit message: `fix(04.1): assert full-tier routes produce non-trivial SSR body in prerender.mjs (IN-03)`
</output>
</content>
</invoke>