---
phase: 04.1
plan: 10
type: execute
wave: 3
depends_on:
  - "04.1-02"
  - "04.1-04"
  - "04.1-06"
  - "04.1-12a"
files_modified:
  - src/content/landingFaq.ts
  - src/content/about.tsx
  - src/i18n/ru.json
  - src/i18n/en.json
  - scripts/seo-routes.ts
autonomous: true
requirements:
  - B-01
must_haves:
  truths:
    - "landingFaq.ts Q2 (RU + EN) no longer claims a free checks tier — describes Pro pricing only (199₽/мес, 299₽ разово; $2.99/мес, $4.99 разово); PromptPerfect comparison preserved"
    - "About body 'По цене' paragraph (now in src/content/about.tsx) no longer claims 10 проверок в месяц бесплатно — describes Pro pricing only + install/registration being free"
    - "pricing.free.* keys are deleted from BOTH ru.json and en.json (8 keys per locale = 16 dead keys removed)"
    - "scripts/seo-routes.ts SOFTWARE_APP_BLOCK offers array no longer contains the Free-tier Offer entry — keeps only the two paid offers (RUB Pro Monthly, USD Pro Monthly)"
    - "scripts/seo-routes.ts RU /pay ogDescription no longer ends with 'Начни бесплатно' tail — reworded to Pro-tier-only messaging"
    - "Build-rendered dist contains 0 hits for: '10 проверок' OR '10 generations' OR 'free tier' (case-insensitive) OR 'бесплатн.* проверк' regex"
    - "App.tsx landing Pricing block still renders both Pro cards (Pro Subscription + Pro One-time) without regression — pricing.pro.* and pricing.onetime.* keys preserved"
    - "Install/registration 'free' copy stays — App.tsx:430-431 'Скачай бесплатно. Перейди на Pro' AND cta.freeLabel 'Установка и регистрация бесплатно' both unchanged (factually true)"
  artifacts:
    - path: "src/content/landingFaq.ts"
      provides: "Rewritten Q2 RU + EN — Pro-tier pricing only"
      contains: "199"
    - path: "src/content/about.tsx"
      provides: "Rewritten По цене paragraph in section 3 — Pro-tier pricing only"
      contains: "199"
    - path: "src/i18n/ru.json"
      provides: "pricing.free.* keys deleted (8 keys removed)"
      contains: "pricing.pro"
    - path: "src/i18n/en.json"
      provides: "pricing.free.* keys deleted (8 keys removed)"
      contains: "pricing.pro"
    - path: "scripts/seo-routes.ts"
      provides: "SOFTWARE_APP_BLOCK offers array reduced from 3 entries to 2 paid entries (Free tier removed); RU /pay ogDescription reworded"
      contains: "SOFTWARE_APP_BLOCK"
  key_links:
    - from: "landingFaq.ts ru[1].q + ru[1].a + en[1].q + en[1].a"
      to: "rewritten Q2 — Pro tier only, retains PromptPerfect comparison"
      via: "string rewrite in source file"
      pattern: "199"
    - from: "scripts/seo-routes.ts SOFTWARE_APP_BLOCK.offers"
      to: "2-entry array (Pro Monthly RU + Pro Monthly USD)"
      via: "drop the {price: 0, name: Free tier} entry at line 100"
      pattern: "Pro Monthly"
    - from: "scripts/seo-routes.ts /pay entry"
      to: "ogDescription reworded"
      via: "drop 'Начни бесплатно' tail; e.g., 'Перейди на Pro и улучшай промпты в один клик. 199₽/мес или $2.99/мес.'"
      pattern: "ogDescription"
---

<objective>
Close B-01 (free-tier copy purge across FAQ, About, schema, OG description, i18n dead-code) in one atomic commit touching five files. Per CONTEXT D-Wave-10 + user-confirmed pricing reality 2026-05-17: Opten has NO free checks tier; install/registration stay free.

Purpose: User-reported bug — "Бесплатных попыток нет; везде в FAQ раскидали неактуальную информацию про то что есть тариф бесплатный". The site currently advertises a "10 проверок в месяц бесплатно" free tier across multiple surfaces. This wave removes those claims (FAQ Q2 RU + EN, About body "По цене" paragraph) without removing legitimate "install is free" copy (App.tsx Pricing section title + cta.freeLabel "Установка и регистрация бесплатно" — both factually true). It also drops the dead `pricing.free.*` i18n keys (no consumer remains after App.tsx already migrated to Pro-only cards) and the Free-tier Offer entry from the SOFTWARE_APP_BLOCK schema baseline.

Depends on:
- Plan 04.1-02 (Wave 1) — already removed Free-tier from `productBlock` calls on /pay + /en/pay. This wave removes the SEPARATE Free-tier from SOFTWARE_APP_BLOCK.offers (used on `/` + `/en/` schema, NOT /pay) — different array, different removal site.
- Plan 04.1-04 (Wave 2) — already widened FaqBlock readonly types; no FAQ source coupling needed but ordering keeps git history clean.
- Plan 04.1-06 (Wave 1) — already removed flat pricing.heading from both dicts. The pricing.free.* keys we delete are a separate set.
- Plan 04.1-12a (Wave 2) — extracted AboutPage body into `src/content/about.tsx`. THIS plan edits `about.tsx` (the new file), NOT AboutPage.tsx. If 12a has not landed, the executor STOPS — this plan cannot proceed against the unrefactored AboutPage.tsx without doubling work.

Output: One commit modifying 5 files atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md
@.planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md
@src/content/landingFaq.ts
@src/content/about.tsx
@src/i18n/ru.json
@src/i18n/en.json
@scripts/seo-routes.ts
@src/app/App.tsx

<interfaces>
From `src/content/landingFaq.ts`:
- `ru[1]` (index 1 — second item) currently has Q: `"Сколько стоит и есть ли бесплатная версия?"` and a multi-sentence A that claims `"Бесплатно — 10 проверок промптов в месяц. Этого хватает попробовать..."` (line 23). REWRITE.
- `en[1]` (second item) currently has Q: `"How much does it cost and is there a free tier?"` and A starting `"Free tier: 10 prompt checks per month..."` (line 46). REWRITE.

Per CONTEXT D-Wave-10 + user confirmation:
- New RU Q (heading): `"Сколько стоит Opten?"`
- New RU A: describe Pro Monthly (199₽/мес via YooKassa, $2.99/мес via Paddle), Pro One-time (299₽ via YooKassa, $4.99 via Paddle); note that install + registration are free; keep the PromptPerfect comparison ("в пять раз дешевле PromptPerfect ($9.99)"); keep the "работает в десятках интерфейсов, а не только в собственном чате" tail. NO free-checks claim.
  - Suggested RU A draft (executor may refine wording to match the author's voice and JSON-escape any apostrophes/quotes):
    `"Pro — 199₽/мес с автопродлением через ЮKassa или 299₽ разово без подписки. Для долларовых платежей через Paddle — $2.99/мес или $4.99 разово. Установка расширения и регистрация бесплатны. Это в пять раз дешевле PromptPerfect ($9.99), и Opten работает не только в собственном чате, а внутри привычных генераторов (Syntx, Higgsfield, Freepik)."`
- New EN Q (heading): `"How much does Opten cost?"`
- New EN A: symmetric rewrite — Pro Monthly $2.99/mo (Paddle) + 199₽/mo (YooKassa); Pro One-time $4.99 (Paddle) + 299₽ (YooKassa); install + registration are free; PromptPerfect comparison; works inside familiar generators.
  - Suggested EN A draft:
    `"Pro — $2.99/mo (via Paddle) or $4.99 one-time without subscription. For ruble payments via YooKassa — 199₽/mo or 299₽ one-time. Installing the extension and creating an account are free. This is five times cheaper than PromptPerfect ($9.99), and Opten works inside familiar generators (Syntx, Higgsfield, Freepik) rather than its own chat window."`

From `src/content/about.tsx` (post-W12a — the body data file):
- The "По цене" paragraph is the LAST paragraph of `aboutContent.ru.sections[2].body` (the "Что вы получаете" section's closing `<p>`). Original copy (from AboutPage.tsx line 116-121 pre-refactor): `По цене: <strong>10 проверок в месяц бесплатно</strong>. Pro — 199 ₽/мес с автопродлением через ЮKassa, либо 299 ₽ разово без подписки на месяц. Для платежей в долларах — Paddle, около $2.99. Это в пять раз дешевле PromptPerfect ($9.99)...`
- REWRITE to drop "10 проверок в месяц бесплатно" and replace with "Установка и регистрация бесплатны", while keeping the Pro pricing breakdown + the PromptPerfect comparison:
  - Suggested RU rewrite:
    `<p className="mb-[14px]">По цене: <strong className="text-white font-semibold">установка и регистрация бесплатны</strong>. Pro — 199 ₽/мес с автопродлением через ЮKassa, либо 299 ₽ разово без подписки на месяц. Для платежей в долларах — Paddle, около $2.99/мес или $4.99 разово. Это в пять раз дешевле PromptPerfect ($9.99), и при этом Opten работает в десятках интерфейсов, а не только в собственном чате.</p>`

From `src/i18n/ru.json` lines 61-69 (post-Wave-6 — the flat pricing.heading is already deleted):
```
"pricing.free.name": "Бесплатно",
"pricing.free.price": "0₽",
"pricing.free.period": "в месяц",
"pricing.free.feature1": "10 генераций в месяц",
"pricing.free.feature2": "Оценка по всем 43+ моделям",
"pricing.free.feature3": "Цветная шкала + рекомендации",
"pricing.free.feature4": "Работа в syntx.ai",
```
(Note: 7 visible keys in the snippet. Confirm exact count via Grep `"pricing.free."` — if there's a `pricing.free.button` or similar, it's also dead. Delete ALL `pricing.free.*` keys; per seed: 8 keys × 2 files = 16 keys.)

From `src/i18n/en.json` lines 61-69 — symmetric. Same deletion.

BEFORE deletion, run `grep -rn 'pricing.free' src/ scripts/ index.html 2>/dev/null` to confirm zero consumers. If any consumer is found, surface it as a blocker — do NOT delete dead keys that turn out to be live.

From `scripts/seo-routes.ts`:
- Lines 99-103 (`SOFTWARE_APP_BLOCK.offers` array): currently 3 entries: `{Free tier, 0, USD}, {Pro Monthly, 2.99, USD}, {Pro Monthly (RU), 199, RUB}`. Remove the FIRST entry (`{Free tier, 0, USD}`). Array becomes 2 entries: `{Pro Monthly, 2.99, USD}, {Pro Monthly (RU), 199, RUB}`.
- Line 246 (RU `/pay` `ogDescription`): currently `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или $2.99/мес. Начни бесплатно."` — rewrite to drop the "Начни бесплатно" tail. Suggested replacement: `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или 299₽ разово."` (alternative: `"Pro-подписка для Opten: 199₽/мес или $2.99/мес. Чисто платный план."` — pick the cleaner phrasing that fits the description meta length).
- Line 482 EN `/en/pay` `ogDescription`: currently `"Upgrade to Pro and improve prompts in one click. $2.99/mo or 199₽/mo. Start for free."` — same treatment. Suggested replacement: `"Upgrade to Pro and improve prompts in one click. $2.99/mo or $4.99 one-time."`

From `src/app/App.tsx` (POST-Wave-6 form — pricing.heading.{lead,accent,tail} now compose the heading; the section-title text is "Скачай бесплатно. Перейди на Pro, когда будешь готов." / "Start Free. Upgrade To Pro When You're Ready."). These are INSTALL-related claims, not USAGE — per CAPTURED-DECISIONS Note at end of Wave 10 section: "Скачай бесплатно. Перейди на Pro" and `cta.freeLabel` stay. **Do NOT touch the pricing.heading.* keys.**

Also DO NOT touch the `pay.onetime.tryBtn` / `pay.pro.tryBtn` / `pricing.pro.*` / `pricing.onetime.*` keys — they describe paid tiers and are correct.

Grep gate post-build (from seed): `grep -ri "10 проверок\\|10 generations\\|free tier\\|бесплатн.* проверк" dist/` returns 0 hits in any rendered HTML / sitemap / llms-full.txt. Confirm install-related "бесплатн" copy on the LANDING (`Скачай бесплатно`, `Установка и регистрация бесплатно`) doesn't trip the regex — the regex is bounded by "проверк" / "tier" / "generation" so install-related copy passes through. (Validate this by spot-running the regex before commit.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite landingFaq.ts Q2 RU + EN (drop free-tier claim, keep Pro pricing + PromptPerfect comparison)</name>
  <files>src/content/landingFaq.ts</files>
  <read_first>
    - src/content/landingFaq.ts (the full file — 63 lines)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 10: Free tier copy purge (B-01)" item 1
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Wave 10"
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"B-01: Pricing reality (2026-05-17 chat)" — pricing facts of record
  </read_first>
  <action>
    Edit `src/content/landingFaq.ts` lines 21-24 (ru[1]) + lines 44-47 (en[1]):

    (a) **RU Q2**: change Q from `"Сколько стоит и есть ли бесплатная версия?"` to `"Сколько стоит Opten?"`. Change A to (use exact string, including periods + spaces):
    `"Pro — 199₽/мес с автопродлением через ЮKassa или 299₽ разово без подписки. Для долларовых платежей через Paddle — $2.99/мес или $4.99 разово. Установка расширения и регистрация бесплатны. Это в пять раз дешевле PromptPerfect ($9.99), и Opten работает не только в собственном чате, а внутри привычных генераторов (Syntx, Higgsfield, Freepik)."`

    (b) **EN Q2**: change Q from `"How much does it cost and is there a free tier?"` to `"How much does Opten cost?"`. Change A to:
    `"Pro — $2.99/mo (via Paddle) or $4.99 one-time without subscription. For ruble payments via YooKassa — 199₽/mo or 299₽ one-time. Installing the extension and creating an account are free. This is five times cheaper than PromptPerfect ($9.99), and Opten works inside familiar generators (Syntx, Higgsfield, Freepik) rather than its own chat window."`

    The other 4 Q&A pairs (Q1, Q3, Q4, Q5) — UNCHANGED in this plan. (Wave 11 will modify Q1 + Q5 for Magnific mention; this plan is B-01 scope only.)

    Keep the `as const` export and the `FaqItem` interface untouched. Keep the file header comment unchanged.

    Editor's note for the executor: if the author's voice in the new Q2 needs gentle tuning (e.g., punctuation, comma vs em-dash style), use editorial judgment but preserve the factual claims exactly: 199₽/мес, 299₽ разово, $2.99/мес, $4.99 разово, install + registration free, PromptPerfect $9.99 comparison, "works in familiar generators" tail. Don't add a footnote or asterisk about the free tier — the topic is closed per user decision.
  </action>
  <verify>
    <automated>grep -q '"Сколько стоит Opten?"' src/content/landingFaq.ts && grep -q '"How much does Opten cost?"' src/content/landingFaq.ts && ! grep -q "бесплатно — 10 проверок" src/content/landingFaq.ts && ! grep -q "Free tier: 10 prompt checks" src/content/landingFaq.ts && npm run build</automated>
  </verify>
  <done>
    Q2 (index 1) in both `ru` and `en` arrays carries the new Pro-only copy. No "10 проверок" / "10 prompt checks" / "Free tier:" substrings remain. Build succeeds. FAQ schema verifier (Wave 5) confirms parity between visible Q/A and JSON-LD (visible "Сколько стоит Opten?" should match the schema's `mainEntity[1].name`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite "По цене" paragraph in src/content/about.tsx aboutContent.ru.sections[2].body</name>
  <files>src/content/about.tsx</files>
  <read_first>
    - src/content/about.tsx (the full file — created in W12a)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 10: Free tier copy purge (B-01)" item 2
  </read_first>
  <action>
    In `src/content/about.tsx`, locate `aboutContent.ru.sections[2].body` (the "Что вы получаете" section). Inside its body fragment, the LAST `<p>` is the "По цене" paragraph — originally `По цене: <strong>10 проверок в месяц бесплатно</strong>. Pro — 199 ₽/мес с автопродлением через ЮKassa, либо 299 ₽ разово без подписки на месяц. Для платежей в долларах — Paddle, около $2.99...` (etc.).

    REPLACE this paragraph with:
    ```jsx
    <p className="mb-[14px]">
      По цене: <strong className="text-white font-semibold">установка и регистрация бесплатны</strong>. Pro — 199 ₽/мес с автопродлением через ЮKassa, либо 299 ₽ разово без подписки на месяц. Для платежей в долларах — Paddle, около $2.99/мес или $4.99 разово. Это в пять раз дешевле PromptPerfect ($9.99), и при этом Opten работает в десятках интерфейсов, а не только в собственном чате.
    </p>
    ```

    Preserve `className`s on `<p>` and `<strong>` (post-Wave-7 Tailwind classes). Preserve the surrounding `<p>` tags before this one (the bullet list etc.) and the section structure. Other sections (1, 2, 4) — UNCHANGED in this plan (Wave 11 will add Magnific in section 2 + section 3 elsewhere; that's B-02 scope).
  </action>
  <verify>
    <automated>grep -q "установка и регистрация бесплатны" src/content/about.tsx && ! grep -q "10 проверок в месяц бесплатно" src/content/about.tsx && npm run build && grep -q "установка и регистрация бесплатны" dist/about/index.html && ! grep -q "10 проверок в месяц бесплатно" dist/about/index.html</automated>
  </verify>
  <done>
    The "По цене" paragraph in `src/content/about.tsx` no longer mentions "10 проверок в месяц бесплатно"; it now leads with "установка и регистрация бесплатны". `dist/about/index.html` reflects the change. The rest of the about body is byte-for-byte unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete pricing.free.* keys from ru.json + en.json (after grep-confirming zero consumers)</name>
  <files>src/i18n/ru.json, src/i18n/en.json</files>
  <read_first>
    - src/i18n/ru.json (lines 60-85 — the pricing.* group)
    - src/i18n/en.json (lines 60-85 — the pricing.* group)
    - Run Grep with pattern `pricing\\.free\\.` across `src/` + `scripts/` + `index.html` — must return ZERO consumer hits in *.tsx/*.ts/*.html source code (matches inside ru.json/en.json themselves don't count). If a consumer is found, STOP and surface the blocker.
  </read_first>
  <action>
    Pre-check: `grep -rn "pricing\\.free\\." src/ scripts/ index.html | grep -v "/i18n/"` — must return 0 hits.

    If clean, edit both `src/i18n/ru.json` and `src/i18n/en.json`:
    - Identify all `"pricing.free.*"` keys (per seed: 8 keys per file = 16 keys total; verify exact count via `grep -c '"pricing.free\\.' src/i18n/ru.json` and same for en.json).
    - Delete ALL `pricing.free.*` keys from both files.
    - Preserve valid JSON syntax — adjust trailing commas as needed.

    DO NOT touch `pricing.heading.*` (Wave 6 already migrated these to lead/accent/tail), `pricing.pro.*` (Pro Monthly card — legit), `pricing.onetime.*` (Pro One-time card — legit), `pricing.notice.*` if it exists.
  </action>
  <verify>
    <automated>node -e "const r = require('./src/i18n/ru.json'); const e = require('./src/i18n/en.json'); for (const k of Object.keys(r)) if (k.startsWith('pricing.free.')) { console.error('RU still has', k); process.exit(1); } for (const k of Object.keys(e)) if (k.startsWith('pricing.free.')) { console.error('EN still has', k); process.exit(1); } console.log('OK — no pricing.free.* keys remain');" && npm run build</automated>
  </verify>
  <done>
    Both ru.json and en.json contain no `pricing.free.*` keys. Both files remain valid JSON. `npm run build` succeeds (TS pipeline doesn't complain about missing keys because no consumer references them).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Drop Free-tier Offer from SOFTWARE_APP_BLOCK + rewrite /pay ogDescription on RU + EN</name>
  <files>scripts/seo-routes.ts</files>
  <read_first>
    - scripts/seo-routes.ts lines 89-104 (SOFTWARE_APP_BLOCK) + lines 246 (RU /pay ogDescription) + line 482 (EN /en/pay ogDescription)
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 10" items 4 + 5
    - Confirm Wave 2 has already landed — `productBlock([...])` calls on /pay + /en/pay should already lack the Free entry; this plan's SOFTWARE_APP_BLOCK edit is a DIFFERENT removal site (the baseline SoftwareApplication schema offers, not Product offers).
  </read_first>
  <action>
    Two edits to `scripts/seo-routes.ts`:

    (a) **`SOFTWARE_APP_BLOCK.offers` array** (lines 99-103): remove the `{ "@type": "Offer", price: "0", priceCurrency: "USD", name: "Free tier" }` entry. Array becomes 2 entries:
    ```
    offers: [
      { "@type": "Offer", price: "2.99", priceCurrency: "USD", name: "Pro Monthly" },
      { "@type": "Offer", price: "199",  priceCurrency: "RUB", name: "Pro Monthly (RU)" },
    ],
    ```

    (b) **RU `/pay` `ogDescription`** (line 246): change `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или $2.99/мес. Начни бесплатно."` to `"Перейди на Pro и улучшай промпты в один клик. 199₽/мес или 299₽ разово."`. (Keeps within reasonable description length; drops the "Начни бесплатно" tail.)

    (c) **EN `/en/pay` `ogDescription`** (line 482): change `"Upgrade to Pro and improve prompts in one click. $2.99/mo or 199₽/mo. Start for free."` to `"Upgrade to Pro and improve prompts in one click. $2.99/mo or $4.99 one-time."`.

    DO NOT touch any other RouteMeta entry, the WEBSITE_BLOCK, ORG_BLOCK, PERSON_FOUNDER_BLOCK, productBlock/breadcrumbBlock/faqPageBlock/howToBlock factories, or any /pay productBlock call sites (Wave 2 already cleaned those).
  </action>
  <verify>
    <automated>npm run build && ! grep -q 'Free tier' "dist/index.html" && ! grep -q '"name": "Free tier"' "dist/en/index.html" && grep -q 'Pro Monthly' dist/index.html && grep -q 'Pro Monthly' dist/en/index.html && ! grep -q 'Начни бесплатно' dist/pay/index.html && ! grep -q 'Start for free' dist/en/pay/index.html</automated>
  </verify>
  <done>
    `dist/index.html` and `dist/en/index.html` Schema's SoftwareApplication.offers array contains only the 2 paid offers (no Free tier). `dist/pay/index.html` ogDescription meta tag no longer ends with "Начни бесплатно"; `dist/en/pay/index.html` no longer ends with "Start for free". Both /pay routes still carry the Pro pricing strings (`199`, `2.99`, `299`, `$4.99`) in their visible card content.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Final phase-close grep gate — confirm no free-tier copy survives in dist/</name>
  <files>(no files modified — gate)</files>
  <read_first>
    - .planning/phases/04.1-content-surface-cleanup/04.1-CAPTURED-DECISIONS.md §"Wave 10" Verify step
    - .planning/phases/04.1-content-surface-cleanup/04.1-CONTEXT.md §"Verification at phase close" item 5
  </read_first>
  <action>
    After Tasks 1-4 land + `npm run build` completes, run the phase-close regex:
    `grep -riE "10 проверок|10 generations|free tier|бесплатн[а-я]* проверк" dist/ | grep -v "binary file"`

    Expected output: 0 lines. If non-zero, investigate the offending file:
    - Hits inside `dist/llms-full.txt` for legitimate references (e.g., a "free tier" mention inside a quoted historical comment) would need source-level removal.
    - Hits inside `dist/<route>/index.html` indicate a missed copy edit (re-trace through landingFaq.ts / about.tsx / i18n / schema).
    - The `cta.freeLabel "Установка и регистрация бесплатно"` and pricing.heading.lead "Скачай бесплатно. " should NOT match the regex (no "проверк", no "tier", no "generation" tokens) — confirm by manual grep.

    DO NOT silently accept any hits. The success criterion of B-01 is zero leak.
  </action>
  <verify>
    <automated>npm run build && [ "$(grep -riE '10 проверок|10 generations|free tier|бесплатн[а-я]* проверк' dist/ | grep -v -i 'binary' | wc -l)" -eq 0 ]</automated>
  </verify>
  <done>
    The phase-close grep returns 0 hits across all of `dist/`. B-01 is closed.
  </done>
</task>

</tasks>

<verification>
  One atomic commit lands.

  Acceptance gates:
  1. `npm run build` exits 0; the new FAQ-mainentity verifier (Wave 5) still passes (visible Q2 == JSON-LD mainEntity Q2 after the rewrite).
  2. `grep -ri "10 проверок\\|10 generations\\|free tier\\|бесплатн.* проверк" dist/` returns 0 hits.
  3. Landing Pricing block still renders both Pro cards: `grep -c "Pro" dist/index.html` ≥ 4 (multiple Pro references); `dist/index.html` still emits visible 199₽ / 299₽ + the two PlanCard markup blocks.
  4. App.tsx pricing.heading.{lead,accent,tail} still composes correctly (Wave 6 invariant): `dist/index.html` contains "Скачай бесплатно" (from pricing.heading.lead — install-related, factually true, NOT a usage claim) and `dist/en/index.html` contains "Start Free".
  5. SoftwareApplication schema on `/` + `/en/` has 2 offers (Pro RU + Pro USD), no Free tier.
  6. `/pay` + `/en/pay` ogDescription meta tags no longer mention "Начни бесплатно" / "Start for free".
</verification>

<success_criteria>
  1. B-01 closed — free-tier usage claims purged from FAQ, About, schema, OG description, i18n dead code.
  2. Install/registration "free" copy preserved (factually true).
  3. Pro pricing facts correctly stated everywhere (199₽/мес, 299₽ разово, $2.99/мес, $4.99 разово).
  4. PromptPerfect comparison preserved.
  5. No regression — landing Pricing block, Pro cards, navigation, FAQ schema parity all hold.
</success_criteria>

<output>
  After completion, create `.planning/phases/04.1-content-surface-cleanup/04.1-10-SUMMARY.md` covering:
  - Diff line counts per file.
  - Final phase-close grep result (must be 0).
  - Sample of the new Q2 RU + EN copy (for the audit trail).
  - Confirmation that the FAQ-mainentity verifier (Wave 5) passes with the rewritten Q.

  Commit message: `fix(04.1): remove obsolete free-tier copy from FAQ, schema, and i18n dead-code (B-01)`
</output>
</content>
</invoke>