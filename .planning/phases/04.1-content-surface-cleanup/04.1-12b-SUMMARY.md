---
phase: 04.1
plan: 12b
status: complete
commit: 5799b6e
approved_by_user: 2026-05-17
gates_passed:
  - en_copy_review
  - route_registration_review
---
# 04.1-12b Summary: /en/about EN sibling (B-03)

**Closes:** B-03 (user-reported 2026-05-17 — "мы добавили вкладку в 4й фазе о проекте но на английском её нет. Также на этой странице я не могу переключить язык")
**Commit:** `5799b6e`
**Files changed:** 6 atomic (+157 / −8)

## What changed

### 1. `src/content/about.tsx` (+113 / −4)
- Replaced `aboutContent.en = null` sentinel with full EN translation (~750 words across 4 sections).
- Sections mirror RU structure: "How Opten came to be" / "Why prompts specifically" / "What you get" / "Contact".
- Tone: first-person founder voice; no marketing puffery; same register as RU.
- All factual claims preserved: YouTube channel + 4,500 subs + 54 videos + ~7M views, web designer background, 60 skill files list (Seedance/Seedream/Kling/Midjourney/GPT Image/Flux/Nano Banana/Imagen/Luma Ray/Sora 2/Veo 3.1/Runway/MiniMax Hailuo/Pixverse/LTX/Wan/Qwen/Grok Imagine), Syntx/Higgsfield/Freepik/Magnific support, 7-bullet feature list (0-100 score, callouts, auto-enhance, multimodal, auto-detected model, privacy via Claude Haiku 4.5 + Vercel proxy, RU/EN), full pricing (199₽/mo YooKassa, 299₽ one-time YooKassa, $2.99/mo Paddle, $4.99 one-time Paddle, install+registration free), PromptPerfect $9.99 comparison, Telegram @v_voronezhtsev, YouTube @v.voronezhtsev, legal block (IE Voronezhtsev Vladislav Pavlovich, INN 723016676391, Tyumen; IE Nikolai Shupletsov Paddle Merchant of Record).
- Internal links use `<LocalizedLink to="/terms">` and `<LocalizedLink to="/refund">` — auto-prefix to `/en/terms` + `/en/refund` on EN routes per `localizeHref()`.

### 2. `src/i18n/paths.ts` (+1)
- `EN_SIBLINGS` gains `"/about"` (8th entry, placed between `/refund` and `/guides/gpt-image-2` for grouping).
- Enables LangSwitcher round-trip RU `/about` ↔ EN `/en/about` and LocalizedLink prefix-preservation on `/en/about`.

### 3. `src/main.tsx` (+1)
- `<Route path="/en/about" element={<AboutPage />} />` added in EN block, after `/en/refund`, before `/en/guides/:slug`.
- AboutPage was already eager-imported (Phase 4 requirement); no import change needed.

### 4. `scripts/entry-server.tsx` (+2 / −1)
- `<Route path="/en/about" element={<AboutPage />} />` added in SSR EN block.
- Comment updated from "Phase 4 D-01/D-02: /about is RU-only — no /en/about mount" to "Phase 4.1 B-03: /en/about now mounted (RU-only restriction lifted)".

### 5. `scripts/seo-routes.ts` (+34 / −4)
- RU `/about` `hreflangAlternates.en` flipped from `${SITE_ORIGIN}/` (D-02 landing fallback) to `${SITE_ORIGIN}/en/about` (true reciprocal sibling). Comment updated to reflect B-03 closure.
- New `/en/about` RouteMeta entry added between `/en/refund` and `/en/guides/gpt-image-2`. Fields: `path: "/en/about"`, `htmlLang: "en"`, `hreflangAlternates: { ru: /about, en: /en/about, xDefault: /about }`, EN title + description + canonical, `ogImage: DEFAULT_OG_IMAGE_EN`, `prerender: "full"`, `changefreq: "yearly"`, `priority: 0.5`, `schema: [ORG_BLOCK, PERSON_FOUNDER_BLOCK, breadcrumbBlock([Home → About], /en/about)]`.
- `PERSON_FOUNDER_BLOCK` shared with RU entry (same `@id` graph node) — search engines see one Person across both locales.

### 6. `src/i18n/en.json` (+3)
- Added 3 missing keys: `about.tagline`, `about.contactCta`, `about.backLink`.
- Before this, AboutPage chrome on `/en/about` would render literal key strings (e.g., "about.tagline") because the keys existed only in `ru.json`.

## What did NOT change

- `src/app/pages/AboutPage.tsx` — untouched per W12b scope. It already consumes `aboutContent[lang] ?? aboutContent.ru` (post-W12a), so the EN render path activates automatically once `aboutContent.en` is non-null.
- `aboutContent.ru` body — untouched. Only the RU `/about` `hreflangAlternates.en` field on the seo-routes.ts entry changed.
- AboutPage hero — still shows "Влад Воронежцев" + "ВВ" initials in Cyrillic regardless of locale. User confirmed this is acceptable; transliteration to "Vlad Voronezhtsev" / "VV" was offered in the approval gate and declined.

## Verification

### Build
- `npm run build` exits 0; 16 routes emitted (was 15 before W12b).
- `/en/about → \dist\en\about\index.html (full)` confirmed in build output.

### Post-build assertions (all PASS)
- `test -f dist/en/about/index.html` — file exists
- `grep -c '<html lang="en">' dist/en/about/index.html` = 1
- `grep -c 'hreflang' dist/en/about/index.html` = 3 (ru + en + x-default triplet)
- `grep -c 'og-card-en' dist/en/about/index.html` = 2 (og:image + twitter:image)
- `grep -c 'How Opten came to be' dist/en/about/index.html` = 1 (EN body landmark)
- `grep -c 'person-founder' dist/en/about/index.html` = 2 (Person schema present, same @id as RU)
- `grep -c '/en/about' dist/sitemap.xml` = 3 (entry + 2 xhtml:link refs)
- `dist/about/index.html` hreflang.en line now points at `https://opten.space/en/about` (verified — was previously `https://opten.space/`)

### Build-time gates (still passing, no regression)
- `verify-faq-mainentity`: 4 routes checked, 20 questions, parity holds
- `applyHeroPreloadGuard`: all 16 routes have both font preloads
- `applyOgImage`: 8 RU routes ship og-card-ru.png x2; 7 EN routes ship og-card-en.png x2; `/en/about` now in the EN x2 set (verified above)

### Manual smoke (deferred to post-deploy UAT)
- LangSwitcher round-trip: RU `/about` → EN `/en/about` → RU `/about` (relies on EN_SIBLINGS set membership; both directions wired via `toEnTarget` / `toRuTarget`).
- Telegram link unfurl on `/en/about` should now show og-card-en.png.
- All LocalizedLink references inside `aboutContent.en` (Terms + Refund) should resolve to `/en/terms` + `/en/refund` when clicked on `/en/about`.

## Approval audit trail

- **Gate 1 (EN copy review):** EN translation drafted in `src/content/about.tsx`, summary surfaced to user 2026-05-17 with sections + key factual coverage. User selected "Approve EN copy + proceed (Recommended)".
- **Gate 2 (route registration):** 5 file edits + post-build gate results surfaced. User selected "Approve commit (Recommended)".

## Notes / deviations

- **Word count:** EN translation came in at ~750 words vs ~1500 estimated in the seed. RU original was ~520 words. The EN expansion (~750 words) is proportional to RU register; the seed's 1500-word estimate was loose. User reviewed and approved the EN copy at the actual length.
- **AboutPage hero locale-awareness:** offered to user as a possible scope extension (transliterate "Влад Воронежцев" → "Vlad Voronezhtsev" on /en/about); user declined. Cyrillic-name display on EN page is intentional and matches founder preference (real names are not translated; transliteration adds ambiguity).
- No other deviations.

## Out of scope (deferred)

- AboutPage hero photo (the `<span>ВВ</span>` initials placeholder is intentional per Phase 4 locked decision option (c) — photo deferred indefinitely).
- Latin transliteration of founder name on EN page (declined by user this round; can revisit later if needed).
