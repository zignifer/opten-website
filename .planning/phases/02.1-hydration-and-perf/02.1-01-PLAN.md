---
phase: 02.1-hydration-and-perf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - src/types/imagetools.d.ts
autonomous: true
requirements:
  - SC-6
must_haves:
  truths:
    - "vite-imagetools@6.2.9 plugin is registered in vite.config.ts plugin array"
    - "Full build chain (vite build + 2x SSR builds + prerender + sitemap) passes cleanly"
    - "TypeScript declaration for vite-imagetools query-string imports is present so future ?as=picture imports type-check"
    - "No runtime behavior change — no JSX touches App.tsx, no imagetools query-string imports added yet"
  artifacts:
    - path: "vite.config.ts"
      provides: "imagetools() plugin registered after react()"
      contains: "import { imagetools } from 'vite-imagetools'"
    - path: "src/types/imagetools.d.ts"
      provides: "Triple-slash reference enabling Picture type from vite-imagetools"
      contains: "/// <reference types=\"vite-imagetools\" />"
  key_links:
    - from: "vite.config.ts"
      to: "node_modules/vite-imagetools"
      via: "named import { imagetools }"
      pattern: "from\\s+['\"]vite-imagetools['\"]"
    - from: "src/types/imagetools.d.ts"
      to: "vite-imagetools shipped types"
      via: "TripleSlash reference"
      pattern: "reference types=\"vite-imagetools\""
---

<objective>
Register the `vite-imagetools@6.2.9` build plugin (already installed as devDep) so subsequent plans can use `?w=...&format=webp;png&as=picture` query-string imports. This plan is **pure infrastructure wiring** — no JSX, no behavior change. It is the safest possible change to ship first.

Also publish the TypeScript declaration for `vite-imagetools`'s `Picture` type so plan 02 (`Picture.tsx`) can import `type { Picture as PictureData } from 'vite-imagetools'` without a TS error.

**Purpose:** Land plugin registration in isolation so any build regression is unambiguously attributable to plugin wiring, not JSX changes.

**Output:**
1. `vite.config.ts` — `imagetools()` added to the plugin array, after `react()` and `tailwindcss()`.
2. `src/types/imagetools.d.ts` — new file with the triple-slash reference so `import type { Picture } from 'vite-imagetools'` resolves under `tsconfig`.

**SC mapping:** SC-6 (no regression — full build chain still green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02.1-hydration-and-perf/02.1-CONTEXT.md
@.planning/phases/02.1-hydration-and-perf/02.1-RESEARCH.md
@.planning/phases/02.1-hydration-and-perf/02.1-PATTERNS.md
@vite.config.ts
@package.json
@src/types/paddle.d.ts
</context>

<interfaces>
<!-- Type contract this plan establishes for plan 02 (Picture.tsx) -->

`vite-imagetools@6.2.9` exports the following type via `node_modules/vite-imagetools/dist/types.d.ts`:

```ts
// Approximate shape (per RESEARCH.md A1 — exact field names verified in plan 02 Wave 0 check)
export interface Picture {
  sources: Array<{ srcset: string; type: string }>;
  img: { src: string; w?: number; h?: number };
}
```

After this plan ships, plan 02 can write `import type { Picture as PictureData } from 'vite-imagetools'` and `tsc` (via `vite build`) resolves it.

`package.json` already declares `"vite-imagetools": "^6.2.9"` in devDependencies (line 75). Do NOT modify package.json in this plan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Register imagetools() plugin in vite.config.ts</name>
  <files>vite.config.ts</files>
  <action>
Add `vite-imagetools` registration to the existing minimal `vite.config.ts`. Per D-05 and PATTERNS.md "vite.config.ts" section, plugin ordering is `react()` first, `tailwindcss()` second, `imagetools()` third. No reordering of existing plugins. No removal of `assetsInclude: ['**/*.svg', '**/*.csv']` (that line treats SVG/CSV as raw imports and does NOT conflict with imagetools, which operates only on PNG/JPG/WebP query-string imports).

Concrete edit:
1. Add a new named import line below `import react from '@vitejs/plugin-react'`:
   `import { imagetools } from 'vite-imagetools'`
2. Inside the `plugins: [...]` array, insert `imagetools(),` as the third entry, after `tailwindcss(),`. Keep the existing inline comment block above `react()` intact.

Add a `// Phase 2.1 D-05:` prefix comment on the line above `imagetools(),` per the codebase phase-comment convention (PATTERNS.md "Phase/bug comment style").

DO NOT pass any options to `imagetools()` (default config is the locked decision — see CONTEXT.md D-05). DO NOT add `rollup-plugin-visualizer` to this config — the visualizer is run via a one-off config or `ANALYZE` env-gated registration in a later optional step; it is not required by Phase 2.1 success criteria.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -40</automated>
Build chain (`vite build` + 2x `vite build --ssr` + `node scripts/prerender.mjs` + `node scripts/sitemap.mjs`) MUST exit 0. The `vite build` step MUST emit `index-*.js` ≈ 362 KB (unchanged from baseline — no imagetools imports yet, so plugin is a no-op on the module graph). SSR builds (`.ssr-cache/entry-server.js` ≈ 159 KB, `.ssr-meta/seo-routes.js` ≈ 4.5 KB) MUST succeed.

Sanity grep:
```
grep -n "imagetools" vite.config.ts
```
MUST return exactly 2 lines (the `import` and the `imagetools()` call).
  </verify>
  <done>
- `vite.config.ts` has `import { imagetools } from 'vite-imagetools'` and `imagetools(),` in the plugins array.
- `npm run build` exits 0.
- Main chunk size in `dist/assets/index-*.js` is within ±2 KB of the 362 KB baseline.
- `dist/index.html` is identical in structure to pre-change baseline (still has `<script type="module" crossorigin src="/assets/index-*.js">`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add src/types/imagetools.d.ts triple-slash reference</name>
  <files>src/types/imagetools.d.ts</files>
  <action>
Create a NEW file `src/types/imagetools.d.ts` with EXACTLY this single line (plus optional trailing newline):

```
/// <reference types="vite-imagetools" />
```

This makes the `Picture` type exported by `vite-imagetools` available without each consumer site needing its own reference. Mirror the existing `src/types/paddle.d.ts` pattern (PATTERNS.md "TypeScript declaration required" note) — same directory, same `.d.ts` suffix, named after the package it augments.

DO NOT add any other declarations to this file. DO NOT modify `src/types/paddle.d.ts`. DO NOT modify `tsconfig.json` (the existing `tsconfig` `include` already picks up `src/**/*.d.ts`).
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; cat src/types/imagetools.d.ts</automated>
File MUST contain exactly the `/// <reference types="vite-imagetools" />` line. `npm run build` MUST exit 0 (no new TS errors — the reference is now valid because `vite-imagetools` is installed and ships its own types).
  </verify>
  <done>
- File `src/types/imagetools.d.ts` exists with the triple-slash reference.
- `npm run build` still exits 0.
- No TypeScript errors related to `vite-imagetools` in build output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host → npm registry | `vite-imagetools` was installed at devDep-install time; runs only at `vite build`. No runtime browser exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-01 | Tampering | vite-imagetools plugin (build-time image transform) | mitigate | Pin version to `^6.2.9` in package.json devDependencies (already pinned). RESEARCH.md confirms 6.2.9 was vetted against Vite 6.3.5; subsequent 7.x–10.x require Vite ≥ 7 and are not installable here. No CDN load — package is local to `node_modules`. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `dist/index.html` structure unchanged vs. baseline (script tag present at end of head/start of body — Vite default).
- `dist/assets/index-*.js` size within ±2 KB of 362 KB baseline.
- `grep -n imagetools vite.config.ts` returns 2 matches.
- `cat src/types/imagetools.d.ts` returns the triple-slash reference line.
- `git diff --stat` shows changes ONLY in `vite.config.ts` and `src/types/imagetools.d.ts` (new file).
</verification>

<success_criteria>
SC-6 (no regression): full build chain green, dist/index.html structurally identical to baseline, main JS chunk size unchanged.
</success_criteria>

<output>
Create `.planning/phases/02.1-hydration-and-perf/02.1-01-SUMMARY.md` documenting:
- The two files modified/created.
- The exact baseline vs post-plan chunk size from `dist/assets/`.
- Commit SHA for `git revert` rollback path.

Atomic commit message:
```
build(02.1): register vite-imagetools plugin + add TS declaration

Plan 02.1-01 — Phase 2.1 D-05 infrastructure prep. Registers
vite-imagetools@6.2.9 (already a devDep) in vite.config.ts and
publishes src/types/imagetools.d.ts triple-slash reference so
downstream plans can use ?w=...&format=webp;png&as=picture imports.

No JSX changes; no behavior change. Full build chain green.
```
</output>
