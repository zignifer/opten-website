---
phase: 02.1-hydration-and-perf
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/RouteLoading.tsx
autonomous: true
requirements:
  - SC-6
must_haves:
  truths:
    - "RouteLoading component exists, renders a full-viewport dark spinner on #011417"
    - "Component is CSS-only (Tailwind animate-spin) — no SVG, no library, ~200 bytes"
    - "Component is NOT yet imported anywhere — net-new file, zero blast radius"
    - "Component has role=status + aria-label=Loading for screen readers"
  artifacts:
    - path: "src/app/components/RouteLoading.tsx"
      provides: "export function RouteLoading() — Suspense fallback for plan 07"
      contains: "export function RouteLoading"
      min_lines: 10
  key_links:
    - from: "src/app/components/RouteLoading.tsx (this plan)"
      to: "src/main.tsx (plan 07)"
      via: "import { RouteLoading } from \"./app/components/RouteLoading\""
      pattern: "from\\s+['\"].*RouteLoading['\"]"
---

<objective>
Create the Suspense fallback component `src/app/components/RouteLoading.tsx` per UI-SPEC.md "New Component: RouteLoading.tsx" (lines 53–110) and CONTEXT.md D-02.

This plan introduces the component **but does NOT wire it into `main.tsx`**. Wiring happens in plan 02.1-07 (the highest-risk plan, scheduled last per Phase 2 lesson). Landing the component alone is safe — TypeScript tree-shakes the unused export, so the production bundle is unchanged.

**Purpose:** Lock the visual + accessibility contract for the lazy-route fallback before the main.tsx lazy switch. If anything about the spinner needs revision (color, size, ARIA), it surfaces in this plan, not in plan 07.

**Output:** One new file with a named `RouteLoading` export.

**SC mapping:** SC-6 (no regression — unused component is tree-shaken from production bundle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02.1-hydration-and-perf/02.1-CONTEXT.md
@.planning/phases/02.1-hydration-and-perf/02.1-UI-SPEC.md
@.planning/phases/02.1-hydration-and-perf/02.1-PATTERNS.md
@src/app/App.tsx
@src/styles/theme.css
</context>

<interfaces>
<!-- Contract this plan publishes for plan 07 (main.tsx lazy switch) -->

Module: `src/app/components/RouteLoading.tsx`

```ts
export function RouteLoading(): JSX.Element
```

Call-site (used by plan 07):
```tsx
import { RouteLoading } from "./app/components/RouteLoading"
<Suspense fallback={<RouteLoading />}> ... </Suspense>
```

Render output (per UI-SPEC.md "Exact Markup Contract" lines 77–89):
```html
<div class="min-h-screen flex items-center justify-center bg-[#011417]" role="status" aria-label="Loading">
  <div class="h-8 w-8 rounded-full border-2 border-white/20 border-t-white animate-spin"></div>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/components/RouteLoading.tsx</name>
  <files>src/app/components/RouteLoading.tsx</files>
  <action>
Create a NEW file at `src/app/components/RouteLoading.tsx`. The file MUST match the exact markup contract from UI-SPEC.md lines 77–89 byte-for-byte (modulo whitespace). Use a NAMED export — per PATTERNS.md "Named (not default) exports for components".

The required structure:

1. Outer `<div>` element with className `"min-h-screen flex items-center justify-center bg-[#011417]"`, `role="status"`, `aria-label="Loading"`.
2. Inner `<div>` (the spinner itself) with className `"h-8 w-8 rounded-full border-2 border-white/20 border-t-white animate-spin"`.

Constraints:
- No `import React` line (React 18 JSX transform is on — matches the leaf-component pattern at `src/app/App.tsx` line 36 `function Logo()`).
- No props.
- No hooks.
- No state.
- No `import` lines at all (Tailwind classes are processed by `@tailwindcss/vite`; no runtime CSS import needed).
- Use `export function RouteLoading()` — NOT `export default`.
- Add a `// Phase 2.1 D-02:` header comment per PATTERNS.md "Phase/bug comment style".

Color contract: `#011417` is the brand dark (locked — matches `html`/`body` background in `src/styles/theme.css` per Phase 2 hotfix `f8b9224`). Do NOT substitute `bg-slate-900`, `bg-black`, or any Tailwind preset. The arbitrary value `bg-[#011417]` is required for visual continuity with the prerendered HTML's `<body>` background.

Spinner color contract: `border-white/20` (white at 20% opacity) for the ring, `border-t-white` (full white) for the top segment. These create the visible "spinning quarter" arc. Do NOT change to a brand accent color — the `#9cfb51` lime accent is for landing CTAs, not for system-status indicators.

Accessibility contract (UI-SPEC line 92–99): `role="status"` is a live region that screen readers announce; `aria-label="Loading"` is the announcement text. Tailwind's `animate-spin` respects `prefers-reduced-motion: reduce` by default in Tailwind 4 — no additional `motion-reduce:` modifier needed.

DO NOT add any of:
- A delay before showing the spinner (no `useState` + `setTimeout`).
- A minimum display duration (no artificial debounce).
- A timeout that displays an error after N seconds.
- An animation library import.
- A loading-text label.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; grep -n "export function RouteLoading" src/app/components/RouteLoading.tsx &amp;&amp; grep -c "animate-spin" src/app/components/RouteLoading.tsx</automated>
- `npm run build` MUST exit 0.
- `grep -n "export function RouteLoading"` MUST return exactly 1 match.
- `grep -c "animate-spin"` MUST be ≥ 1.
- Main JS chunk size delta ≤ ±0.5 KB vs. baseline (unused export is tree-shaken; plan 07 will pull it in).
  </verify>
  <done>
- File exists, named export present.
- Build green.
- No other files modified.
- Component ready to import in plan 07.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure presentational component; no network, no storage, no user input. |

## STRIDE Threat Register

No applicable STRIDE categories — leaf JSX component with no I/O.
</threat_model>

<verification>
- Build exits 0.
- File contains the exact markup contract from UI-SPEC.md.
- Bundle size delta ≤ ±0.5 KB.
- `git diff --stat` shows only the new file.
</verification>

<success_criteria>
SC-6 (no regression): build green, bundle unchanged.
</success_criteria>

<output>
Create `.planning/phases/02.1-hydration-and-perf/02.1-03-SUMMARY.md` capturing the final markup as shipped (so plan 07's executor can verify import path and component name without re-reading UI-SPEC).

Atomic commit message:
```
feat(02.1): add RouteLoading Suspense fallback component

Plan 02.1-03 — Phase 2.1 D-02. Net-new file
src/app/components/RouteLoading.tsx. Full-viewport dark spinner
(bg-[#011417], CSS-only animate-spin, role=status). Not yet wired
to <Suspense>; plan 07 wires it in main.tsx.
```
</output>
