---
phase: 2.1
slug: hydration-and-perf
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This phase has **no automated test framework** in the project (CLAUDE.md "no tests, no ESLint config, no `typecheck` script — TS errors surface during `vite build`"). Validation is **build-driven + manual smoke checks**: every change must keep `npm run build` green, and acceptance criteria are verified via Lighthouse mobile + manual mobile-tap-latency tests.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | none (build + manual smoke; no Jest/Vitest/Playwright in repo) |
| **Config file** | none — `vite build` IS the type-and-bundle check |
| **Quick run command** | `npm run build` (exits non-zero on TS errors) |
| **Full suite command** | `npm run build && npx unlighthouse --site http://localhost:8080` (after `npx http-server dist`) |
| **Estimated runtime** | ~30s build + ~60s Lighthouse run |

**Wave 0 install (optional, dev-only, deferred unless researcher recommends):**
- `npm install --save-dev rollup-plugin-visualizer` (treemap generator for bundle-size acceptance check)
- `npm install --save-dev unlighthouse` OR use `npx unlighthouse` per-run

---

## Sampling Rate

- **After every task commit:** Run `npm run build` (≤ 30s; non-zero exit = task is red).
- **After every plan wave:** Run `npm run build`, inspect `dist/index.html` for `<link rel="modulepreload">`, inspect `dist/assets/index-*.js` size, run `npx serve dist` + manual tap test in Chrome DevTools mobile emulation.
- **Before `/gsd-verify-work`:** Full Lighthouse mobile run on production preview. Confirm Performance ≥ 90 (or document why ≥ 75 is acceptable per RESEARCH.md finding that 220 KB target unrealistic), CLS = 0.
- **Max feedback latency:** 30s per task commit, 90s per wave.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 2.1-01-01 | 01 (vite-imagetools plugin registration) | 1 | SC-6 (no regression) | T-2.1-01 | `vite.config.ts` registers `vite-imagetools@^6.2.9` after `react()`; SSR + client builds still exit 0 | build | `npm run build 2>&1 \| tail -20` (all 3 builds + prerender + sitemap exit 0) | ✅ | ⬜ pending |
| 2.1-02-01 | 02 (Picture.tsx component) | 1 | SC-6 (no regression) | — | New file `src/app/components/Picture.tsx` exists; exports `Picture` component; unused at this point (no call sites yet) | build | `test -f src/app/components/Picture.tsx && npm run build 2>&1 \| tail -5` | ✅ | ⬜ pending |
| 2.1-03-01 | 03 (RouteLoading.tsx component) | 1 | SC-6 (no regression) | — | New file `src/app/components/RouteLoading.tsx` exists; CSS-only spinner on `#011417`; unused at this point | build | `test -f src/app/components/RouteLoading.tsx && grep -q "bg-\[#011417\]" src/app/components/RouteLoading.tsx` | ✅ | ⬜ pending |
| 2.1-04-01 | 04 (width/height attrs on all `<img>`) | 2 | SC-4 (CLS=0) | — | Every `<img>` in `src/app/` has explicit `width` + `height` attributes derived from `scripts/img-dims.json` + Tailwind size classes | grep | `! grep -rE '<img \b(?:(?!width=)[^/])*src=' src/app/` (no `<img>` without width attribute) | ✅ | ⬜ pending |
| 2.1-05-01 | 05 (`<Picture>` wrapping in App.tsx) | 3 | SC-6 (no regression) + image-payload win | T-2.1-01 | PNG `<img>` in App.tsx replaced by `<Picture>` (uses `vite-imagetools` static imports + JSX switching for dynamic-src); WebP variants in `dist/assets/` | build | `npm run build && ls dist/assets/*.webp \| wc -l` (expect ≥ 14: 4 partners + 8 features + 2 steps) | ✅ | ⬜ pending |
| 2.1-06-01 | 06 (modulepreload injection in `prerender.mjs`) | 3 | SC-6 + RTT win | — | Every emitted HTML contains `<link rel="modulepreload" href="/assets/index-*.js">` in `<head>` AFTER the Paddle `<script>` tag and BEFORE the `__PRERENDER_PATH` script | build | `for f in dist/index.html dist/welcome/index.html dist/privacy/index.html dist/terms/index.html dist/refund/index.html dist/pay/index.html; do grep -c 'rel="modulepreload"' "$f"; done` (each = 1) | ✅ | ⬜ pending |
| 2.1-07-01 | 07 Task 1 (read-only verify: entry-server.tsx still imports prerendered routes statically) | 4 | D-01 invariant | — | `scripts/entry-server.tsx` retains static imports of `App, WelcomePage, PrivacyPage, TermsPage, RefundPage` — guards against accidental lazy in plan 07 Task 2 | grep | `grep -c "^import [A-Z][a-zA-Z]*\s*from" scripts/entry-server.tsx` (expect ≥ 5) | ✅ | ⬜ pending |
| 2.1-07-02 | 07 Task 2 (lazy() + Suspense in main.tsx) | 4 | SC-1, SC-3 | T-2.1-03, T-2.1-04 | `src/main.tsx`: `React.lazy(() => import(...))` applied to PayPage, AccountPage, SuccessPage, DownloadSkillPage ONLY; `<Routes>` wrapped in `<Suspense fallback={<RouteLoading />}>`; Paddle bootstrap block + `__PRERENDER_PATH` discriminator byte-identical | build + size check | `npm run build && node -e "const fs=require('fs'); const f=fs.readdirSync('dist/assets').filter(n=>n.match(/^index-[a-zA-Z0-9_]+\.js$/))[0]; const b=fs.statSync('dist/assets/'+f).size; process.exit(b<=325*1024?0:1)"` (main chunk ≤ 325 KB) | ✅ | ⬜ pending |
| 2.1-07-03 | 07 Task 3 (post-deploy Lighthouse + tap-latency checkpoint) | 4 | SC-2, SC-5, SC-6 | — | Push to main → Vercel deploys → Lighthouse mobile Performance ≥ 75; CLS = 0; navbar `Menu` hamburger registers `setOpen` within 500ms of FCP on Slow 4G + 4× CPU | manual | `npx unlighthouse --site https://opten.space` + manual tap-latency via Chrome DevTools mobile emulation | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs map to the 7 plan files: `02.1-{NN}-PLAN.md`. Plan 07 has 3 sub-tasks (read-only verify → lazy switch → post-deploy checkpoint).*

---

## Wave 0 Requirements

- [ ] `rollup-plugin-visualizer` installed as devDependency — emits `dist/stats.html` treemap for SC-1 acceptance.
- [ ] `vite-imagetools@6.2.9` installed (Vite 6 compatibility — DO NOT install 10.x per RESEARCH.md).
- [ ] `sharp` already installed via vite-imagetools transitive — no extra install needed.

*If `npm install` succeeds with these three and `npm run build` still produces a green build, Wave 0 is complete.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tap-latency on real iPhone | SC-5 (`<button onClick>` responds ≤ 500ms after FCP on Slow 4G) | Lighthouse mobile emulation does not exercise real touch events; requires Safari Web Inspector with a paired iPhone | 1. Push fix to main → wait for Vercel deploy. 2. Open https://opten.space on iPhone Safari with USB cable to Mac. 3. In Safari → Develop → iPhone → opten.space, open console. 4. Reload page with Network throttled to "Slow 4G" in Network conditions. 5. Tap navbar menu hamburger within 1s of first dark paint. 6. Confirm console shows React onClick handler firing (add `console.log('hamburger tap', Date.now())` temporarily). 7. Acceptance: handler fires within 500ms of paint. |
| Paddle modal still opens on `/pay` after lazy load | SC-6 (no regression) | Paddle.Checkout.open is a side-effectful CDN call; only real interaction proves it works | 1. Open https://opten.space/pay. 2. Wait for Paddle chunk lazy load (spinner shows briefly). 3. Click "Upgrade to Pro" CTA. 4. Acceptance: Paddle modal opens, no console errors. |
| `__PRERENDER_PATH` discriminator still works on `/account` | SC-6 (no regression) | Phase 2 hotfix invariant; requires real Vercel SPA rewrite | 1. Open https://opten.space/account from cold cache. 2. Acceptance: no React #418/#423 warnings in console; AccountPage renders after brief landing flash (existing Phase 2 behavior). |
| OG card unfurl on Telegram | SC-6 (Phase 2 invariant) | Telegram fetches HTML + extracts og:image — only Telegram-side test proves prerender survived. | 1. Paste https://opten.space/, /welcome, /privacy, /terms, /refund into Telegram saved messages. 2. Acceptance: each shows distinct 1200×630 dark hero card and distinct title. |

---

## Validation Sign-Off

- [ ] All tasks have a build-driven or grep-based automated verify, or are explicitly listed in Manual-Only Verifications.
- [ ] Sampling continuity: every commit runs `npm run build` (max latency 30s); no 3 consecutive tasks without an automated verify.
- [ ] Wave 0 (3 npm install commands + verify build still green) covers all infrastructure dependencies.
- [ ] No watch-mode flags (`npm run build` is one-shot).
- [ ] Feedback latency < 90s (build + targeted check).
- [ ] `nyquist_compliant: true` set in frontmatter after planner produces PLAN.md and Wave 0 is complete.

**Approval:** pending (auto-mode — planner will refine task IDs and re-check)

---

## Success Criteria (mapped from ROADMAP.md + RESEARCH.md revisions)

- **SC-1 (treemap):** Main chunk for `/` does NOT contain `chrome.runtime` or `Paddle` references. Treemap (`dist/stats.html` from rollup-plugin-visualizer) shows PayPage / AccountPage / SuccessPage / DownloadSkillPage as separate chunks. **Build-driven.**
- **SC-2 (Lighthouse):** Mobile Performance score ≥ 75 on production (revised from ROADMAP's ≥ 90 per RESEARCH.md ceiling analysis — React + Radix/MUI/motion floor is ~280 KB). **Manual + Lighthouse.**
- **SC-3 (bundle reduction):** Main chunk for `/` reduced by ≥ 10% from baseline 362 KB (revised from ROADMAP's ≥ 40% per RESEARCH.md). Concrete target: 362 KB → ≤ 325 KB. **Build-driven.**
- **SC-4 (CLS):** Cumulative Layout Shift = 0 on `/` per Lighthouse mobile. Achieved via D-04 (width/height attributes). **Lighthouse.**
- **SC-5 (tap latency):** Navbar menu hamburger registers `setOpen` within 500ms of FCP on simulated Slow 4G + 4× CPU throttling. **Manual via Chrome DevTools.**
- **SC-6 (no regression):** All 9 routes still respond. Prerendered routes still have distinct titles/meta/canonical. Paddle modal still opens on `/pay`. `__PRERENDER_PATH` discriminator unchanged. OG card unfurl on Telegram still works. **Mixed: build + manual.**

**Revision note:** Per RESEARCH.md "Achievable Bundle Reduction" section, the ROADMAP's ≥ 40% target was set without bundle treemap data; researcher's empirical run shows ~10% is the realistic ceiling. Phase 2.1 acceptance uses revised targets (SC-2 ≥ 75, SC-3 ≥ 10%) — planner and verifier MUST honor the revised numbers, not the ROADMAP draft.
