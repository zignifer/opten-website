---
phase: 02.1-hydration-and-perf
plan: "07"
subsystem: routing
tags:
  - react-lazy
  - suspense
  - code-splitting
  - main.tsx
  - performance
  - highest-risk
dependency_graph:
  requires:
    - "02.1-03 — RouteLoading.tsx (named export RouteLoading)"
  provides:
    - "src/main.tsx — 4 lazy() route declarations + single Suspense boundary"
    - "dist/assets/PayPage-*.js, AccountPage-*.js, SuccessPage-*.js, DownloadSkillPage-*.js"
  affects:
    - "All route navigations to /pay, /account, /success, /dashboard/download-skill"
    - "Main chunk size for / route (landing page cold load)"
tech_stack:
  added: []
  patterns:
    - "React.lazy(() => import(...)) for SPA-fallback routes only"
    - "Single <Suspense fallback={<RouteLoading />}> wrapping <Routes>"
    - "Vite auto-naming for split chunks (no webpackChunkName)"
key_files:
  created: []
  modified:
    - src/main.tsx
decisions:
  - "D-01 respected: lazy() applied ONLY to PayPage, SuccessPage, AccountPage, DownloadSkillPage (the 4 SPA-fallback routes). App, WelcomePage, PrivacyPage, TermsPage, RefundPage remain static (entry-server.tsx renderToString-safe)."
  - "D-02: Single <Suspense> wraps all <Routes> — no per-route boundaries."
  - "D-08: Paddle bootstrap block (lines 23-45 post-edit) and __PRERENDER_PATH hydration detector (lines 81-89 post-edit) are byte-identical to pre-change baseline."
  - "Phase 2 hotfix 80b16be precedent honored: no lazy-loading of prerendered routes."
metrics:
  duration_minutes: 12
  completed: "2026-05-16"
  tasks_completed: 2
  tasks_total: 3
  files_created: 0
  files_modified: 1
---

# Phase 2.1 Plan 07: React.lazy + Suspense Code Splitting Summary

**One-liner:** React.lazy() code-splits 4 SPA-fallback routes (PayPage/AccountPage/SuccessPage/DownloadSkillPage) into separate chunks, reducing main bundle from 362 KB to 320 KB raw with a single Suspense boundary and byte-identical Paddle/hydration invariants.

## What Was Built

Modified `src/main.tsx` to:
1. Add `import { lazy, Suspense } from "react"` and `import { RouteLoading } from "./app/components/RouteLoading"`.
2. Replace 4 static page imports with `React.lazy()` declarations (PayPage, SuccessPage, AccountPage, DownloadSkillPage).
3. Wrap `<Routes>` in `<Suspense fallback={<RouteLoading />}>`.
4. Keep static imports for App, PrivacyPage, TermsPage, RefundPage, WelcomePage (prerendered routes — renderToString-safe requirement).
5. Left Paddle bootstrap block and `__PRERENDER_PATH` hydration detector byte-identical.

## Tasks

### Task 1: Confirm entry-server.tsx invariant (READ-ONLY)

**Status: PASSED**

`scripts/entry-server.tsx` confirmed clean:
- Static imports present: `App`, `WelcomePage`, `PrivacyPage`, `TermsPage`, `RefundPage` — exactly 5.
- Zero imports of `PayPage`, `SuccessPage`, `AccountPage`, `DownloadSkillPage`.
- The plan can safely lazy-load the 4 SPA-fallback routes without SSR-mismatch risk.

### Task 2: Edit src/main.tsx

**Status: COMPLETE — Commit `a8c5a66`**

**Build output:**
- `npm run build` exits 0 — all 5 build steps passed (client build, SSR cache, SSR meta, prerender 6 routes, sitemap)
- Prerendered routes: `/ → dist/index.html (full)`, `/pay → dist/pay/index.html (head)`, `/welcome`, `/privacy`, `/terms`, `/refund`

**Chunk analysis:**

| File | Size | Type |
|------|------|------|
| `index-D_K0_2sl.js` | 320.26 KB (327,944 bytes) | Main chunk (was 362 KB) |
| `PayPage-DFqrmv4Q.js` | lazy chunk | Contains Paddle.Checkout code |
| `AccountPage-4Xyagnru.js` | lazy chunk | Contains chrome.runtime calls |
| `SuccessPage-Z8jP_kIY.js` | lazy chunk | |
| `DownloadSkillPage-BUPvDe_s.js` | lazy chunk | |
| `svg-bvy0jfb1g6-B4RdHL3Y.js` | shared chunk | SVG assets |

**Bundle reduction:** 362 KB → 320 KB raw (-42 KB, -11.6%)

**Invariant checks:**
- Main chunk ≤ 325 KB: **PASS** (320.26 KB)
- Main chunk contains `chrome.runtime`: **NO** (PASS)
- Main chunk contains `Paddle.Checkout`: **NO** (PASS)
- `__PRERENDER_PATH` in `dist/index.html`: **PASS**
- Paddle CDN sync `<script>` in `dist/index.html`: **PASS**
- `pathMatches` hydration detector in `src/main.tsx`: **PASS**
- Paddle bootstrap byte-identical (lines 23-45): **PASS** (no change via git diff)
- Hydration detector byte-identical (lines 81-89): **PASS** (no change via git diff)

**Commits:**

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 2 | lazy-load SPA-fallback routes via React.lazy + Suspense | a8c5a66 | src/main.tsx (24 ins, 15 del) |

### Task 3: Mobile tap-latency + Paddle modal regression (POST-DEPLOY)

**Status: AWAITING USER ACTION**

This is a `checkpoint:human-verify` task. It cannot be executed autonomously.

**Required steps:**
1. Push local `main` branch to origin: `git push origin main`
2. Wait for Vercel auto-deploy to complete (~30-60s) — check https://opten.space
3. On iPhone Safari (or Chrome DevTools, Slow 4G + 4× CPU throttle):
   - Open `https://opten.space/` from cold cache
   - Within 1s of first dark paint, tap the navbar hamburger
   - Acceptance: menu opens within 500ms (SC-5)
4. Open `https://opten.space/pay` — confirm brief RouteLoading spinner → PayPage renders → Paddle modal opens on CTA click (SC-6)
5. Open `https://opten.space/account` from cold cache — confirm no React #418/#423 warnings (SC-6)
6. Paste `/`, `/welcome`, `/privacy`, `/terms`, `/refund` into Telegram — confirm distinct OG cards (Phase 2 invariant)
7. Run `npx unlighthouse --site https://opten.space` — capture Performance score, LCP, FID, CLS, TBT
   - Acceptance: Performance ≥ 75, CLS = 0 (SC-2)

## Deviations from Plan

None — plan executed exactly as written.

- Paddle bootstrap block: byte-identical (only imports block above it changed)
- Hydration detector: byte-identical
- Route order inside `<Routes>`: byte-identical
- No per-route Suspense added
- No `webpackChunkName` comments added (Vite auto-naming per RESEARCH.md recommendation)
- No modifications to `scripts/entry-server.tsx`

## Known Stubs

None. All lazy routes were fully functional static imports prior — the change is a structural import mechanism swap only. No logic was changed.

## Threat Flags

None beyond those documented in the plan's threat model:
- T-2.1-03: No error boundary for lazy-chunk fetch failure (accepted — same degradation as pre-change unhandled-promise case)
- T-2.1-04: Chunk names visible in Network panel (accepted — no secrets exposed)

## Self-Check: PASSED

- [x] `src/main.tsx` modified with lazy() + Suspense
- [x] `scripts/entry-server.tsx` NOT modified (Task 1 read-only)
- [x] Commit `a8c5a66` exists in git log
- [x] Build exits 0 — all 5 steps
- [x] Main chunk `index-D_K0_2sl.js` = 320.26 KB ≤ 325 KB
- [x] Main chunk: no `chrome.runtime`, no `Paddle.Checkout`
- [x] 4 lazy chunks present: PayPage-DFqrmv4Q.js, AccountPage-4Xyagnru.js, SuccessPage-Z8jP_kIY.js, DownloadSkillPage-BUPvDe_s.js
- [x] `__PRERENDER_PATH` in dist/index.html
- [x] Paddle CDN script in dist/index.html
- [x] hydration detector `pathMatches` unchanged
- [x] No file deletions in commit
- [x] Task 3 is a checkpoint — not executed autonomously
