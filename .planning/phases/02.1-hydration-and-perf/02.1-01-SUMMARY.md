---
phase: "02.1-hydration-and-perf"
plan: "01"
subsystem: "build-infra"
tags:
  - vite
  - imagetools
  - typescript
  - build
dependency_graph:
  requires: []
  provides:
    - "vite-imagetools plugin active in Vite build pipeline"
    - "Picture type available to all src/**/*.ts(x) files via triple-slash reference"
  affects:
    - "vite.config.ts"
    - "src/types/imagetools.d.ts"
tech_stack:
  added:
    - "vite-imagetools@^6.2.9 (devDep — already installed, now registered)"
  patterns:
    - "Vite plugin registration: react() → tailwindcss() → imagetools()"
    - "Triple-slash type reference in src/types/ (mirrors paddle.d.ts pattern)"
key_files:
  created:
    - "src/types/imagetools.d.ts"
  modified:
    - "vite.config.ts"
decisions:
  - "imagetools() registered after tailwindcss() per PATTERNS.md plugin-order rule (D-05)"
  - "No options passed to imagetools() — default config is the locked decision (CONTEXT.md D-05)"
  - "rollup-plugin-visualizer NOT registered in main vite.config.ts — remains a one-off analysis tool per PATTERNS.md guidance"
metrics:
  duration: "~3 minutes"
  completed: "2026-05-16"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 2
---

# Phase 2.1 Plan 01: Register vite-imagetools build plugin (Phase 2.1 D-05 wiring) Summary

**One-liner:** Wired vite-imagetools@6.2.9 into Vite 6 plugin array and published the `Picture` TS declaration so downstream plans can use `?as=picture` query-string imports.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Register imagetools() plugin in vite.config.ts | bf0574d | vite.config.ts |
| 2 | Add src/types/imagetools.d.ts triple-slash reference | bf0574d | src/types/imagetools.d.ts (new) |

Both tasks landed in a single atomic commit: `bf0574d`.

## Build Chain Verification

All 5 build steps passed cleanly after the changes:

| Step | Output | Size | Status |
|------|--------|------|--------|
| `vite build` (SPA client) | `dist/assets/index-nE1Iwdmv.js` | **362.46 KB** (gzip: 105.20 KB) | PASS |
| `vite build --ssr entry-server.tsx` | `.ssr-cache/entry-server.js` | 159.28 KB | PASS |
| `vite build --ssr seo-routes.ts` | `.ssr-meta/seo-routes.js` | 4.54 KB | PASS |
| `node scripts/prerender.mjs` | 6 routes emitted | — | PASS |
| `node scripts/sitemap.mjs` | 6 routes in sitemap.xml | — | PASS |

**Baseline vs post-plan main chunk:** 362 KB → 362.46 KB (delta: +0.46 KB, well within ±2 KB gate). The plugin is a no-op on the module graph until `?as=picture` imports are added in Plan 02.

**dist/index.html integrity:**
- `<script src="https://cdn.paddle.com/paddle/v2/paddle.js">` present
- `window.__PRERENDER_PATH="/"` present
- `<script type="module" crossorigin src="/assets/index-nE1Iwdmv.js">` present

## Files Changed

### `vite.config.ts` (modified)

Added import and plugin registration:
```ts
import { imagetools } from 'vite-imagetools'

plugins: [
  react(),
  tailwindcss(),
  // Phase 2.1 D-05: register vite-imagetools for ?w=...&format=webp;png&as=picture query-string imports
  imagetools(),
],
```

`assetsInclude: ['**/*.svg', '**/*.csv']` unchanged — no conflict with imagetools (operates on different file types).

### `src/types/imagetools.d.ts` (created)

Single line:
```ts
/// <reference types="vite-imagetools" />
```

Mirrors `src/types/paddle.d.ts` pattern. Makes `import type { Picture } from 'vite-imagetools'` resolve under the existing `tsconfig` (which already picks up `src/**/*.d.ts`).

## Deviations from Plan

None — plan executed exactly as written.

## Rollback Path

```bash
git revert bf0574d
npm run build  # verify clean after revert
```

## Threat Flags

None. vite-imagetools runs only at build time (no runtime browser exposure). Version is pinned at `^6.2.9` in devDependencies, consistent with T-2.1-01 mitigation in the plan's threat register.

## Known Stubs

None. This plan introduces no user-visible UI or data.

## Self-Check: PASSED

- [x] `vite.config.ts` contains `import { imagetools } from 'vite-imagetools'` and `imagetools()` call
- [x] `src/types/imagetools.d.ts` exists with `/// <reference types="vite-imagetools" />`
- [x] Commit `bf0574d` exists: `git log --oneline | head -1` → `bf0574d build(02.1): register vite-imagetools plugin + add TS declaration`
- [x] Build chain all 5 steps: PASS
- [x] Main chunk 362.46 KB — within ±2 KB of 362 KB baseline
- [x] No files accidentally deleted in commit
