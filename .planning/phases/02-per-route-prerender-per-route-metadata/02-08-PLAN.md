---
phase: 02-per-route-prerender-per-route-metadata
plan: 08
type: execute
wave: 4
depends_on:
  - 02-per-route-prerender-per-route-metadata/06
  - 02-per-route-prerender-per-route-metadata/07
files_modified:
  - .gitignore
autonomous: false
requirements:
  - GEO-B-1
  - GEO-B-2
  - GEO-B-3
must_haves:
  truths:
    - ".ssr-cache/ is gitignored — Vite SSR build outputs never land in commits"
    - "After deploy to Vercel preview, all per-route HTML files serve at their canonical paths with route-specific <title> + <meta description> + <link rel=canonical>"
    - "Phase 1 + integration-contract invariants hold on the preview deploy (sync Paddle script, 2 JSON-LD blocks, security headers, og-card-ru.png)"
    - "fetch_page.py reports has_ssr_content: true and word_count > 100 on the 5 full-tier routes"
    - "fetch_page.py reports distinct title strings per route"
    - "/account, /success, /dashboard/download-skill continue to respond at their existing paths (locked-route invariant)"
    - "Telegram/Slack/Twitter unfurls on per-route preview URLs render the correct route-specific OG title and description"
  artifacts:
    - path: ".gitignore"
      provides: ".ssr-cache directory excluded from git"
      contains: ".ssr-cache"
  key_links:
    - from: ".gitignore"
      to: "vite build --ssr output"
      via: "directory pattern matching .ssr-cache"
      pattern: ".ssr-cache"
---

<objective>
Close Phase 2 with two activities:

1. **Mechanical fix:** add `.ssr-cache` to `.gitignore` so the intermediate Vite SSR build output never lands in commits.

2. **Acceptance verification (post-deploy):** run the full Verification Recipe from RESEARCH.md against the Vercel preview deploy. Confirm every Phase 2 success criterion is met against a real network response, not just the local `dist/`. This is a NON-AUTONOMOUS plan — it includes a human checkpoint for OG card visual unfurl tests in Telegram/Slack/Twitter (no curl gate can substitute).

Purpose: GEO-B-1, GEO-B-2, GEO-B-3 are conditionally closed by Plans 02-01 through 02-07 (they produce the artifacts); this plan PROVES the artifacts behave correctly in production. If any verification fails, the phase is not done — file a follow-up task and re-plan.

Output: One added line in `.gitignore`. One human-readable verification report appended to the plan's SUMMARY (no separate file). Phase 2 closure status declared based on the verification results.

Risk: LOW for the `.gitignore` edit. HIGH-value for the verification — this is the only gate that catches Vercel-preview-only regressions (e.g., Pitfall 6: static-file precedence vs SPA rewrite).
</objective>

<context>
@.planning/phases/02-per-route-prerender-per-route-metadata/02-CONTEXT.md
@.planning/phases/02-per-route-prerender-per-route-metadata/02-RESEARCH.md
@.planning/phases/02-per-route-prerender-per-route-metadata/02-PATTERNS.md
@.planning/phases/02-per-route-prerender-per-route-metadata/02-VALIDATION.md
@.gitignore
@public/sitemap.xml
@public/robots.txt
</context>

<interfaces>
<!-- Contracts this plan establishes. -->

File: `.gitignore`

EXACT change — insert ONE line after the existing `dist` entry on line 2. Suggested placement: between `dist` (line 2) and `.vite` (line 3). No trailing slash (matches the `dist` precedent on line 2).

Before (lines 1-3):
```
node_modules
dist
.vite
```

After (lines 1-4):
```
node_modules
dist
.ssr-cache
.vite
```

No other lines modified.

---

Post-deploy verification checklist (the executor runs this against the Vercel preview deploy URL once the prior commits are pushed):

For `https://<preview>.vercel.app/...` substitute the actual Vercel preview URL.

A. Per-route distinct titles + canonicals (GEO-B-2)
B. Phase 1 invariants preserved (`<critical_constraints>` items 1, 2)
C. Integration-contract preservation (`<critical_constraints>` items 3, 4)
D. Sitemap.xml has 6 lastmod entries (GEO-B-3)
E. AI-crawler verification via fetch_page.py (CONTEXT.md acceptance criterion)
F. OG unfurl visual check (manual — Telegram/Slack/Twitter)
G. Paddle modal functional on /pay (highest-risk integration check)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add .ssr-cache to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (entire file — 12 lines — for the exact byte-level state before edit)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-PATTERNS.md (`.gitignore` section, lines 333-362; convention: one pattern per line, no trailing slash, no comments)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-RESEARCH.md (Example A — uses `--outDir .ssr-cache`; Pattern Map row `.gitignore` lines 812)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-CONTEXT.md (D-08 — `.ssr-cache/` should be gitignored)
  </read_first>
  <action>
    Insert one new line containing exactly `.ssr-cache` (no trailing slash, no leading whitespace, no quotes) into `.gitignore` between the existing `dist` entry (currently line 2) and the existing `.vite` entry (currently line 3).

    Final file state (12 lines become 13 lines):
    - Line 1: `node_modules` (unchanged)
    - Line 2: `dist` (unchanged)
    - Line 3: `.ssr-cache` (NEW)
    - Line 4: `.vite` (was line 3)
    - Line 5: `*.local` (was line 4)
    - Line 6: `.env` (was line 5)
    - Line 7: `.env.*` (was line 6)
    - Line 8: blank (was line 7)
    - Line 9: `.vercel` (was line 8)
    - Line 10: `.env*.local` (was line 9)
    - Line 11: blank (was line 10)
    - Line 12: `.obsidian/workspace.json` (was line 11)
    - Line 13: `.obsidian/workspaces.json` (was line 12)

    Do NOT:
    - Add a comment before or after the new line (existing `.gitignore` has zero comments — preserve that convention).
    - Add a trailing slash (`/`) on `.ssr-cache` — the `dist` precedent on line 2 has none.
    - Reorder any existing lines.
    - Add other unrelated patterns.

    Then verify: `git check-ignore -v .ssr-cache` returns the matching line (proves the pattern is active). `git status --ignored .ssr-cache` should list it as ignored (after running a build to materialize the directory).
  </action>
  <verify>
    <automated>
      Step 1 (single-line addition): `git diff --numstat .gitignore` reports `1 0 .gitignore` (one line added, zero removed).
      Step 2 (line content exact): `grep -c '^\.ssr-cache$' .gitignore` returns `1`.
      Step 3 (no trailing slash variant): `grep -c '^\.ssr-cache/' .gitignore` returns `0`.
      Step 4 (no comments added): `grep -c '^#' .gitignore` returns `0` (matches pre-edit state).
      Step 5 (pattern is functionally active): run `npm run build` (produces `.ssr-cache/`), then `git status --porcelain .ssr-cache` returns empty (directory exists but is ignored).
      Step 6 (existing entries preserved): `grep -E '^node_modules$|^dist$|^\.vite$|^\*\.local$|^\.env$|^\.env\.\*$|^\.vercel$|^\.env\*\.local$|^\.obsidian/workspace\.json$|^\.obsidian/workspaces\.json$' .gitignore | wc -l` returns `10` (all 10 original patterns still present).
    </automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains exactly one new line: `.ssr-cache` (no trailing slash, no comment, no whitespace).
    - All 10 pre-existing patterns are byte-preserved.
    - After `npm run build`, `git status --porcelain .ssr-cache` returns empty (pattern is functionally ignoring the directory).
    - Diff is exactly +1/-0 lines.
  </acceptance_criteria>
  <done>
    `.ssr-cache/` is committed-safe — future Vite SSR builds produce intermediate artifacts that never accidentally land in git.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Post-deploy acceptance verification on Vercel preview</name>
  <what-built>
    All Plan 02-01 through 02-07 changes shipped:
    - `scripts/seo-routes.ts` — 6-route metadata manifest
    - `scripts/entry-server.tsx` — SSR React tree entry (5 full-tier routes)
    - `src/i18n/LangContext.tsx` — SSR-safe (detectLang moved to useEffect)
    - `scripts/prerender.mjs` — emits per-route HTML
    - `scripts/sitemap.mjs` — emits sitemap.xml with build-time lastmod
    - `package.json` — `build` script chains SPA + SSR + postbuild scripts
    - `src/main.tsx` — hydrateRoot on prerendered routes, createRoot on SPA routes
    - `.gitignore` — `.ssr-cache` excluded (Task 1 above)

    A `git push origin <branch>` triggers a Vercel preview deploy. The preview URL is the verification target.
  </what-built>
  <how-to-verify>
    Substitute `https://<preview>.vercel.app` with the actual Vercel preview URL throughout. All steps below are commands the human executor (or Claude) runs; mark `[PASS]` / `[FAIL]` per item in the SUMMARY.

    **Section A — Per-route distinct titles + canonicals (GEO-B-2):**

    A1. `for route in / pay welcome privacy terms refund; do echo "=== $route ==="; curl -s "https://<preview>.vercel.app$route" | grep -oE '<title>[^<]*</title>' | head -1; done`

    Expected: 6 distinct `<title>` strings, each matching the corresponding entry in `scripts/seo-routes.ts`.

    A2. `for route in / pay welcome privacy terms refund; do echo "=== $route ==="; curl -s "https://<preview>.vercel.app$route" | grep -oE '<link rel="canonical"[^>]*>' | head -1; done`

    Expected: 6 distinct canonical href values, each ending in the route path.

    A3. `for route in / pay welcome privacy terms refund; do echo "=== $route og ==="; curl -s "https://<preview>.vercel.app$route" | grep -E '(og:title|og:description|og:url)' | head -3; done`

    Expected: per-route og:title and og:description match `scripts/seo-routes.ts`.

    **Section B — Phase 1 invariants preserved (`<critical_constraints>` items 1, 2):**

    B1. For each route: `curl -s "https://<preview>.vercel.app/welcome" | grep -c '<script type="application/ld+json">'` returns `2` (both JSON-LD blocks intact). Repeat for `/`, `/pay`, `/privacy`, `/terms`, `/refund`.

    B2. For each route: `curl -s "https://<preview>.vercel.app/welcome" | grep -c 'cdn.paddle.com/paddle/v2/paddle.js'` returns `1` (synchronous Paddle script tag). Repeat for all 6 routes.

    B3. For each route: `curl -s "https://<preview>.vercel.app/welcome" | grep -E 'paddle\.js"\s+(async|defer)'` returns nothing (sync only). Repeat for all 6 routes.

    B4. For each route: `curl -s "https://<preview>.vercel.app/welcome" | grep -c 'rel="preconnect" href="https://cdn.paddle.com"'` returns at least `1` (preconnect preserved).

    B5. For each route: `curl -s "https://<preview>.vercel.app/welcome" | grep -c 'og-card-ru.png'` returns at least `2` (og:image + twitter:image — Phase 2 keeps site-wide).

    **Section C — Integration-contract preservation (`<critical_constraints>` items 3, 4):**

    C1. Locked SPA routes still respond:
    - `curl -sI "https://<preview>.vercel.app/account" | head -1 | grep -q '200'` exits 0.
    - `curl -sI "https://<preview>.vercel.app/success" | head -1 | grep -q '200'` exits 0.
    - `curl -sI "https://<preview>.vercel.app/dashboard/download-skill" | head -1 | grep -q '200'` exits 0.

    C2. SPA fallback delivers SPA shell to locked routes (verify by ensuring `<div id="root">` is empty in the served HTML):
    - `curl -s "https://<preview>.vercel.app/account" | grep -oE '<div id="root">[^<]*'` shows `<div id="root">` (empty inner — the SPA rewrite delivered `dist/index.html`).

    C3. No leaked integration-contract constants in the prerendered HTML (the CWS install URL in JSON-LD is the only legitimate appearance):
    - `curl -s "https://<preview>.vercel.app/welcome" | grep -c 'iphkppgbobpilmphloffcalicmejacfl'` returns at most `1` (the SoftwareApplication JSON-LD installUrl — index.html line 65).
    - `curl -s "https://<preview>.vercel.app/welcome" | grep -c 'vuywydhwkqmihfztpkgl.supabase.co'` returns `0` (Supabase URL must NOT leak into prerendered HTML).

    C4. Phase 1 security headers still present (regression check — Plan 02-06 did not touch vercel.json):
    - `curl -sI "https://<preview>.vercel.app/" | grep -i 'x-content-type-options: nosniff'` returns one line.
    - `curl -sI "https://<preview>.vercel.app/" | grep -i 'referrer-policy: strict-origin-when-cross-origin'` returns one line.
    - `curl -sI "https://<preview>.vercel.app/" | grep -i 'permissions-policy'` returns one line containing `payment=(self`.

    **Section D — Sitemap.xml has 6 lastmod entries (GEO-B-3):**

    D1. `curl -s "https://<preview>.vercel.app/sitemap.xml" | grep -c '<lastmod>'` returns `6`.

    D2. `curl -s "https://<preview>.vercel.app/sitemap.xml" | grep -oP '<lastmod>\K[0-9]{4}-[0-9]{2}-[0-9]{2}' | sort -u | wc -l` returns `1` (uniform date — D-04).

    D3. Static files still served with correct MIME types (Phase 1 regression check):
    - `curl -sI "https://<preview>.vercel.app/sitemap.xml" | grep -i 'content-type' | grep -E 'application/xml|text/xml'` returns one line.
    - `curl -sI "https://<preview>.vercel.app/robots.txt" | grep -i 'content-type: text/plain'` returns one line.
    - `curl -sI "https://<preview>.vercel.app/llms.txt" | grep -i 'content-type: text/plain'` returns one line.

    **Section E — AI-crawler verification via fetch_page.py (CONTEXT.md primary acceptance):**

    E1. For each full-tier route (`/`, `/welcome`, `/privacy`, `/terms`, `/refund`):
    `~/.claude/skills/geo/scripts/fetch_page.py https://<preview>.vercel.app/<route>`
    Expected for each: `has_ssr_content: true`, `word_count > 100`, `h1_tags` is a non-empty array, `structured_data` count = 2, `title` is the route-specific title (NOT the default landing title for non-`/` routes).

    E2. For `/pay` (head-only tier):
    `~/.claude/skills/geo/scripts/fetch_page.py https://<preview>.vercel.app/pay`
    Expected: `has_ssr_content: false` (body intentionally empty per D-02), `structured_data` count = 2 (JSON-LD preserved), `title` is the `/pay` route-specific title from `scripts/seo-routes.ts` (NOT the landing title — proves head-only injection worked).

    **Section F — OG unfurl visual check (manual; NO curl substitute):**

    F1. Paste `https://<preview>.vercel.app/welcome` into a Telegram chat. Confirm the unfurl card shows:
    - The og-card-ru.png 1200×630 image (NOT the favicon)
    - The `/welcome` route-specific og:title (matching `scripts/seo-routes.ts` welcome entry)
    - The `/welcome` route-specific og:description

    F2. Repeat F1 for `https://<preview>.vercel.app/privacy` — should show the `/privacy` route's og:title and og:description with the same og-card-ru.png.

    F3. Repeat F1 against Slack — confirm same per-route title/description rendering.

    F4. (Optional) Paste into Twitter / X — confirm twitter:title and twitter:description show route-specific values.

    **Section G — Paddle modal functional on /pay (highest-risk integration check):**

    G1. Visit `https://<preview>.vercel.app/pay` in a browser.
    - Open DevTools Console.
    - Expected: no hydration warnings (the `/pay` body is empty, so `hasChildNodes()` returns false, `createRoot` runs — same path as pre-Phase-2).
    - Expected: no `ReferenceError`, no Paddle bootstrap errors.

    G2. On the `/pay` page, click the USD pricing CTA.
    - Expected: Paddle Checkout modal opens with the correct priceId.
    - Expected: NO errors in the console (`window.Paddle.Checkout.open` succeeds).

    G3. (Optional, only if you have a sandbox CC available) Complete the checkout flow to confirm the post-payment redirect to `/success` still works (locked route).

    **Result:** mark each section A-G in the SUMMARY file with PASS/FAIL. If ANY item fails, do NOT mark the phase as closed — open a follow-up plan in this phase directory.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any FAILed verification item with the specific curl/fetch_page.py output that failed.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (autonomous):

1. `git diff .gitignore` shows exactly one added line `.ssr-cache`.
2. `npm run build && git status --porcelain .ssr-cache` returns empty (pattern is active).

After Task 2 (human verification):

The executor reads the verification report appended to the SUMMARY and confirms:
- Sections A, B, C, D, E all PASSED via curl + fetch_page.py.
- Section F (OG unfurl) PASSED via Telegram and Slack visual check.
- Section G (Paddle modal) PASSED via in-browser test on `/pay`.

If any section reports FAIL, the phase is NOT closed — file a follow-up plan in this directory referencing the specific failure.
</verification>

<success_criteria>
- `.gitignore` excludes `.ssr-cache` (autonomous, mechanical).
- All 7 verification sections (A through G) report PASS against the Vercel preview deploy.
- GEO-B-1 closed: build-time prerender strategy delivered (per-route HTML files exist with route-specific metadata).
- GEO-B-2 closed: per-route `<title>`, `<meta description>`, canonical, OG tags shipped on all 6 marketing routes.
- GEO-B-3 closed: `dist/sitemap.xml` carries 6 `<lastmod>` entries with uniform build-time ISO date.
- All `<critical_constraints>` items (1-7) hold on the live preview deploy.
- Locked routes (`/welcome`, `/pay`, `/success`, `/account`, `/dashboard/download-skill`) all respond at their existing paths with status 200.
- Paddle modal still functional on `/pay`.
- Phase 1 security headers still present.
- OG unfurl cards render per-route metadata visually.
</success_criteria>

<output>
After Task 1 completion, commit with message: `chore: gitignore .ssr-cache (Vite SSR build output)`.

Task 2 has no commit (it's a verification activity). Instead, append the verification report to `.planning/phases/02-per-route-prerender-per-route-metadata/02-08-SUMMARY.md` with each Section A-G entry tagged PASS/FAIL and the timestamp/Vercel preview URL used.

If ALL sections PASS: update `.planning/STATE.md` (Phase 2 status → complete) and `.planning/ROADMAP.md` (mark Phase 2 plan items as `[x]`). If ANY section FAILS: do NOT update STATE.md or ROADMAP.md; file a follow-up plan in the same phase directory describing the failure and the proposed fix.
</output>
