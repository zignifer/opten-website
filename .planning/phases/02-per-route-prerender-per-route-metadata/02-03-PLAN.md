---
phase: 02-per-route-prerender-per-route-metadata
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/LangContext.tsx
autonomous: true
requirements:
  - GEO-B-2
must_haves:
  truths:
    - "LangProvider's initial useState value is the string literal \"ru\" (no function call) — safe under renderToString"
    - "detectLang() is invoked inside a useEffect that runs only on the client mount"
    - "SSR build of any consumer wrapping <LangProvider> no longer throws ReferenceError on localStorage or navigator"
    - "Client behavior is unchanged for end users — RU/EN detection still happens, just one microtask later"
  artifacts:
    - path: "src/i18n/LangContext.tsx"
      provides: "SSR-safe LangProvider component"
      contains: "useState<Lang>(\"ru\")"
  key_links:
    - from: "src/i18n/LangContext.tsx"
      to: "scripts/entry-server.tsx (Plan 02-02)"
      via: "LangProvider wrap that no longer crashes in Node"
      pattern: "useEffect.*setLangState.*detectLang"
---

<objective>
Surgical 2-line edit to `src/i18n/LangContext.tsx` to remove the SSR crash blocker (RESEARCH.md Pitfall 1). Move `detectLang()` out of the `useState` initializer (which fires eagerly during `renderToString`, reading `localStorage` and `navigator` — both undefined in Node) and into a `useEffect` that runs only on the client.

Purpose: Unblocks SSR compilation of `scripts/entry-server.tsx` (Plan 02-04 + 02-06). Without this fix, `vite build --ssr scripts/entry-server.tsx` and `node scripts/prerender.mjs` would throw `ReferenceError: localStorage is not defined` at the postbuild step. Mitigation is non-controversial and matches D-05 (prerender RU, hydrate to user preference).

Output: Two changes to one file. Line 30 changes from `useState<Lang>(detectLang)` to `useState<Lang>("ru")`. A new `useEffect` block is inserted that calls `setLangState(detectLang())` on mount.

Risk: LOW. Behavior change is one extra render on EN-locale clients (the RU→EN flip happens as a normal post-mount state update, no hydration warning). RU-locale clients see no change. SSR-safe path is the canonical React 18 pattern.
</objective>

<context>
@.planning/phases/02-per-route-prerender-per-route-metadata/02-CONTEXT.md
@.planning/phases/02-per-route-prerender-per-route-metadata/02-RESEARCH.md
@.planning/phases/02-per-route-prerender-per-route-metadata/02-PATTERNS.md
@src/i18n/LangContext.tsx
</context>

<interfaces>
<!-- This plan modifies an existing module. The PUBLIC interface of LangContext.tsx is UNCHANGED. -->

Module: `src/i18n/LangContext.tsx`

Public exports (unchanged):
- `export type Lang = "ru" | "en"`
- `export const LangContext`
- `export function LangProvider({ children }: { children: React.ReactNode })`
- `export function useT(): (key: string) => string`
- `export function useLang(): { lang: Lang; setLang: (l: Lang) => void }`

Internal change (this plan):
- The `useState` initializer in `LangProvider` no longer calls `detectLang()` synchronously.
- A new `useEffect(() => { setLangState(detectLang()); }, [])` is added immediately after the `useState` declaration.
- `detectLang()` function body itself is UNCHANGED (lines 9-13).
- `useEffect` is already imported on line 1 — no import change required.

Behavioral contract for downstream consumers:
- Initial render (SSR or client): `lang === "ru"`.
- Post-mount tick (client only): `lang` switches to the user's stored preference or `navigator.language`-derived value via a normal React state update.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Patch src/i18n/LangContext.tsx — move detectLang() into useEffect</name>
  <files>src/i18n/LangContext.tsx</files>
  <read_first>
    - src/i18n/LangContext.tsx (entire file — 59 lines — for exact byte-level state before edit)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-RESEARCH.md (Pitfall 1, lines 382-406; Example D, lines 635-658; Pitfall 2 — the acceptable RU→EN flash on EN-locale machines)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-PATTERNS.md (`src/i18n/LangContext.tsx` section, lines 156-193 — including the "Lines that must NOT change" table)
    - .planning/phases/02-per-route-prerender-per-route-metadata/02-CONTEXT.md (D-05 — RU-only in Phase 2 locks the SSR default to "ru"; D-06 hydration policy)
  </read_first>
  <action>
    Surgical edit to `src/i18n/LangContext.tsx`. ONLY change line 30 and insert a useEffect block immediately after it. All other lines (1-29 and 32-59) MUST remain byte-identical.

    EXACT change to line 30:
    - Before: `  const [lang, setLangState] = useState<Lang>(detectLang);`
    - After: `  const [lang, setLangState] = useState<Lang>("ru"); // Phase 2 D-05: SSR-safe RU default; detection runs in useEffect below.`

    EXACT insertion: immediately after the modified line 30 (which becomes the new line 30), insert a blank line and then a 4-line useEffect block. The result around lines 30-35 should read:

    Line 30: `  const [lang, setLangState] = useState<Lang>("ru"); // Phase 2 D-05: SSR-safe RU default; detection runs in useEffect below.`
    Line 31: `` (blank)
    Line 32: `  useEffect(() => {`
    Line 33: `    // detectLang() reads localStorage + navigator — defer to client mount (RESEARCH.md Pitfall 1).`
    Line 34: `    setLangState(detectLang());`
    Line 35: `  }, []);`

    The existing blank line at current line 31 and the `setLang` function block (currently lines 32-35) shift down accordingly. Use two-space indent matching the rest of the file. Preserve all whitespace, semicolons, and quote styles (double quotes throughout the file).

    Do NOT:
    - Modify lines 1-29 (imports, `detectLang` body, dicts, interface, context default).
    - Modify the `setLang` function body (currently lines 32-35, will be lines 37-40 after insertion).
    - Modify the existing `useEffect` that syncs `document.documentElement.lang` (currently lines 37-39, will be 42-44).
    - Modify any of `t`, `LangProvider` JSX return, `useT`, `useLang`.
    - Add or remove imports. `useEffect` is already imported on line 1.
    - Wrap `detectLang()` in `typeof window !== "undefined"` guards. The useEffect pattern is sufficient (RESEARCH.md "Alternative (rejected)").
  </action>
  <verify>
    <automated>
      Step 1 (file still parses + build passes): `npm run build` exits 0.
      Step 2 (initializer is a string literal, not a function reference): `grep -c 'useState<Lang>("ru")' src/i18n/LangContext.tsx` equals `1`.
      Step 3 (old initializer is gone): `grep -c 'useState<Lang>(detectLang)' src/i18n/LangContext.tsx` equals `0`.
      Step 4 (new useEffect calls detectLang via setLangState): `grep -c 'setLangState(detectLang())' src/i18n/LangContext.tsx` equals `1`.
      Step 5 (detectLang function body unchanged — still reads localStorage and navigator): `grep -c 'localStorage.getItem(STORAGE_KEY)' src/i18n/LangContext.tsx` equals `1` AND `grep -c 'navigator.language.startsWith("ru")' src/i18n/LangContext.tsx` equals `1`.
      Step 6 (no SSR-unsafe guards introduced — useEffect pattern is the chosen mitigation): `grep -E 'typeof window' src/i18n/LangContext.tsx` returns nothing (exit 1).
      Step 7 (useEffect is still imported once): `grep -c 'useEffect' src/i18n/LangContext.tsx | head -1` is at least `3` (1 import, 2 hooks — the new one for detect and the existing one for document.documentElement.lang).
      Step 8 (existing document.documentElement.lang useEffect preserved): `grep -c 'document.documentElement.lang = lang' src/i18n/LangContext.tsx` equals `1`.
      Step 9 (public API unchanged — same exports): `grep -E '^export (type|const|function) (Lang|LangContext|LangProvider|useT|useLang)' src/i18n/LangContext.tsx | wc -l` equals `5`.
      Step 10 (SSR smoke test — optional but recommended): `node --input-type=module -e "import('react-dom/server').then(m => console.log('react-dom/server loaded:', typeof m.renderToString))"` prints `react-dom/server loaded: function`. Note: a full SSR render of LangProvider can only be tested once Plan 02-04 lands the postbuild script — at this point we rely on `npm run build` + grep gates.
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (existing client behavior preserved — no TS errors, no runtime regressions in the SPA).
    - Line containing `useState<Lang>("ru")` exists exactly once.
    - Line containing `useState<Lang>(detectLang)` exists zero times.
    - A `useEffect` block exists whose body calls `setLangState(detectLang())` (single grep match).
    - `detectLang` function body (lines 9-13 in the original file) is byte-identical to before the edit — same `localStorage.getItem(STORAGE_KEY)` call, same `navigator.language.startsWith("ru")` check.
    - The existing `useEffect` syncing `document.documentElement.lang` is preserved exactly.
    - The 5 public exports (`Lang`, `LangContext`, `LangProvider`, `useT`, `useLang`) are all still exported with unchanged signatures.
    - No `typeof window` guard introduced (the useEffect pattern is sufficient per RESEARCH.md Pitfall 1 mitigation).
    - No new imports added (`useEffect` was already imported on line 1).
  </acceptance_criteria>
  <done>
    `src/i18n/LangContext.tsx` is SSR-safe. `npm run build` exits 0. The file is now ready for inclusion in the SSR build (Plan 02-04/06): when `vite build --ssr scripts/entry-server.tsx` runs, `LangProvider` will render under `renderToString` without throwing `ReferenceError: localStorage is not defined`. End-user behavior is preserved: RU-locale users see no change; EN-locale users see one extra post-mount render (the acceptable Pitfall-2 RU-flash documented in CONTEXT.md `<code_context>`).
  </done>
</task>

</tasks>

<verification>
After the task commit:

1. `npm run build` exits 0.
2. `grep -c 'useState<Lang>("ru")' src/i18n/LangContext.tsx` returns `1`.
3. `grep -c 'useState<Lang>(detectLang)' src/i18n/LangContext.tsx` returns `0`.
4. `grep -c 'setLangState(detectLang())' src/i18n/LangContext.tsx` returns `1`.
5. Manual smoke test: `npm run preview &` then `curl -s http://localhost:4173/ | grep -c '<title>'` returns at least `1` (the SPA still renders correctly; per-route titles arrive in Plan 02-04).
</verification>

<success_criteria>
- LangContext is SSR-safe: a hypothetical `renderToString(<LangProvider>...)` call in Node would no longer throw on `localStorage` or `navigator`.
- Client behavior is preserved end-to-end: RU-locale users see no observable change; EN-locale users may see a one-tick RU flash (acceptable per Pitfall 2, documented decision).
- The public API of `LangContext` is unchanged.
- `npm run build` exits 0.
</success_criteria>

<output>
After completion, commit with message: `fix(i18n): defer detectLang() to useEffect for SSR safety (GEO-B-2 pre-req, RESEARCH.md Pitfall 1)`.

Create `.planning/phases/02-per-route-prerender-per-route-metadata/02-03-SUMMARY.md` recording: lines changed (line 30 + insertion of 4-line useEffect), unchanged-lines audit (1-29, 32-59 byte-identical), and the acceptable behavior change for EN-locale clients.
</output>
